<!--
Ordered by dependency, and the first group is not implementation. Group 1 settles the unknowns that
design §6 puts on the terminal's critical path; groups 5, 8 and 9 are sized differently depending on
their answers, so nothing there is built before it is measured.

Updated 2026-08-17: 1.1 and 1.2 are DONE and their answer is negative — a running session cannot be
adopted by resuming it (design §6.1). The shrink that answer entitled group 8 to was declined by the
user, so the group keeps its planned size and 1.5 carries the search for another route. Four
requirements were added in that round — a refusal to resume a live session, an agent that registers
nothing, waiting-for-a-person as a state, and the orphaned waiters the design had measured but no
task had covered (the user placed them here rather than in a change of their own) — with their tasks
and acceptance criteria below.

Updated 2026-08-18 for the work-cycle engine change, which lands before this one: it OWNS the data
this screen was going to guess at. Five places moved — the goal and progress an agent reports, the
capability report, starting a unit, answering an open decision, and a project stopped on a question
with no agent running. Everything the engine supplies degrades to the same behaviour this change
already defines for a missing bus: absent, stated, never inferred.

Layer marking, per .claude/rules/code-quality.md: every task says CORE (lib/set_orch/, abstract),
API (lib/set_orch/api/) or WEB (web/). No task puts project-type knowledge into CORE.
-->

## 1. Measure first — what decides the terminal's reach

- [x] 1.1 CORE — Measure whether a running agent session can be adopted into a framework-owned pseudo-terminal by resuming it: start a session by hand, resume it into an owned pty, and check that its history is intact and that input reaches it. Record the exact command and its output. **DONE 2026-08-17 — the answer is no.** Resuming a live session returns exit 0, the intact history and the same session id, appends to the same log, and forks the conversation into a second branch the running original never sees. Nothing reports it. [REQ: adoption-of-a-running-session-is-measured-before-it-is-relied-upon]
- [x] 1.2 Write the answer into `design.md` §6 — including a negative one, with what was run and what it returned. A negative result shrinks group 8 and is a finding, not a failure. **DONE 2026-08-17 — written up as design §6.1**, with the commands, the observation table and the log's branch structure. ⚠ The group-8 shrink it anticipated was **declined by the user**: the terminal stays at its planned size and adoption is pursued by another route (design §5.2), so no task below was cut on the strength of this. [REQ: adoption-of-a-running-session-is-measured-before-it-is-relied-upon]
- [x] 1.3 CORE — Encode the measured default, which 1.1 turned from provisional into settled: every session the framework did not start reports no terminal. **DONE 2026-08-19** — encoded structurally rather than as a rule: `population_of()` answers about the HANDLE this owner holds, so it cannot drift out of agreement with reality, and the listing carries it per agent. It has three values because the default has an honest gap: an owner that cannot be asked yields `unknown`, never `foreign`. Reporting `foreign` there would state the default as a fact about agents the owner was holding a second ago. [REQ: adoption-of-a-running-session-is-measured-before-it-is-relied-upon]
- [x] 1.4 Re-measure the three counts the design rests on (live sessions, sessions with a registry seat, sessions with a live waiter) and note them with today's date. They were taken on one machine on one day; a task list built on a stale count inherits it silently. Resolve every process match to an identity — a bare `pgrep` count includes the counting command itself, which is how the waiter figure was first wrong. **DONE 2026-08-18 — all three moved, and one grew a shape nobody planned for.** 21 live sessions (was 12); 11 with a registry seat (52 %, was 9 of 12); 6 with a live waiter of their own (was 4 of 12); 17 orphaned waiters (was ~30). A naive command-line match returned 31 false positives, all shell snapshots — the identity rule held. **One session owns four waiters at once**, so a missing-waiter check must count rather than test presence. Written up as design §6.3. [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire]
- [x] 1.5 CORE — Measure the remaining adoption route before anything depends on it: whether the runtime's own cross-session channel, or a remote-control mode, can reach a session that is **already running**. Both are known to be enabled where a session starts; neither is known to attach to one afterwards. A negative here is a finding, not a failure. **DONE 2026-08-18 — the channel reaches a running session but does not attach to one.** Input is delivered, an idle peer wakes, a reply returns as a message; there is no output stream, no pty, no way to observe the peer's screen. Remote Control is **not measured** — the account has used it but nothing is connected here, so no remote row was listed; not measured is not negative. Design §6.3. [REQ: adoption-of-a-running-session-is-measured-before-it-is-relied-upon]
- [x] 1.6 Re-measure the delivery outcomes against the runtime's own channel, not only the framework's bus — the fourth outcome (held for the recipient's human, and expiring on its own) was found there and exists in no other source. **DONE 2026-08-18 — the fourth outcome reproduced, and the measurement corrected design §3 in the dangerous direction.** The runtime channel sees 21 of 21 live sessions against the framework bus's 11, verifies the sender's **pid** from the socket while marking the name a claim, and returns a real refusal for an unresolvable recipient instead of a constant. **But a delivered send and a held send return the identical `success: true`** — so the send call reports *acceptance for delivery*, not delivery, and no tile may render its answer as an outcome. The true outcome arrives **asynchronously ~4–5 min later** as a runtime notice keyed by the recipient's verified socket, not by the returned `msg_id`. **Expiry measured too, as a SECOND notice** ~6 min after the send — so one instruction has three states, accepted → held → expired, and the middle one is not terminal; a surface that renders the first notice and stops shows *held* for a dead message. Design §6.3. [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire]

## 2. Discovery — the inventory (CORE)

- [x] 2.1 New module `lib/set_orch/fleet/discovery.py`: enumerate live agent processes and read each one's working directory. Do not match project paths inside command lines. **DONE 2026-08-18** — `lib/set_orch/fleet/discovery.py`. Identity from `/proc/<pid>/comm`, project from `cwd`; a naive command-line match was re-measured at **31 false positives** the same day and a test holds that wrong pattern. Finding CB-8 folded in: a `-p` subprocess is classified `oneshot` and excluded by default rather than tiled. [REQ: an-agent-is-discovered-from-process-state-not-from-a-command-line]
- [x] 2.2 Resolve a working directory to a project through the repository's common git directory, so every worktree of one repository resolves to one project; report the branch per agent. **DONE 2026-08-18** — resolved through `git rev-parse --git-common-dir`, so every worktree of one repository reports one project; branch reported per agent. Verified live: two sessions in one worktree collapsed to one project, 22 agents across 12 projects. [REQ: a-working-directory-resolves-to-a-project-through-git-not-by-path-matching]
- [x] 2.3 Bind a process to its session log from the messaging registry's recorded binding; mark any heuristic fallback as unconfirmed, and prefer no binding to an arbitrary one. **DONE 2026-08-18, from a BETTER source than the task named.** The binding comes from the runtime's own per-session record (`~/.claude/sessions/<pid>.json`), not from the messaging registry: measured that day, **23 records to 23 live processes, one to one, nothing stale**, against the framework registry's 11 of 21. So the 4-of-9 heuristic is not merely labelled, it is unnecessary — there is no guessing path at all, and `binding_confirmed` is False only when no record exists. [REQ: a-process-is-bound-to-its-session-log-by-recorded-fact-and-a-guess-says-so]
- [x] 2.4 Build the project list as the union of project registry, messaging registry and live process working directories, and report per entry which sources knew about it. **PARTIAL 2026-08-18 — two of the three sources.** The union covers the project registry and live process working directories, and names them per entry (`sources`). The **messaging registry is not yet a source**, so a project known only to it is still missing. Deliberately left open rather than marked done: the union's whole value is that it is complete. [REQ: the-inventory-is-a-union-of-its-sources-and-names-them] **COMPLETED 2026-08-19 — the third source is in, and the gap it closed was bigger than the task implied.** Measured before building it: **8 of 49** project roots were known ONLY to the messaging registry, and every one of the eight is a directory that exists on disk with a seat enrolled against it. That is a sixth of the estate, and the screen showed no sign of it — nothing marked missing, the list simply ended, which is the false-absence class at the level of the whole surface. The union now returns **49** where it returned 41. **Three decisions, each held by a test.** The messaging entries are merged SECOND, so a project the project registry also knows keeps the name a person chose and the `archived` flag every other surface honours, and merely gains `messaging` in `sources` — a merge that overwrote either would quietly return an archived project to the screen under a different name, two false values at once and neither looking like an error. The reader **fails open**: absent file, unparseable JSON, a reshaped envelope, a seat with no project, a seat that is not a map — six cases, all yielding an empty list rather than an exception, because this is one contributor to a union and the other two must still answer. The cost of that choice is stated rather than hidden: an empty answer and an absent registry are indistinguishable to the caller, so the honest report is *the messaging registry named no project*, never *there is no messaging registry*. And it honours **the same env var the messaging system itself honours**, so an isolated store is isolated for this reader too — otherwise a test that believes it is sandboxed reads the developer's real registry, and the first sign of that is a name from another repository on screen. **Confidentiality:** the registry names projects outside this framework, so nothing read here is logged — the debug line carries a count, not a list, and a mutation that turns it back into a list is caught. The detector is proven to fire, so its zero means something. Six mutations, all caught. ⚠ **And the union was not the last word — a FILTER undid it, silently, one loop later.** The listing dropped every project with no live agent and no registry entry. That condition names ONE source, which was harmless while there were two and every entry had either an agent or a registration; the third source broke it without a word. Measured: the union returned 49 and the endpoint served **41** — the eight new projects passed the union and vanished immediately after it, and the in-process call reported the correct 49 while HTTP reported 41, so the two obvious checks disagreed and neither was wrong. The condition now asks whether ANY source named the project, which is the rule the union was built on; the mutation back to the source-specific form is caught. **The lesson is not the line, it is where to look:** completing a union means auditing every step downstream of it, because a narrowing filter looks exactly like a source that returned nothing.
- [x] 2.5 Walk process ancestry to the first agent ancestor; report the parent by seat identity, never by pid, and derive directing/executing from the relation. **DONE 2026-08-19 — and the ancestry walk alone CANNOT answer what this task is for, which is measured rather than argued.** `parent_seat()` climbs through non-agent processes (an agent running `claude` from its own shell is two links away) and parses `/proc/<pid>/stat` after the last `)`, because a comm may contain spaces and brackets and a whitespace split reads the wrong field for exactly the processes a walk passes through. ⚠ **But: 0 of 23 live agents had an agent ancestor**, and an agent started from the fleet screen has the OWNER — a plain python process — as its parent, with systemd above that. So no walk will ever find which agent asked for a framework-started one. That relation exists only at the moment of the act, so it is **recorded during it**: `requested_by` travels start → owner → listing. The payload's `parent` carries a `source` of `recorded` or `ancestry`, because the two answer different questions and can disagree — and the recorded one is a **claim the framework did not verify**, which the surface must not render as a measured relation. [REQ: an-agent-started-by-another-agent-is-identified-as-its-descendant]
- [x] 2.6 Report per project which framework capabilities are connected, derived from files present; keep the capability set data, and distinguish not-connected from unknown. **DONE 2026-08-19** — `lib/set_orch/fleet/capabilities.py`. **FOUR states, not three**, and the fourth was found by looking at real output: `partial`. With `connected = any file present`, two module capabilities reported themselves connected on a **single file they share with a third** — *already wired in* about a project that is not, in the reassuring direction. The requirement says a capability is connected when *the files* that constitute it are present, plural, and now it means that. The capability set is **DATA**: the core rules come from the directory that ships them and each module's files from that module's manifest, so adding a rule changes the report with nothing here edited — a mutation that freezes it into a hand-kept list is caught. Target paths come from the **deploy engine's own** `_target_path`, not a second copy of the mapping. ⚠ **`.claude/` is not a signal** — measured 12 of 12 before anything was built, because every project somebody opens an agent in has one; a test asserts a project with only `.claude/` reports zero. ⚠ **And a one-liner made every capability read `not-connected` until the output was looked at:** `.as_posix().lstrip("./")` strips a SET OF CHARACTERS, not a prefix, so `.claude/rules/x.md` became `claude/rules/x.md` — a path no project has. Invisible in the code, obvious in the result; held by a test. 5/5 mutations caught. Measured live on three real projects: 0 connected, everything present is `partial`, and **0 ledgered anywhere**. [REQ: a-project-reports-what-it-has-wired-in-and-dim-is-not-absent]
- [x] 2.7 Assert the non-persistence boundary in code: no session-derived content reaches disk, cache or log, and diagnostics name file and failure kind only. **DONE 2026-08-19 — DRIVEN rather than asserted about.** A test that scanned the source for `open(...)` would be a substring check wearing a guarantee: it passes while a logger formats a conversation into a line, and it fails on a docstring that mentions writing. `tests/unit/test_fleet_persistence_boundary.py` instead runs the real read paths over a session log carrying a distinctive marker shaped like the content this rule exists for — a partner name in a business sentence — and then looks for it in the two places content escapes to: the filesystem and the log records, **filenames included**. The boundary is persistence and not naming, so the test first proves the marker REACHED the reader; otherwise it would pass by reading nothing, which is the shape that proves nothing. A third test shows the detector a planted leak, because a check that reports clean is indistinguishable from one that cannot report anything. Mutation: a diagnostic that dumps the file's head to aid debugging — the realistic way this line gets crossed — is caught. [REQ: nothing-derived-from-an-agent-s-session-is-persisted]
- [x] 2.8 CORE — List a live agent that has no registry entry and no session log at all, from process discovery alone, naming the sources that lacked it. **DONE 2026-08-19.** The listing half already held; what was missing is the NAMING, and it is not derivable from `sources` alone — a shorter list is only meaningful against the set that was consulted, so "known to one source" and "known to one of three" rendered identically, and the second is the one worth looking at. `sources_missing` is the complement of a stated constant rather than of whatever a caller passed, so a source that stops being consulted leaves BOTH lists instead of silently becoming a permanent absence. ⚠ Measured the same day: **0 of 23** live agents were known from the process alone — all had a session record — so this is driven from a fixture and the live case is not claimed to have been seen. Two measured causes: a session started as another session's child (its transcript is off and it never registers), and a session still at its start-up trust prompt. Both are alive, both are invisible to the native source. [REQ: an-agent-that-registers-nothing-is-still-an-agent]
- [x] 2.9 CORE — ⚠ **Measured 2026-08-18 (design §6.4): the record exists for 1 project of 12, and covers 4 of that project's 16 rule files — so `inferred` is the NORMAL case here, not the footnote this task implies, and `un-ledgered` must not render as `stale`: for those files the framework cannot separate a project's own edit from its own drift, and *cannot tell* is the honest report.** Take the capability report from the project's module install record where one exists — modules and their versions — and report a version the project expects but does not have as a mismatch. Fall back to inferring from files only where no record exists, and mark such an entry as inferred. A declaration and a guess answer different questions. **PARTIAL 2026-08-19, and the task's own inversion is now IN THE DELIVERY.** Built: the project declaration is consulted FIRST and only then file presence (`declared: true/false` travels with the report), the install ledger separates `ledgered` from `inferred` per capability, and **un-ledgered is never rendered as stale** — the reason string says the framework *cannot tell* a project edit from its own drift, and a mutation to the word `stale` is caught. Measured live: **0 ledgered files across three real projects and no declaration anywhere**, so `inferred` is not the footnote this task assumed — it is the whole answer. NOT built: the **version-mismatch** half. `module_install.version_report` exists and is called, but nothing has been driven against a project that expects a version it does not have, so that path is carried and unproven. ⚠ **A neighbouring finding, recorded rather than fixed here because it belongs to the module-install capability:** `read_project_declaration` logs the ABSOLUTE PROJECT PATH when no declaration is found. The first version of this module's confidentiality test asserted over every log record and failed on that line; widening the assertion to pass would have hidden the finding AND stopped guarding this module, so the test is scoped to this module's logger and a second test holds the neighbouring behaviour so the note cannot rot. [REQ: a-project-reports-what-it-has-wired-in-and-dim-is-not-absent] **COMPLETED 2026-08-19 — the version half is now driven end to end.** It was called and never exercised, which is a path that has never run: measured across three real projects, **no declaration exists anywhere**, so nothing on this machine could reach it by accident. Four tests drive `report_for_project` against a project tree that carries both a `set/modules.yaml` and an install record, and they cover the direction each other cannot. A project that wants 1.2.0 and has 1.1.0 reports `mismatch` — **the answer inference is structurally blind to**, because a file is either present or absent and a half-upgraded project has every file present. A project whose versions agree reports `match`, so the mismatch above means something rather than being what this path always says. A module asked for and not installed reports `unknown`, **never a match** — and the mutation proving that is the fail-toward-reassuring one: turning `expected is None or installed is None → unknown` into `and → match` is caught. A project that declared nothing reports `declared: False` and an empty comparison list, not agreement, because a surface rendering *no mismatches* over a project that never adopted the mechanism is reporting calm it did not verify. ⚠ **One mutation was NOT caught on the first attempt, and the mutation was the weak half:** it altered a branch inside `describe()`, which these tests never call. Re-aimed at the `state` property itself, it is caught. Recorded because the loop's report accuses the TEST when the code under test was never touched — following that accusation means weakening a test that was right.

## 3. State — read from the log (CORE)

- [x] 3.1 `lib/set_orch/fleet/state.py`: derive last movement from the session log's mtime, never from a registry heartbeat. **DONE 2026-08-18** — `lib/set_orch/fleet/state.py`, from the session log's mtime. A test asserts the module's source contains no reference to the record's `status` field, because that field was measured at a **median age of 11 hours, maximum 83** across 23 live sessions. [REQ: activity-is-read-from-the-session-log-never-from-a-heartbeat]
- [x] 3.2 Define working as an outstanding tool call — a `tool_use` in the tail with no matching `tool_result` — and name the tool and its elapsed time. **DONE 2026-08-18** — an outstanding `tool_use` with no matching `tool_result`, naming the tool and its elapsed time. **Verified by self-experiment, not by assertion:** sampling this session's own log while a `Bash` call was open showed the state flip to `working` with that tool named. ⚠ The runtime flushes a turn in batches — the log was ~25s stale mid-turn — so `quiet` means *no outstanding call as of the last flush*, and the surface says so. [REQ: an-outstanding-tool-call-is-what-working-means]
- [x] 3.3 Return unknown, never idle, for anything undetermined; distinguish an absent key from an empty value (a missing `status` raises where `.get()` returns a false negative). **DONE 2026-08-18** — three distinct ways of knowing nothing (no log bound, log absent, log unparsable) each return `unknown` **with a reason**, and a mutation replacing `unknown` with `idle` fails three tests. [REQ: a-state-that-cannot-be-determined-is-unknown-never-idle]
- [x] 3.4 Report a phase only where the agent declared one; emit nothing where undeclared, and keep role separate from phase. **DONE 2026-08-19, and the SOURCE was the finding.** A declared phase already exists and nothing had to be invented for it: `sac focus --phase` writes it, and `sac agents --json` carries it per seat as `focus: {text, files, phase, ts}` — the same roster call this surface already makes for instructability, so it costs no extra work. Measured live: **52 seats, 18 with a declared focus, 10 with a declared phase.** `None` stays `None` and is never defaulted, because the bus's own rule is that a re-declaration WITHOUT a phase **clears** it rather than carrying the old one over — substituting a value here would resurrect a phase the agent deliberately dropped, and a mutation that defaults it is caught. Role stays where it was (`parent`, with its `source`); a phase is a claim and lineage is a measurement, and they are separate fields. `declared_at` travels with it: a declaration does not expire on its own, so its AGE is what lets a reader weigh it. [REQ: a-phase-is-reported-only-where-the-agent-declared-one]
- [x] 3.5 Carry a declared blockage independently of the measured state, so blocked-while-busy is representable. **DONE 2026-08-19.** `declared.blocked` is read from the declared phase and touches nothing measured; the payload carries `state` and `declared` as two objects, so *measured working + declared blocked* is representable rather than merged away. Found in the LIVE data the moment it was wired: an agent measured `quiet` while declaring itself `blocked` — the two are orthogonal in practice, not just in theory. ⚠ **The distinction the negative half protects:** a measured `waiting` is the runtime waiting for somebody to type; a declared blockage is the agent saying it cannot go on. Tying the badge to the waiting state would hide *working, but stuck on an answer for three hours*, which is exactly the case this exists for — a test asserts a waiting agent that declared nothing is NOT reported as blocked. ⚠ **CONFIDENTIALITY, and it is not hypothetical:** the focus sentence is written by an agent about work that may be a consumer's — measured on the live roster the same minute, one named **a partner company and an unpaid invoice**, and `files` are that project's own paths. Display-only at both layers; a test plants such a marker, proves it REACHED the reader, and then asserts it appears in no log record — and a mutation that logs it is caught. [REQ: a-declared-blockage-is-independent-of-the-measured-state]
- [x] 3.6 Keep the list path to `stat` + tail only; full parse happens when a log is opened. **DONE 2026-08-18** — `stat` plus a bounded 256 KB tail; a test builds a 60 000-line log and asserts the tail path does not read it whole. Measured live: 22 agents in **0.21 s**. [REQ: listing-every-agent-does-not-read-every-log-in-full]
- [x] 3.7 Bound the number of file watchers so it does not grow with the agent count — and before believing any watcher measurement, allocate one by hand and check it succeeds: an exhausted watcher table makes every allocation fail and reports a reassuring zero. [REQ: watching-the-fleet-costs-a-bounded-number-of-file-watchers] **DONE 2026-08-19 — met by construction, and the number is zero.** The fleet read path allocates no file watchers at all: it polls. Measured in-process, before and after a full `discover_agents` + `discover_projects` + five `read_state` calls — **0 allocated** — and held by a test, so adding a watcher to this path fails where the reason is written down rather than months later on a machine whose table happens to be nearly full. ⚠ **The instrument was checked before the zero was believed**, which is the half of this task that is easy to skip: `inotify_init()` was called by hand and returned fd 3, so the table is not exhausted (191 instances in use of a 512 limit). Without that, a zero means nothing — measured 2026-08-17 on this machine, a watcher-count test returned 0 for every variant because the table was FULL and every allocation failed silently, and the number read as efficiency. The guard test skips rather than passes when it cannot allocate one.
- [x] 3.8 CORE — Report *waiting for a person* as a state of its own, carrying what it waits for, taken from the runtime's session record rather than the log — the log cannot distinguish an agent stopped at a prompt from one that finished its turn. Check the record's freshness before believing it (its timestamps have been measured days stale) and yield unknown when it is stale. **DONE 2026-08-19, and the freshness check the task asks for DOES NOT EXIST — measured, not assumed.** `updatedAt` equals `statusUpdatedAt` in **22 of 22** records that carry both, so the record is written only when the status CHANGES: its timestamp is the age of the STATE, not of the observation. An agent waiting three hours honestly carries a three-hour stamp, so an age threshold would discard the true positives first — and it would have discarded the only real one here, whose stamp was 9.5 hours old. An mtime cross-check fails the same way in the other direction: **22 of 22** logs had moved since their status stamp, so it rejects everything, that true positive included. **What works is composing the two sources instead of ranking them.** The log decides working from not-working (structural, measurable); the record may refine a QUIET state into `waiting`, because only it knows. A record claiming `waiting` while a call is outstanding is contradicted — the measurement wins and the disagreement is carried as `declaration_ignored` rather than dropped, since a contradiction the surface cannot see is one nobody will fix. That test fires on the staleness that is real here: one record still said `busy` while its log showed no outstanding call. **No record is not the same as not waiting**, and the payload keeps them apart. `/api/fleet/agents` now carries a top-level `waiting` count present even at zero — the KEY is what tells the surface the state is reported at all. Live: 23 quiet, 1 working, **1 waiting on "input needed"**. ⚠ Two guards were narrowed rather than deleted: a test banning the string `status` anywhere in `state.py` now bans the thing it was protecting (no measured value derived from the declaration), which survives a rename; and the state tests moved out of `test_fleet_discovery.py`, where their name had drifted from their content. [REQ: waiting-for-a-person-is-a-state-of-its-own-and-it-says-what-for]
- [x] 3.9 CORE — Read an agent's declared purpose and its progress from the work-cycle engine's recorded run state (which change, which group, the last verdict); measure progress in completed tasks, never in turns or events; report a record whose process is gone as stale; report no purpose at all where there is no record. **DONE 2026-08-19** — `lib/set_orch/fleet/purpose.py`. ⚠ **It reads the engine's records off disk and imports nothing**, because `set_orch` may not import `set_workcycle` (D10) — the same guard that had already caught 4.7. The price is that the on-disk layout is a **second copy**, and second copies drift, so a test imports BOTH and fails when `RUN_STATE_REL` and the engine's `RUN_STATE_DIR` diverge; a comment asking to be believed would not have held it. **Progress is counted in ticked TASKS**, and the pattern demands the task NUMBER: measured on this repo, `^- \[` gives **215** and the numbered pattern **93**, because acceptance criteria wear the same shape — a test holds the wrong pattern beside the right one so a simplification inflates visibly instead of quietly. **A `~` partial is NOT rounded up to done**: the mark exists to carry a limit, and rounding it up reports work that is not there. **Three run states, and the order is load-bearing:** `finished` is decided BEFORE the pid is looked at, because a completed run's process is always gone and asking the pid first would call every finished run stale. A live pid is not proof either — pids are recycled, so a record claiming a run whose pid is held by something that is not an agent process is reported `running` with **`pid_unverified: true`**, which says *which question was answered* rather than quietly picking one. A stale record never lends its purpose to a live agent. Measured on this machine: **0 recorded runs anywhere** — no engine run has happened yet, so this path is held by unit tests and 6/6 mutations rather than by live data, and that limit is stated here rather than left to be assumed. [REQ: what-an-agent-is-working-towards-is-read-from-the-engine-s-record-never-guessed]

## 4. Instruction — over the bus (CORE)

- [x] 4.1 `lib/set_orch/fleet/instruct.py`: deliver an instruction addressed to a specific session; refuse rather than broadcast when identity cannot be resolved. **DONE 2026-08-19.** The join is the **session id**, recorded by both sides: the fleet knows an agent by its runtime session, and `sac agents --json` carries that same id on every seat — so §1's pairing problem (a heuristic link right 4 times in 9) does not arise here at all. Verified live: 55 session-bearing seats on this machine, **4 of 9** live agents instructable, the other 5 all for one stated reason. An unresolvable addressee is refused **by the channel, at the sender** (exit 1, and the error names who *is* in the room) and the refusal is carried through unchanged — a test asserts exactly one send attempt and that it carried `--to`, because the tempting repair for a bad address is the broadcast that would deliver a one-session message to everyone who can read the room. `REQUEST` is the default type, not `FACT`: a fact with an errand inside it wakes nobody by the channel's own rule. [REQ: an-instruction-is-delivered-over-the-messaging-bus]
- [x] 4.2 Take the delivery outcome from the channel's own answer, and map it to the four outcomes — including *held pending the recipient's human*; report unknown rather than upgrading it to delivered. **DONE 2026-08-19, and it corrected the design first.** ⚠ **§6.3 attributed a measurement to the wrong bus, and the error pointed this task at the forbidden channel.** It said the framework bus *"returns a fixed `{"status": "queued"}` for every send"*; that constant is `mcp-server/set_mcp_server.py:530` — the **git-based `.set-control` path**, live in 0 of 39 projects — not `set-agent-comm`. Measured against an isolated store (`SET_AGENT_COMM_DIR`, two seats, nobody else's channel touched): `sac send` answers **per send**, `wakes: ["proj-b#bbbbbbbb"]` for an addressed entry, `wakes: []` plus an explicit notice for a broadcast, and a **refusal with exit 1** for an addressee nobody answers to. Written up as design **§6.3a**. **The outcome is composed from two sources, because neither is one alone:** `wakes` is the channel's *rule decision* about whose attention the entry claims — `store.liveSeats` admits a seat that is merely *not known to be gone* — and the waiter count is whether anything will act on it without a person. All four reproduced end to end against the **real** `sac` binary, not a fake: *arrives-now* / *at-turn-end* / *sits-unread* / *refused*. `accepted` and `outcome` are two fields, and `delivered_to_agent` is false for every outcome that is not one of the first two. **The limit is in the name of what was built:** `held` is *modelled* (`instruct.held()`) and never *manufactured* — see 4.5. [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire]
- [x] 4.3 Use any direct local channel only to prompt a mailbox check, never to carry content, and degrade to the durable path alone when it is unavailable. **DONE 2026-08-19 — the prohibition is a SIGNATURE, not a rule.** `ring_mailbox_check(seat, *, transport)` takes **no message parameter at all**, and a test asserts the parameter set, because a rule saying *do not put the text here* is one careless keyword argument away from being broken and the break would be invisible: the message would arrive and the durable record simply would not exist. `transport` is injected rather than discovered — the direct channel is not a documented interface — so with none supplied nothing is attempted and the system behaves exactly as it does without a bell. `instruct_agent()` fixes the ORDER, which is the requirement rather than an implementation detail: the durable send happens first and unconditionally, and the bell is offered for **one** outcome only (`sits-unread` — the case where nothing starts a turn until a person types; ringing an agent that already has a waiter or a stop-hook interrupts it for a message it was going to read anyway). A bell that raises cannot fail a delivery, and a refused send rings nothing. [REQ: a-direct-channel-may-ring-the-bell-but-never-carry-the-message]
- [x] 4.4 Report an agent with no bus identity as observable but not instructable, with the reason; keep discovery and state working when no bus exists at all. **DONE 2026-08-19.** **Three negatives, kept apart, and only one of them is about the agent:** the bus is not installed / could not be asked (`BUS_UNREADABLE`), the process has no session id (`NO_SESSION`), and the bus was asked and does not know this session (`NO_SEAT`). `read_seats()` returns **None** for the first and `{}` for "asked, knows nobody" — collapsing them would mark every agent uninstructable on a machine where they are all reachable, which is the false-absence class in the direction that hides work. Nothing in `discovery` or `state` imports this module, so both keep working with no bus at all. Measured live: **5 of 9** agents not instructable, every one of them for `NO_SEAT` — and per `CLAUDE.md` the answer to that is enrolment, never a second transport. [REQ: an-agent-that-cannot-be-instructed-says-so-where-the-input-would-be]
- [x] 4.5 Carry the lapse of a held message through to the caller: a hold expires on its own, so the outcome shown must be corrected rather than left standing. An outcome with a clock is not a resting state. **DONE 2026-08-19, and the LIMIT IS IN THE DELIVERY.** `HELD` is deliberately absent from `TERMINAL_OUTCOMES`, so `settled` is false for it and a surface cannot treat it as a resting state; `delivered_to_agent` is false, because held means a human was interrupted and the agent saw nothing. `DeliveryReport.lapsed()` **replaces** the outcome rather than standing beside it — it returns `EXPIRED` carrying `superseded="held"`, so the place that showed *held* stops showing it. ⚠ **What is NOT built, and it is deliberate: no producer.** A hold exists on the runtime's own cross-session channel and on no other source, and that channel is exactly the one 4.3 forbids for content. So `instruct.held()` is a constructor for a caller listening to the asynchronous notice, and a test asserts that the **durable path never reaches `HELD`** — including when the answer carries a `held` field, because reading one off a channel that does not set it would be a false value in the one place that looks like caution. Wiring an actual listener to the runtime's notices is not in this change. [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire]
- [x] 4.6 CORE — Find waiter processes whose session has exited, resolving each candidate to a process identity and confirming it against its session; never derive a candidate from a match count. Treat an undeterminable session as alive. **DONE 2026-08-19.** The test is **structural, in three positions** — `argv[1]` ends in `sac.mjs` **and** `argv[2] == "wait"` — which no shell satisfies by accident; the session comes from `/proc/<pid>/environ`'s `CLAUDE_CODE_SESSION_ID`, which is what the bus itself derives a seat from. A test holds the **wrong** pattern (`"sac.mjs wait" in cmdline`) beside the right one and asserts they disagree on a grep, a shell snapshot and a `python -c`, so a later "simplification" fails here instead of looking identical and quietly matching the checking command. Every uncertainty resolves toward not removing: `live_sessions is None` yields an **empty** orphan list rather than every waiter, a waiter whose own session cannot be read is treated as alive, and `remove_waiter` **re-resolves the pid at removal time** because a candidate list is seconds old and a pid is recycled. There is **no bulk form** and a test asserts no other `remove*` entry point exists. Verified live: 3 waiters on this machine, **1 orphan** — and it is this thread's own, left behind by the `/clear` that started this session. [REQ: an-orphaned-waiter-is-shown-and-removing-it-is-an-offer-rather-than-a-tidy-up]
- [x] 4.7 CORE — Deliver an answer to an open decision into the deferred-work connector, keyed by change and task, and report it as **recorded** rather than received. The asking session is usually gone by then, so a message addressed to it would be delivered to nobody. **DONE 2026-08-19** — through the work-cycle engine's own `set_workcycle.connector.write_answer`, which shipped since this task was written (66/66, `lib/set_workcycle/`). No parallel mechanism was built. **The asking session is never consulted, and that is asserted on the SIGNATURE**: a liveness parameter here could only ever refuse something that should succeed, so the check is that there is nowhere to pass one. ⚠ **The first version imported the connector, and a guard caught it before any commit.** `set_orch` may not import `set_workcycle` in any form — the engine's design D10 makes the one-way dependency a requirement, because it is what turns *"orchestration keeps working with the engine deleted"* into something a test can check. `test_workcycle_dependency_direction.py` failed on `instruct.py:829`, and it was **the baseline set-diff that surfaced it** — the fleet suite alone was green, and the failure was one line in a 108-entry list against a 107-entry baseline. So `writer` is now **required and injected, with no default**, and `resolve_answer_writer()` finds one through the entry-point group `set_orch.deferred_work` (added to `pyproject.toml`), returning **None** where no engine is installed — which is design §8.1's stated behaviour: the control is not offered rather than offered and broken. ⚠ **Not yet live, and it fails toward absent:** an entry point only appears after the dist metadata is rebuilt, so `resolve_answer_writer()` measures **None** on this machine until `pip install -e .` is run. Not done here on purpose — `set-agent-owner` is holding two live agents, and nothing calls this path until the API of group 6 exists. **That reinstall is a precondition of 6.x, not of this task.** ⚠ One test of mine measured the wrong thing on its first run: a substring sweep for "receiv" over the result dict matched the **pytest temp directory**, which is named after the test. The measurement inside its own corpus, in miniature. [REQ: an-answer-to-an-open-decision-goes-to-the-connector-not-to-a-session]

## 5. Terminal — the owned process (CORE)

- [x] 5.1 `lib/set_orch/fleet/terminal.py`: start an agent under a pseudo-terminal the framework owns, and record which population each agent belongs to (started here / adopted / foreign) as a carried fact. **DONE 2026-08-19 — but NOT in the file this task names, and the difference is worth stating rather than quietly ignoring.** The starting and the population live in `owner.py`, because they are the same act: the population is a fact about the HANDLE this owner holds, so a second module would have to ask the owner for it and could then disagree with it. `adopted` was dropped by measurement, not by omission — task 1.1 established that a running session cannot be adopted, so a third value would name a population that cannot exist. **The half that was missing until today is the CARRYING**: the population existed inside the owner and never reached the screen, so no tile could know whether it had a terminal at all. `GET /api/fleet/agents` now carries `population` and `terminal_label` per agent. It has **three** values, not two: an owner that cannot be asked yields `unknown`, never `foreign` — `foreign` is the claim "the framework did not start this, so there is no terminal and cannot be", and that claim is false for every agent the owner was holding while it restarts. The cause is named once at the top (`owner_reachable`) rather than as a reason on every row, because a screen that can offer no terminal anywhere has one cause. Verified live in both directions: owner up and holding nothing → 23 `foreign`; owner unreachable → 23 `unknown`. [REQ: a-terminal-exists-only-for-a-process-the-framework-started-or-adopted]
- [x] 5.2 Refuse any write into a terminal the framework does not own, and never report a terminal as available for a foreign session. **DONE 2026-08-18** — the refusal is structural rather than a rule: this owner can only write to a pty master it is holding, so it cannot drift out of agreement with reality. `population_of()` is a fact about the **handle**, not about history — an agent a *previous* owner started is `foreign` here, because the property the surface needs is "can I type into this". [REQ: a-terminal-exists-only-for-a-process-the-framework-started-or-adopted]
- [x] 5.3 Relay terminal output and browser keystrokes in both directions, persisting nothing; diagnostics name the stream and failure kind only. **PARTIAL 2026-08-18 — the OWNER half only, and the name of this task is wider than what is delivered.** `ownerd.py` drains every held pty continuously into a bounded in-memory tail (mandatory, not optional — see 5.8's measurement), answers `tail`, and passes `write`/`resize` through; bytes travel base64 so a pty read that splits a UTF-8 sequence is not silently lost. Nothing is written to disk or to a log, and the diagnostics name the stream and the failure kind only. **COMPLETED 2026-08-19 — the bytes now reach a browser.** `attach`/`detach` turn an owner connection full-duplex, the drain fans out to every viewer, and `WS /ws/fleet/agents/{label}/terminal` relays both directions. Terminal bytes travel as **binary** frames and control as JSON text, because a pty read can end mid-UTF-8 and carrying output as a string would force a lossy decode at the boundary, silently. Backpressure **drops the viewer rather than slowing the source**: an owner that waits on a slow browser is an owner that is not reading the pty, and an unread pty freezes the agent. Verified end to end against a real agent: 1735 bytes of the live TUI replayed on attach, a keystroke written and echoed back on a LIVE frame, resize accepted, and **closing the view left the agent running** (task 5.4). ⚠ **The end-to-end probe found two bugs that unit tests had not, both in the client and both in the reassuring direction.** A request that reads its own answer eats the frames it passes over — the owner sends the replayed screen *before* the response, so the skip discarded it: **1752 bytes sent, 44 arrived**, and the first screen was wrong rather than absent. And two coroutines cannot read one stream: with output pumping and a keystroke's request both reading, the connection died on the first keystroke — a terminal that showed output until you touched it. Now exactly one loop reads; responses resolve futures by id, frames go to a bounded queue. Both shapes are held by tests, and all three mutations of them are caught. [REQ: terminal-traffic-travels-in-both-directions-and-is-never-persisted]
- [x] 5.4 Define the lifetime: the agent survives a browser disconnect and is reattachable; stopping is an explicit action, never a consequence of closing a view. **DONE 2026-08-19.** Detaching forgets the viewer and touches nothing else; the agent stays held and running. Verified end to end — after the WebSocket closed the agent was still in the fleet listing — and held by a test that drives the real attach/detach path, because the hazard is a future "tidy up on disconnect" that would look reasonable and would live exactly there. Reattachable by MORE than one reader: the terminal belongs to the owner, not to whoever is looking at it, so each arrival is sent the buffered screen and live output reaches every viewer. [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting]
- [x] 5.5 Implement the measured answer for a service restart: the terminal does **not** survive it, because its handle dies with the owning process — a pty master cannot be reacquired from outside (design §6.1). The agent keeps running and stays reachable over the bus; only the terminal column becomes no, and it is never listed as attachable. ⚠ **Corrected 2026-08-18 by measurement (design §6.2): it is not only the terminal that fails to survive.** A pty-attached agent whose pty holder dies **dies with it** — probed both ways: pty-attached + owner killed → scope inactive, process gone; no-pty + starter killed → scope active, process alive. So the scope protects against a cgroup kill and not against losing a tty, and the surface must claim survival of a **web-service** restart only, never of the owner's death. **DONE 2026-08-19, and it removed a FALSE VALUE nobody had named.** The population had three values, so an agent whose owner had restarted fell to **`foreign`** — which states *the framework did not start it*, when the framework did and merely lost the handle. It also hides the one control that helps, because recovery (5.11) is possible exactly there and impossible for a genuinely foreign session. A fourth value, **`orphaned`**, is asked of the PROCESS's own cgroup (`scopes.scope_of`) rather than of anything we wrote down — a record of what we started is a record of our intent, and after a crash the two differ precisely when it matters. An `orphaned` carries the `scope` `recover` must stop, and the frontend refuses to render one WITHOUT a scope as recoverable: an offer whose action cannot be performed is worse than no offer. `unknown` stays `unknown` and the cgroup is not even consulted — the owner being unreachable is no evidence about whether it holds this pty. **And the survival claim is in the payload, not in the surface's memory:** `survives: "web-service-restart"`, because the measurement says a pty-attached agent dies with its pty holder. A mutation that widens that word to include the owner is caught. 4/4 mutations caught; Python baseline diff empty, 417 web tests green. [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting]
- [x] 5.6 Log the process lifecycle with pid and exit signal, per code-quality rules, so an orphan is findable from the logs rather than only from the screen. **DONE 2026-08-19.** Start, stop, reap, attach, detach and every refusal carry the label, the unit, the pid and the cgroup. The exit is logged as a SENTENCE rather than as the packed number `waitpid` returns: 36608 means "the shell below the pty saw a SIGTERM" and nothing about the number says so. It also names the 128+N convention, which is the common case here and not an exotic one — an agent under a pty runs below a shell, so a SIGTERM arrives as exit code 143. A log line an operator has to decode by hand is a log line they skip, and being skippable defeats the point of this task. [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting]
- [x] 5.7 CORE — Refuse to resume any session with a live process bound to it, and expose no control that would; treat an undeterminable liveness as live. A resume that succeeds here forks the running agent's conversation silently. **DONE 2026-08-19, and the check is on the SESSION rather than on the unit — which is the whole of it.** Stopping the scope covers the agents this framework started; it says nothing about the rest, and the rest is most of them. A session started by hand has no scope, so every unit-based check clears it for resume. **"Undeterminable is live" needed a new reader, not a new branch.** Every reader in `discovery` turns an unreadable `/proc` into "no agents" and logs a warning — right for a listing, where an empty screen is honest, and exactly backwards here, where "nothing is running" clears the way for the fork. `live_session_ids()` returns `None` instead, and `None` is not the empty set. The check runs **before** the scope is stopped, because stopping first would kill a running agent on the way to refusing — a refusal that damages what it was protecting. Three mutations caught, including that ordering. [REQ: resuming-a-session-that-is-running-is-refused-not-offered]
- [x] 5.8 CORE — Split the agent-owning process from the web service: a second unit owns agent lifecycle, the dashboard serves UI and API, and the two communicate. Extends the existing per-project supervisor daemon's shape rather than inventing one. Design §6.2. **The owner must stay THIN, and this is a requirement rather than a style note (user, 2026-08-18):** its uptime IS the terminal's uptime, because a pty master cannot be reacquired, so every restart of it is a terminal outage. It holds ptys, relays bytes, and starts/stops named scopes — nothing else. Discovery, state, the API and the screen stay in the web service, which stays freely restartable. A line of business logic added here is a future outage. **DONE 2026-08-18 — the two services talk.** `lib/set_orch/fleet/owner.py` holds the ptys, starts and stops named scopes, and strips every inherited `CLAUDE_*` variable so the started session actually writes a transcript (measured: a child-marked session writes none at all and is invisible to every source the fleet reads). `templates/systemd/set-agent-owner.service` carries `KillMode=process` and `Restart=on-failure`, both deliberate. `lib/set_orch/fleet/protocol.py` carries the JSON-lines envelope, `ownerd.py` serves it over an `AF_UNIX` socket, `owner_client.py` reaches it from the dashboard, and `set-agent-owner` is registered in `pyproject.toml` so the unit's `ExecStart` resolves. **The envelope deliberately copies `set_memoryd.protocol` rather than importing it**, and the reason is measured rather than stylistic: `set_memoryd` is not a packaged module (`packages.find` excludes it, and `import set_memoryd` fails in the installed environment), so importing it would make a systemd-started service depend on `PYTHONPATH` happening to carry `lib/`. ⚠ **The measurement that changed the design: holding a pty is not passive.** A child writing to its own tty with nobody reading the master froze after **17 408 bytes**, and one drain let it advance exactly **4 096** further. So the owner drains continuously into a bounded in-memory tail — the one piece of real behaviour it is allowed, because without it every other guarantee here describes a frozen process. Proven by disabling it: 400 000 bytes through with the drain, **0 and a blocked process** without. Design §6.2. **Verified live on this machine 2026-08-18**: unit installed and enabled, socket at `$XDG_RUNTIME_DIR/set-agent-owner.sock` (0600), a real agent started into `app.slice/set-agent-<label>.scope` — a sibling of `set-web.service` — discovered by the fleet endpoint with its session id, its terminal drained (the Claude TUI's own output), a keystroke written and echoed back, resize accepted, then stopped and **reaped**. That last one is a defect the cycle exposed: the owner never `waitpid`-ed its forked children, so each start/stop left a `Zs <defunct>` child for the life of a service whose whole point is to be long-lived — and `ps -p <pid>` reports a zombie as an existing process, so the obvious "is it still running" check answers yes for a dead agent. It misfired here while measuring, which is the proxy-instead-of-the-thing class caught in the act. **The entry point is a `bin/` wrapper, not the pyproject console script**, because on a machine installed by `install.sh` the console scripts are not deployed at all — measured: `~/.local/bin/set-orch-core` is a symlink into `bin/`, while `set-manager`, which exists only as a project script, is absent. The pyproject entry stays for pip-installed contexts. **The client deliberately does NOT auto-start the owner** the way `set_memoryd.client` does: an owner started from inside the web service joins its control group and dies with it, which is CB-1 rebuilt silently — so an absent owner is reported with the `systemctl` command, never repaired. [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting]
- [x] 5.9 CORE — Start every surface-started agent in its own transient scope, so it survives a restart of **either** service — the split alone does not deliver this, it only moves which service kills it. Measured: a transient scope lands as a sibling of the service, not a child. Assert the cgroup placement, not just that the process started. **DONE 2026-08-18** — `lib/set_orch/fleet/scopes.py`. `assert_sibling()` refuses rather than warns, because a scope inside the service's cgroup would leave the surface promising a survival it does not have. Verified live: the scope lands at `app.slice/<name>.scope`, a sibling of `set-web.service`, and the process survives its starter being killed. ⚠ Two live defects the run found and unit tests would not have: a scope reports `MainPID=0` (it adopts rather than forks), so the pid comes from `cgroup.procs`; and an interactive agent **ignores SIGTERM**, which kept `systemctl stop` blocked past 20 s — `stop()` now uses `--no-block`, waits, then escalates to SIGKILL and says which path it took. ⚠ **A THIRD live defect, found 2026-08-18 while proving 5.8 on this machine, and it made the second one's remedy inert.** The wait never waited: `_await_gone` accepted `not scope.active`, and `Scope.active` is the string test `ActiveState == "active"`, so `deactivating` — the state a unit enters the instant a stop is requested — read as gone. Measured on a live interactive agent: `stop()` returned `gone=True` in **0.0 seconds**, logged "stopped on SIGTERM", and the agent was **still running four minutes later** — because returning early also meant the SIGKILL escalation never ran. The fix asks about the PROCESSES (`scopes.is_gone` / `scope_is_gone`: a cgroup holding a pid is not gone, whatever systemd calls the unit), and `Scope` now keeps the raw `state` because collapsing it into a bool is what threw the distinction away. Four tests hold it, including one that holds the WRONG pattern so a simplification back to `not active` fails here. Re-verified live: start → stop in **0.6 s**, scope gone, process gone. [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting]
- [x] 5.10 CORE — Start a work unit by invoking the engine's command entry point under the framework-owned pty, never by spawning an agent directly; keep starting a bare interactive session a separate, separately-labelled act. A run started outside the engine is absent from the engine's state, which is the source this screen reads. **DONE 2026-08-19** — `POST /api/fleet/units` RUNS `set-work-cycle … run` under the framework-owned pty. The route **builds the argv and accepts none**: `StartUnitBody` has no `argv`, no `command` and no `label`, so there is no parameter through which a second start path could be smuggled in, and a test asserts that on the model's fields. The bare-session route (5.8) stays, separately labelled — the label prefix `unit-` is not decoration, because the terminal column, the stop action and the recovery path all key on the label and must be able to tell a work unit from a hand-started shell without asking anything else. Both routes reuse the SAME two refusals (no such directory, unknown project root) rather than a second copy that would drift, and an absent owner is a 503 with no local fallback for the CB-1 reason. ⚠ `set_orch` invokes the engine as a COMMAND and imports nothing from it (D10). 5/5 mutations caught, including *the unit route spawns an agent instead of the engine* and *the bare route allowed to carry an argv*. [REQ: starting-work-goes-through-the-engine-s-one-entry-point-not-a-second-spawn-path]
- [x] 5.11 CORE — Recover an agent whose owner died: **stop the orphaned transient scope by name, then resume the session into a fresh pty** — replacement, not reattachment, because the terminal handle died with the owner. The order is load-bearing and must be enforced in code rather than left to the operator: resuming while the old scope is still up reproduces the §6.1 fork (two live sessions appending to one transcript, neither aware of the other, nothing reporting it). The surface refuses to offer resume until the scope is down, and says which of the two acts it is performing. Verified 2026-08-18 that a transient scope lands at `app.slice/<name>.scope`, a sibling of the web service, and stops by name. **DONE 2026-08-18** — `recover()` in `lib/set_orch/fleet/owner.py`. Stops the orphaned scope by name, verifies it is down, **re-reads the state rather than trusting the stop's return**, and only then resumes into a fresh pty; the resumed agent carries `resumed_session` so the surface can say replacement rather than reattachment. Both refusals are held by tests, and a mutation that reverses the order or drops the re-check fails them. ⚠ **Corrected 2026-08-18: the re-check was open, and for the reason that makes re-checks worth writing down at all.** It asked `still.active` — *the same question* the stop path had already mis-answered (see 5.9's third defect) — so a scope that was `deactivating` with a live agent in it passed both the stop and the verification, and the resume would have gone ahead against a running session: exactly the §6.1 silent fork this task exists to prevent. **A re-read is only a guard if it asks a different question than the one that can be wrong.** Both checks now go through `scopes.is_gone`, and the refusal names the pids that are still there rather than a state word. Held by `test_recover_refuses_while_the_old_scope_is_still_shutting_down`. [REQ: adoption-of-a-running-session-is-measured-before-it-is-relied-upon]

## 6. API (API)

- [x] 6.1 New route module in `lib/set_orch/api/` serving the fleet inventory: projects, agents, sources, branch, parent seat, role, capability flags. No existing route modified. **PARTIAL 2026-08-18 — the inventory is served; parent seat is NOT.** `lib/set_orch/api/fleet.py` serves `GET /api/fleet/agents` with projects, agents, sources and branch. Parent seat waits on task 2.5, which is not in this slice. Finding CB-16 is closed here: the router is registered FIRST in `api/__init__.py`, ahead of the `/api/{project}/...` wildcards that would otherwise answer it as a project named "fleet". ⚠ **Corrected 2026-08-18: that guard was held by nothing.** `api/fleet.py`'s docstring stated that "the unit test `test_fleet_api.py` fails if that ordering is lost" and no such file existed — a check asserted in prose, which is the name-outranks-the-body class applied to a test. The file now exists and the claim is true: moving the fleet router behind the wildcards fails it. The first attempt to prove that failed to fire, and the **mutation** was the weak half, not the test — `projects_router` carries no `{project}` wildcard at all, so moving the fleet router behind it changed nothing that mattered. [REQ: the-inventory-is-a-union-of-its-sources-and-names-them] **COMPLETED 2026-08-19 — and the note above had gone stale in the direction that leaves work undone.** It said *parent seat is NOT [served]; it waits on task 2.5, which is not in this slice*. 2.5 shipped on 2026-08-19 and the field has been on the payload since: measured live, `fleet-ui-720` reports `{"seat": "set-core#…", "source": "recorded"}`. So the gap was closed by a neighbouring task and the record kept describing it as open — a marker outranking the body, with the fail direction that hides finished work rather than unfinished. **What was genuinely missing is that NOTHING HELD IT:** no test in the API suite mentioned `parent`, so the field could have been dropped or reshaped in silence. Three tests now do, and each asserts a direction the others cannot: a recorded binding is preferred and the walk is **never consulted** when one exists (2.5 measured 0 of 23 live agents with an agent ancestor, and a framework-started agent's parent is the owner process — so asking the walk first would answer `None` for exactly the agents whose parent is known); a walk that finds an ancestor reports it, and a walk that finds none reports `None` rather than inventing a seat; and the `source` travels with the binding, because a measurement and an inference rendered identically is a flattening the screen cannot undo. Three mutations, all caught — including the one that keeps the field but relabels a record as ancestry.
- [x] 6.2 Per-agent state and log-tail endpoints, and a full-parse endpoint used only when a log is opened. **DONE 2026-08-19, and the task named the wrong cost — worth recording, because acting on its wording would have built a route that saves nothing.** It assumes a cheap tail and an expensive full parse. Measured on a 0.6 MB log: `read_conversation` reads the last 2 MB whatever `limit` says, so `limit=3`, `limit=60` and `limit=500` all cost **2.9 ms** — there is no full parse to avoid, and a separate tail route would be a second copy of one that already tails. **The real cost was the DISCOVERY.** `GET /api/fleet/agents/{pid}/log` re-discovered the whole fleet to resolve one pid, and `discover_agents()` asks git for the project root and the branch of every agent — two subprocesses each, ~44 here — while the surface polls an open log every 5 s. Measured: **202 ms for the fleet against 3.5 ms for one agent, 58x**. `discovery.discover_agent(pid)` is the single-pid path, with `resolve_git` off by default because a caller wanting the log needs the session binding and not the branch. It still **re-verifies the pid by `comm` identity** rather than trusting the caller — pids are reused, and answering with whatever log a stale pid maps to would serve one session's conversation under another's name. `GET /api/fleet/agents/{pid}/state` is the second route, for a tile that is open and refreshing and would otherwise re-measure 22 agents to redraw one. A test holds the pattern that was wrong: it fails if either per-agent route reaches for `discover_agents` again — the cost is invisible from outside, since the endpoint answers correctly either way. [REQ: listing-every-agent-does-not-read-every-log-in-full]
- [x] 6.3 Send endpoint returning the three-way delivery outcome verbatim from the bus. **DONE 2026-08-19** — `POST /api/fleet/agents/{pid}/instruct`. ⚠ **The name says three and there are four**, so the route reports `accepted`, `outcome` and `delivered_to_agent` as three separate fields: a **200 here means the send was made and answered**, which is compatible with the message reaching nobody. The agent is re-discovered by pid (a pid is recycled, and instructing whatever session it now maps to delivers one person's message to another's agent), and the addressee is the seat resolved from that pid — the body has no recipient field at all, so a mistyped seat cannot become a message to the wrong agent. A **fresh** roster is read here while the listing uses a cached one, and a test fails if that reverses. Not instructable → **409 carrying the reason** so the surface can put it where the input would be, rather than a generic failure; a bus refusal → 409 with the channel's own words, and a test asserts the route does not retry. 7 mutations, 7 caught. ⚠ **PROVEN AT TWO LAYERS SEPARATELY, NOT AS ONE.** The route is held by unit tests and mutation; the channel was driven end to end against the **real** `sac` binary. What has NOT been done is route→channel→a live enrolled agent in one act, and the reason is worth recording rather than working around: **this project has no room on the bus at all** (`sac rooms`: 4 rooms, none of them `set-core`), which is also why 5 of 9 agents measure as not instructable. Creating one is *enrolment*, which `CLAUDE.md` puts in its own module and `design.md` §6.6 assigns to the framework — improvising it inside a verification would be building that module by accident, in the place nobody would look for it. Measured the same minute, and it is the same finding from the other end: the agent this surface **started** (`fleet-ui-720`, via `POST /api/fleet/agents`) came up **without a seat** — so a framework-started agent is observable and not instructable today, which is exactly the case 4.4 describes and exactly what enrolment is for. [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire]
- [x] 6.4 Bidirectional terminal stream endpoint, plus start and stop for a surface-started agent. **DONE 2026-08-19 — the stream landed; start and stop shipped a day earlier.** `WS /ws/fleet/agents/{label}/terminal`, registered on the fleet router, which `server.py` includes ahead of the `/ws/{project}/stream` wildcard. `POST /api/fleet/agents` and `POST /api/fleet/agents/{label}/stop` go through the owner client, plus `GET /api/fleet/owner` so the screen can ask whether a start is possible *before* offering one — a button that is present and fails is worse than one that is absent with a reason beside it. Three refusals are held by tests and each has its own status: a directory outside every known project root is a 400 (not choosing here would choose the permissive option — an agent startable anywhere on the machine), an absent owner is a **503 with no local fallback** because starting it here would rebuild CB-1, and an owner that refuses is a 409. The body deliberately takes no `argv` even though the socket does: task 5.10 adds the engine's entry point as its own, separately-labelled act. [REQ: terminal-traffic-travels-in-both-directions-and-is-never-persisted]
- [x] 6.7 API — Store and serve the hand-made arrangement (groups, order within a group, parked), and JOIN it to what discovery found rather than substituting for it. **DONE 2026-08-19, and it exists because D-2 was answered** — manual two-level ordering needs somewhere to live. `lib/set_orch/fleet/layout.py` plus `GET`/`PUT /api/fleet/layout`. Server-side rather than `localStorage` (which the dashboard uses for its other view preferences) because arranging 45 projects is work done once and relied on, and it should not differ between two browsers on one machine. **Membership is stored, never derived from a name pattern** — `set-*` looks like a rule, and a rule re-evaluates: a rename would silently move a project, a new matching name would land somewhere nobody put it. **Writes take a `base_version` and a stale one is refused with a 409**, because two dashboard tabs are ordinary and the loser of a silent race would find an arrangement they never made. Written through a temp file and renamed. Four mutations caught, including the two silent ones: a vanished project dropped instead of reported, and an unarranged project falling out of the screen. [REQ: the-arrangement-is-the-user-s-and-it-never-becomes-the-inventory]
- [x] 6.5 Endpoint installing a module into a project **through the module installer**, returning its report verbatim — skipped files with reasons, a changed-nothing outcome, a refusal naming a missing required module. No capability-specific install path, and no ownership check invented here: provenance is the installer's job. ⚠ **BLOCKED 2026-08-19, and the blockage is the finding — measured, not assumed.** There is no installer to call. `lib/set_orch/module_install.py` defines `InstallReport`, `Skip`, `plan_files`, the declaration and record readers and the version comparison — but **`InstallReport` is never constructed anywhere in the repository** (`grep -rn 'InstallReport(' lib bin` → no hits), and no function writes a module's declared files into a project. The deploy engine (`profile_deploy.deploy_templates`) writes templates and knows nothing about modules. Design §8.0 is explicit that *the install belongs to the installer, the screen asks for it*, and §8.2 says that a task here needing the other side to build something is the signal to **raise it rather than accommodate it**. Building the write path inside a fleet API route would be the parallel-mechanism failure this repository already names. **7.15 and 9.17 are blocked with it** — a surface that renders an installer's report cannot be written against an installer that does not exist, and a test asserting the unhappy paths would be asserting against a fixture rather than a mechanism. [REQ: an-install-offered-from-the-screen-goes-through-the-module-installer-and-shows-what-it-did-not-do] **UNBLOCKED AND DONE 2026-08-19 — the blocker was real and is gone.** It was recorded here as blocked because `InstallReport` was never constructed and nothing wrote a module's files into a project. ⚠ **The second half of that was WRONG**, and finding out is what unblocked this: `profile_deploy._deploy_single_template` has always copied a module's declared files, ledgered them and announced them. What was missing was every *account* of the act — no structured report, no refusal on a missing requirement, and no install record unless the module happened to announce itself. Built as its own change, `module-install-writer`, because it is a capability that shipped with no spec at all. The route is now `POST /api/fleet/projects/{name}/install`: it accepts only a project the SCREEN listed, defaults to `dry_run=True`, returns the installer's report **unchanged**, and answers a refusal with **409** rather than 400. See `module-install-writer` tasks 2.x and 5.1.
- [x] 6.6 Endpoints listing orphaned waiters and removing one **by named process**; the removal takes the same discipline as the install — it writes into a tree the framework does not own — and refuses anything it cannot confirm dead. No bulk-remove endpoint — a cleanup that takes a list is one mistaken list away from killing live waiters. **DONE 2026-08-19** — `GET /api/fleet/waiters` and `POST /api/fleet/waiters/{pid}/remove`. **Three statuses, not a boolean**: `orphaned` may be removed, `live` must not be, and `undeterminable` — a waiter whose session cannot be read — is listed and never offered; collapsing the third into either of the others is the only way to get this wrong, and one direction of that kills a live waiter. An unreadable process table answers `measured: false` rather than an empty list, because *no waiters* invites installing one and *we could not look* does not. A test asserts **no bulk route exists**, by path. Verified live: 4 waiters on this machine, 2 orphaned, 2 live. 7 mutations, 7 caught. [REQ: an-orphaned-waiter-is-shown-and-removing-it-is-an-offer-rather-than-a-tidy-up]

## 7. The screen (WEB)

- [x] 7.1 `web/src/pages/Fleet.tsx`: two panels — projects left, the selected project's agents right, selection without a further navigation step. **UNBLOCKED 2026-08-19 — D-2 answered: manual ordering plus a parked section** (see `review-findings.md`). The left panel is therefore a hand-ordered, drag-reorderable list with a collapsed parked section beneath it — **and the ordering is TWO-LEVEL** (user refinement, same day): ordered groups rendered as blocks, projects ordered within their own group only. Group membership is a stored fact rather than a name-prefix rule; a prefix may seed a group as a one-time bulk act, but what persists is the membership, or a rename silently moves a project. Ungrouped projects behave as one group at the end. Each collapsed group carries its own waiting count, because it is one more place a waiting agent can sit while the screen looks calm. [REQ: projects-on-the-left-the-selected-project-s-agents-on-the-right] **DONE 2026-08-19.** Two-level as specified: groups are blocks with their own order, a project drags only inside its group, ungrouped projects behave as the last group, and the parked section collapses beneath. Membership is a STORED list on the server (`fleet-layout.json` carries `groups[].projects`), so a rename cannot silently move a project the way a prefix rule would; a prefix only seeds a group once. Verified live rather than in a test: one drag is one save, and the order came back after a reload (layout version 8→9, `set-core`/`set-designer` swapped and swapped back).
- [x] 7.2 Project tile carrying agent count and enough state to see a waiting agent without selecting the project, and keeping that count readable when compacted. ⚠ **D-2's third requirement lands here and it is load-bearing:** a hand-maintained order has no construction that keeps a waiting project visible — one dragged to position 30 six weeks ago is below the fold today and nothing will move it. So a **sticky header** carries the count of agents awaiting an answer, with a jump to the first, **counting across the parked section too**. Measured shape to design against: 45 projects listed, 12 with agents, 33 empty. [REQ: a-project-tile-carries-the-state-of-the-agents-inside-it] **DONE 2026-08-19, including the load-bearing third requirement.** The sticky header counts agents awaiting an answer across the parked section and every collapsed group, and jumps to the first — the jump expands whatever hides it rather than scrolling to a row that is not rendered. Each collapsed group carries its own waiting count for the same reason.
- [x] 7.3 Agent tile: identity, state, log excerpt, input; state and input survive every density; an unconfirmed binding is marked as a guess. **PARTIAL 2026-08-19 — identity, state, excerpt and the guess marker are done; the INPUT is not, and it is blocked on group 4.** The excerpt is the last thing actually *said* (tool calls, tool results and thinking blocks are skipped — a tile showing `Bash` repeats the state line, and a thinking block shows something the conversation never contained), and it costs no extra I/O: it comes from the same tail the state pass already reads. Absence is stated rather than blanked — a tail of pure tool traffic means *nothing was said recently*, and an empty line would claim *nothing was ever said*. ⚠ It carries verbatim consumer-session content, so it is display-only: the code says, at both layers, that it must never reach a log, a cache, a memory or a committed artifact. [REQ: an-agent-tile-carries-state-log-excerpt-and-its-own-input] **RE-MEASURED 2026-08-19 by the UI side, and the note above is now stale in the direction that causes wasted work.** The INPUT is no longer blocked and is no longer missing: group 4 shipped (`58393c0b` — 7.7 and 4.4), so every card carries `FleetInstruct`, and where an agent cannot be instructed the producer's own sentence stands in the input's place. Identity, state, excerpt and the guess marker were already done. **What is actually left is one thing, and it is measured rather than inferred:** the input does NOT survive the ROW density. With one tile enlarged the panel renders 1 card / 1 input / 1 row, and the row (`AgentRow`) carries identity, state, the contradiction marker, branch and age — but no input and no excerpt (`[data-fleet-row] [data-fleet-instruct-input]` → none, live, 2026-08-19). So an agent you could instruct becomes uninstructable purely because you enlarged a DIFFERENT tile, which is the compaction-hides-something shape applied to an affordance rather than to a failure. **DONE 2026-08-19.** The row carries its input: `AgentRow` is a `div` with a guarded click (an input inside a `button` is invalid, and every keystroke's click would also have been an enlarge), and `FleetInstruct` renders beside the identity half in a `compact` frame that differs from the card's only in the border — never in what can be sent or to whom, because a compact input that could do less would let the density decide instructability. Two regression tests, both mutation-proven: removing the input fails one, removing the click guard fails the other.
- [x] 7.4 Enlarge one tile with the others remaining as single-line rows carrying state and activity; a row selects back. **DONE 2026-08-18** — the enlarged tile keeps its place in the list's own order rather than jumping to the top, so the row you clicked is where you clicked; the rows reuse the same `StateLine` component, so `unknown` cannot read one way enlarged and another way collapsed. Enlargement resets on project switch and is **deliberately not remembered** — that is 7.5, which is still open. No entanglement with D-2: this is entirely inside the right-hand agent panel and the project column is untouched. [REQ: a-tile-can-be-enlarged-and-the-other-agents-stay-visible-as-rows]
- [x] 7.5 Per-project view state: enlarged tile, density, unsent draft; a remembered view never determines state, and a remembered choice outranks the single-agent default. **UNBLOCKED 2026-08-19 by D-2**, and it grew a second subject: the project ORDER and the parked set are also remembered state, and they are the kind that must survive a reload rather than a tab. A project stays in the list when its agents close — its position is a statement about the project, not about who happens to be running in it. [REQ: view-state-is-remembered-per-project] **PARTIAL 2026-08-19.** Remembered and verified: the enlarged tile (with `undefined` ≠ `null`, so a deliberate close is not reopened by the single-agent default), the open terminal by LABEL rather than pid, and — on the server, because it is work done once — the order, the groups and the parked set. **Not done: density, and the unsent draft.** The draft field exists but the input it belongs to is group 4, which is blocked on CB-7; storing a draft for an input that cannot send is a remembered view that determines nothing. **RE-MEASURED 2026-08-19 by the UI side.** **Density is DONE** — `COLUMN_CHOICES` 1–4, chosen per project, remembered and resolved through `resolveColumns`, and offered only where it can change anything (not with one agent, not while a tile is enlarged). **The unsent draft is a DECISION now, not remaining work: it is deliberately NOT persisted.** The `draft` field survives in `ProjectView` and nothing writes it. An instruction being typed can carry a consumer's own words — the same content class as `declared.focus`, where one live value named a partner company and an unpaid invoice — and the standing rule is that this content may be displayed and must never reach `localStorage`, a log or a cache. A remembered draft would put exactly that into the browser's store, keyed by project, surviving every reload. `fleetInstructSurface.test.tsx` holds the boundary: after typing and clicking through the tile's controls, `localStorage.getItem('set-fleet-view')` is non-empty and contains none of the typed or declared text. If this task is to be closed, close it on that decision rather than by implementing the field. **CLOSED 2026-08-19 on the decision, not on code.** Density and the remembered enlargement shipped; the unsent draft is deliberately NOT persisted and that is the answer, not a gap — an instruction being typed can carry a consumer's own words, and the standing rule is that such content may be displayed and must never reach `localStorage`, a log or a cache. The `draft` field stays in `ProjectView` unwritten, so a future change reopens the question explicitly rather than by accident.
- [x] 7.6 Dictation into the same input, reusing the existing voice component; absent rather than failing when unconfigured. [REQ: dictation-writes-into-the-same-input-as-typing] **DONE 2026-08-19 (`4ad6f1df`).** The dashboard's existing `VoiceInput`, unchanged, writing into the SAME `text` the keyboard writes into — so everything after the words exist is one path: the same review, the same Enter, the same three facts back. Two rules it must not quietly break, both held by tests: it **never sends** (a transcript that dispatched itself would put words nobody read in front of a live agent, and the send is the irreversible half of this screen), and **a partial is not what you have** — in-progress text sits BESIDE the box, never in it, so a sentence cut off by a dropped connection cannot be sent as if it had been finished. The preview expires on its own, because the voice component announces a transcript but never announces that it stopped hearing: a recording ended with no finalised words leaves the last partial standing, and *"hearing: …"* about a microphone that is off is the false-presence shape this screen exists against. ⚠ **The absent path is the one measured live:** Soniox is not configured on this machine (`/api/soniox-key` → "not configured"), so what the screen shows is the MISSING mic — 2 instructable tiles, 2 textareas, 0 mic controls. The present path is proven in unit tests with the availability hook mocked, and has not been seen on a screen. 5 mutations, 5 caught.
- [x] 7.7 Show the delivery outcome on the tile it was sent from, distinguishing the three, and offer the remedy where nothing will wake the agent. [REQ: the-delivery-outcome-is-shown-where-the-message-was-sent] **DONE 2026-08-19 (`58393c0b`).** The three facts are rendered as three — `accepted`, `outcome`, `delivered_to_agent` — because a 200 is not a delivery and `sits-unread`/`wakes-nobody` are ordinary 200s in which nobody was told anything. `held` is the only non-terminal outcome and it has a clock: re-rendered every second as *held as of N ago, and nothing has re-checked it since*, which is a statement about a moment and stays true, where a drawn-once "held" points at a message that may already be dead. The remedy is offered exactly at `waiters_here === 0` (`n === 0`, never `!n`), and a 409 carrying an outcome renders as an outcome rather than as a generic error. Text stays in the box on a refusal.
- [x] 7.8 Lineage on the tile: an edge plus a reference to the parent seat, and the measured role. [REQ: an-agent-started-by-another-agent-is-identified-as-its-descendant] **DONE 2026-08-19 (`37e83a68`).** `recorded` and `ancestry` get different weight and different sentences — the measurement and the inference are never flattened into one "parent", because the screen is the last place they can still be told apart. The seat reference is also a WAY IN: it becomes a button only when that agent is on this screen (crossing projects if needed) and stays a plain fact when it is not, since a control that goes nowhere is the shape 8.2 forbids. An ancestor with no session record falls back to its bare pid rather than being dropped — the relation is known, only the name is missing. A render-time bug was avoided in the wiring: one callback answering both *is it reachable* (asked during render) and *go there* would have changed state inside a render pass, so the question and the act are two callbacks.
- [x] 7.9 Capability icons per project, dim for not-connected and distinct from unknown, rendered from data rather than a fixed list. [REQ: a-project-reports-what-it-has-wired-in-and-dim-is-not-absent] **DONE 2026-08-19** — `fleetCapabilityMarks.ts` + `ProjectFacts` in `FleetProjectColumn`. One mark per capability, drawn from the report's own list rather than a fixed set of names, so a framework that gains a module does not keep confidently drawing the old four. `not-connected` is HOLLOW and present — dim is not absent — while `unknown` is dashed, because the two invite opposite acts; a state this screen does not recognise reads as `unknown` and never as connected, which is the fail direction that would silently stop offering a capability. An absent report says *not measured* instead of drawing an empty strip, which would be a gap rendered as a zero. Measured live: 6 rows, 6 partial and 18 not-connected marks, all rendered.
- [x] 7.10 `web/src/App.tsx`: the root route renders the fleet; the projects overview keeps its own route and a navigation entry. **DONE 2026-08-18** — the root renders the fleet, `/projects` renders the overview unchanged, and `/fleet`, `/set` and `/manager` redirect. **Findings CB-4 and CB-5 close with it:** the navigation entry lives in `web/src/lib/sidebarRegistry.ts` (Fleet at `/`, Projects at `/projects`), and the three E2E specs that opened `/` in `beforeEach` were repointed at `/projects` — without that they die before their first assertion. ⚠ **Not verified: those three specs were not RUN** (they need a live server and `E2E_PROJECT`). The edit is one string per file, but "the route change is right" is a different claim from "the suite is green". [REQ: the-fleet-is-the-landing-screen-and-an-unfinished-answer-is-not-an-empty-one]
- [x] 7.11 The pre-answer state: while discovery has not returned, the screen says it is looking — never an empty fleet, a zero, or the word idle. Distinguish it from a completed discovery that genuinely found nothing. **DONE 2026-08-18 — four states, not the two the task names.** *Looking* (no count of any kind), *answered-empty* (says the discovery ran, with when and how many projects were searched), *answered*, and *unreachable* — discovery never answered, which says it knows nothing rather than that there is nothing. The fourth was not asked for and is the same false-absence class the task exists for. A defect found while reading was fixed with it: any failed poll used to replace a true screen with an error, so one bad refresh erased a real measurement; now the last answer is kept and its age is marked. [REQ: the-fleet-is-the-landing-screen-and-an-unfinished-answer-is-not-an-empty-one]
- [x] 7.12 Open-the-log view shows the raw conversation (design §5.8); leave room for the existing timeline as a later tab without building it. **DONE 2026-08-18, and the limit is IN the delivery.** The raw conversation was already there; what landed is the room — a tab strip with `beszélgetés` selected and the timeline present as a **disabled span, not a button**, because a control that opens onto nothing is what 8.2 forbids and silence would be worse. The timeline itself is not built and nothing renders it. [REQ: listing-every-agent-does-not-read-every-log-in-full]
- [x] 7.13 Show orphaned waiters where the missing-waiter remedy is offered, each removable individually and never in one sweep; state that removal kills a process. The debris belongs next to the offer that would otherwise add to it. [REQ: an-orphaned-waiter-is-shown-and-removing-it-is-an-offer-rather-than-a-tidy-up] **DONE 2026-08-19 (`58393c0b`).** Only `orphaned` rows get a remove control, one at a time, with an in-place confirm that says it stops process N — no bulk control and no checkboxes, so the surface has no shape that could send more than one. `measured: false` renders as *could not be measured*, never as a clean list: an empty list and an unmeasured one are the false-absence pair this whole screen is built against.
- [x] 7.14 WEB — Surface a project that holds work awaiting a human answer even when it has no running agent, counting what is awaiting rather than who is present. This is the ordinary shape of stopped work, and an agent-centric tile renders it as nothing to do. **DONE 2026-08-19** (`lib/set_orch/fleet/awaiting.py`). **THREE kinds, never summed into one "blocked"**, because a reader acts on them differently: *manual* (the plan declares a step no agent can take), *stalled* (the engine recorded it), and *orphaned* — MEASURED: marked in flight, the recorded process is gone. A fourth value, *unverifiable*, is carried and deliberately NOT counted: a pid alive is not the same process (pids are recycled), so reporting it as healthy would be a claim and as orphaned a false alarm. Found on real data the day it was built: one project held two changes marked `running` since 12 June whose processes were long dead and whose state file had not been written since 24 July — **68 days of "in progress" that was not**, and counted by agents that project rendered as nothing to do. `source_missing` travels with the count because 37 of 41 projects have no orchestration state at all: a bare zero would have described *we looked nowhere* as *there is nothing*. [REQ: a-project-awaiting-a-human-is-surfaced-even-when-no-agent-is-running]
- [x] 7.15 WEB — Render the install affordance where the capability report says *not connected*, and render the installer's report where the reader is standing: skipped files with reasons, changed-nothing said out loud, a missing requirement as a refusal rather than a warning. A screen that renders only success re-creates the silence the installer's contract forbids. [REQ: an-install-offered-from-the-screen-goes-through-the-module-installer-and-shows-what-it-did-not-do] ⚠ **NO LONGER BLOCKED as of 2026-08-19.** The missing module installer was the blocker; it is built (`module-install-writer`), and the route it needs is live at `POST /api/fleet/projects/{name}/install` — `dry_run` by default, returning `written`, `skipped` (each with its reason), `changed_nothing` as its own field, and `lines`. A refusal comes back as **409** with the missing requirement named, which is the case this task calls a refusal rather than a warning. Nothing here is done yet; the blocker note is lifted so the next session does not re-derive it — a blocker that is gone and not recorded reads as still blocked. **DONE 2026-08-19 (`e80a970c`).** Preview first, the report in full, then a second deliberate click that writes — in the project's own strip, folded shut, because it is the only control here that writes into a foreign tree. `changed_nothing` is READ, never derived (`written.length === 0` is the producer's rule copied, and the copy that reads as success); a missing field is *unstated*, not false. Skips are listed with their reasons at the same weight as the writes and never behind a fold; a skip the producer did not explain is marked rather than dropped. A 409 renders red and terminal with nothing to click past it, 404 gets its own sentence, and the tense comes from `dry_run` in the report rather than from which button was pressed. The offer is the NARROW reading — `not-connected` only: `partial` means the project already holds some of these files without an install record, so nobody has measured what a write would land on; the state and its reason are shown, the offer is not. ⚠ **Finding for the API side: the two namespaces overlap 3 of 4.** The capability report names `core-rules`, and `POST …/install` answers 409 "no module named 'core-rules' ships with this framework" — the surface draws it correctly as a refusal, but it is the first row a reader would click. 9 mutations, 9 caught. Measured against the running server with dry runs only; no real write was made. Hooks for 9.17: `data-fleet-install-preview`, `data-fleet-install-report`, `data-fleet-install-tense` (`preview`/`done`), `data-fleet-install-changed` (`nothing`/`unstated`), `data-fleet-install-skipped`, `data-fleet-install-skip-stated`, `data-fleet-install-refusal` (`refused`/`not-listed`/`failed`), `data-fleet-install-for-real`.

- [x] 7.16 WEB — The agent panel is a GRID, not a column: **two columns by default**, 1–4 per project, remembered. **DONE 2026-08-19** — asked for in the morning conversation (*"elég nagy a képernyő hozzá"*). An out-of-range stored value falls back to the DEFAULT rather than to the nearest legal one: a hand-edited or stale value is corruption, not preference, and "nearest sensible" would claim to know what was meant. The counts are whole class names — a `grid-cols-${n}` is invisible to Tailwind's source scan, which produces a class in the DOM and none in the CSS: a layout that silently does not apply. [REQ: view-state-is-remembered-per-project]
- [x] 7.17 WEB — The project column needs **visible boundaries**. **DONE 2026-08-19, and the first attempt rendered invisibly** — `--color-surface-line` and `--color-surface-raised` are the SAME value (neutral-800), so a border painted with it cannot be seen against the surface it bounds. A palette name that promises an edge while aliasing a fill is the second-place defect one layer down; `surface-edge` (neutral-700) is the visible one, and the code now says why. The separator sits ON the row rather than BETWEEN rows: a gap between siblings disappears exactly when one is dragged out, and a boundary that vanishes during a reorder is worse than none. Raised 2026-08-19: *"nagyon összefolynak a dolgok, mert nincsenek határok közöttük"* — rows, group headers, the parked section and the ungrouped block all read as one surface. This is the density rule from `ui-quality.md` applied to the left panel: comparable rows want a table's separation, and a group is a block only if its edges are visible. [REQ: projects-on-the-left-the-selected-project-s-agents-on-the-right]
- [x] 7.18 WEB — **Descendants ON the tile, with a count and a way in.** Raised 2026-08-19: small arrows or buttons next to an agent showing how many run under it, opening them from there. 7.8 answers *who started this one* (upwards); this is the same relation downwards, and it is the one that matters when an agent has spawned a fleet of its own. ⚠ It must not count what it cannot see: an agent started with `claude -p` that has already exited leaves no process, so a count taken from live processes alone will under-report — the same false-absence class as 7.14. **CORE HALF DONE 2026-08-19; the tile itself is the UI agent's.** The payload carries `descendants: {known, live, pids, live_only, reason}`. Built from **`requested_by`, recorded by the owner in the act of starting** — the only moment the relation exists. The process tree cannot answer it: measured, **0 of 23** live agents had an agent ancestor, and an agent this surface starts has the OWNER (a plain python process) as its parent with systemd above it, so no walk will ever find which agent asked for it. ⚠ **The blind spot travels with the number, including when it is zero** — which is exactly when a reader takes a count for completeness: a child started with `claude -p` that has already exited leaves no process, so `live_only` and its reason are always present, and a mutation that drops them is caught. A seatless agent reports `known: false` rather than `0`, because a zero there would say *nothing runs under it* about an agent that may have started five. The index is built ONCE for the whole fleet — a per-project one would report a lineage that stops at the project boundary, which is not where lineage stops — and a test fails if it moves inside the loop. Verified live: this session shows **1 descendant, the agent it started from the screen**. `pids` is the way in the task asks for. 4/4 mutations caught. [REQ: an-agent-started-by-another-agent-is-identified-as-its-descendant] **DONE 2026-08-19 (`37e83a68`).** `↳ N*` on the tile with the pids clickable, and the `*` carries the bound ON the number — `live_only` means the count covers recorded starts that are still running, so a bare `2` would claim something nobody measured. `known: false` renders as *could not look*, never as zero: without a seat there is no key to look the agent up by. The count comes from the pid LIST rather than the reported number, because a count disagreeing with its own breakdown is what turns a wrong zero into a proof. Suppressed where the tile already says the agent has no seat — seen on the live screen as `↳ ?` on four tiles of five, one cause marked twice.
- [x] 7.20 WEB — **A beszélgetés-nézet 92,5%-ban gépezetet rajzol, és „te"-nek nevezi, amit nem te mondtál.** Felvetve 2026-08-19 képernyőképpel: *„nagyon szarul néz ki a naplós beszélgetés, ha nem terminál."* A 7.12 csak azt szállította, hogy a nézet LÉTEZIK; a kinézetéről egyetlen taszk sem szólt eddig, és a `ui-quality.md` mércéje ettől még kötelező. **Mérve egy élő ülés 40 fordulóján:** 19 forduló `user` szerepű, szöveg nélkül, csak egy eszköz-EREDMÉNNYEL; 18 forduló `assistant`, szöveg nélkül, csak egy eszközhívással; és **3 forduló, azaz 7,5%, hordoz valódi szöveget.** Ebből három állítás következik, és az első nem esztétikai: **DONE 2026-08-19 (`c07b8f8a`).** The speaker is decided from the TEXT, never from the role — `you` is reserved for what the person actually said, and a `user`-role turn carrying only a runtime envelope is labelled `runtime`. A tool call and its result are ONE act on one row, so 40 turns stop rendering as 37 speakers. Hierarchy: what was said is body text, tool traffic is one dim line. ⚠ **The compaction says what it cannot see:** `/log` reduces tool results to a count and drops `is_error`, so the collapsed group states *failure state not carried* rather than implying calm — the rule that compacting must never hide a failure, honoured by admitting the gap instead of inventing a zero. Left for the API side: passing `is_error` through would let a failed call be marked where the reader is standing.

  1. **HAMIS ÉRTÉK: a 19 eredmény-forduló `te` felirattal jelenik meg.** Azt mondja, hogy az ember mondta, amit a futtatókörnyezet táplált vissza. A szerep a naplóban `user`, de az nem a beszélő — a szerep-mező és a beszélő két különböző dolog, és a felület a másodikat állítja az elsőből.
  2. **Egy aktus, két sor, két „beszélő".** Az eszközhívás és az eredménye egy cselekvés; a nézet szétvágja őket, és a kettő közé rakja a szerepváltást. Össze kell vonni.
  3. **A tartalom a zajban áll.** Minden sor `text-xs`, azonos súllyal: nincs mód végigfutni a szemmel és megtalálni, mit MONDOTT az agent. A `ui-quality.md` sűrűség-szabálya ide szól — az összehasonlítható sorok táblázatot kívánnak, a mondatok nem sorok.

  ⚠ Amit a tömörítés NEM tehet: elrejteni egy hibát. Egy összecsukott eszközcsoport, amiben egy hívás hibára futott, jelölést kap ott, ahol az olvasó áll. És a mérce nem a szerkezet: a `ui-quality.md` szerint a kész képernyőt **meg kell nézni** — a lerenderelt sorok száma azt bizonyítja, hogy megjelenik, nem azt, hogy olvasható. [REQ: listing-every-agent-does-not-read-every-log-in-full]

- [x] 7.21 WEB — **A flotta-képernyő szövege ANGOL, mint a dashboard többi része.** Felvetve 2026-08-19: *„flotta angolul kellene legyen ugye!! itt minden angol. csak a projektek magyarok."* Mérve, mielőtt bármit átírtam: a `web/src` alatt **kizárólag a flotta-fájlok** hordoznak magyar felhasználói szöveget (`Fleet.tsx` 318, `FleetProjectColumn.tsx` 209, `FleetTerminal.tsx` 55 ékezetes karakter), a dashboard minden más része végig angol — tehát ezt **ez a change hozta be**, nem örökölt állapot. ⚠ **A fordítás ára a tesztekben van, és ez a fele nem kozmetikai:** nincs i18n-réteg, tehát hat flotta-teszt közvetlenül a magyar stringekre állít; elsőre **15 teszt bukott el, mind a 15 a szöveg miatt, egy sem viselkedés miatt**. Ami NEM fordul le: a felhasználó szavait szó szerint idéző kommentek — egy idézet attól idézet, hogy az, ami elhangzott. **RÉSZLEGES 2026-08-19 (`675d6b5f`):** kész a bal oszlop, a terminál-panel és a terminál-ajánlat; **a `Fleet.tsx` nincs meg**, mert egy párhuzamos agent épp azt írja a 7.20-hoz, és két író egy fájlon a legdrágább fajta ütközés. Egy állítás emiatt szándékosan magyar maradt (`fleetArrangement`, „válaszra vár"): a szöveg a `Fleet.tsx`-ből jön, és angolra állítani előre annyi volna, mint olyan képernyőre tesztelni, amit még senki nem épített meg. [REQ: the-fleet-is-the-landing-screen-and-an-unfinished-answer-is-not-an-empty-one] **DONE 2026-08-19** — the remaining half landed with `50cfad62` and the rounds after it. **Measured now, the way the first measurement was made:** zero Hungarian user-visible text anywhere under `web/src` outside comments (all `.ts`/`.tsx`, block and line comments stripped before counting). What accented characters remain are inside comments and are verbatim quotes of the user — a quote is a quote because it is what was said, and paraphrasing it into English destroys the evidence it carries.

- [ ] 7.19 WEB — **A workflow is a level between the project and the agent.** Stated 2026-08-19: agents will be started per developer need (bugfix, an OpenSpec feature), each will start its own sub-agents, and several such workflows will run in one project at once — *"most leginkább egy workflow-ben dolgoztunk, de hát ez nem így lesz"*. The panel groups by project today, which renders a project's parallel workflows as one undifferentiated list. ⚠ Do NOT invent a workflow id here: the work-cycle engine is the thing that knows what a workflow is, so this task is blocked on that record rather than on UI work — inventing a second definition would be the parallel-mechanism failure this repository already names. [REQ: a-project-tile-carries-the-state-of-the-agents-inside-it]

## 8. Terminal in the browser (WEB)

- [x] 8.1 Add the terminal emulator component as a frontend dependency and render it against the stream endpoint. **Sized by 1.1, now answered: surface-started sessions only** — resume-based adoption is refuted, and the user chose not to shrink the rest of this group on the strength of it. [REQ: terminal-traffic-travels-in-both-directions-and-is-never-persisted] **DONE 2026-08-19** — `@xterm/xterm` + `@xterm/addon-fit`, rendered against the WebSocket relay. Nothing is written down on the way: frames pass through the component and are not cached, logged or persisted.
- [x] 8.2 Offer a terminal only where one can exist; where it cannot, state the reason in its place and keep the bus input. No control that opens onto nothing. [REQ: a-tile-offers-a-terminal-only-where-one-can-exist-and-says-why-when-it-cannot] **DONE 2026-08-19, and it is a THREE-way distinction rather than the two the task names.** Where the framework started the agent the terminal opens; where it did not, the place says *terminál: nem a kereté*; and where the owner service cannot be asked it says *nem tudjuk* — a silent owner used to read as *no agent has a terminal*, which is a false absence stated with confidence.
- [x] 8.3 Start-an-agent action, and reattach to a running one after a reload. [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting] **DONE 2026-08-19, and the reattach half was broken in a way only the live path could show.** Start works from the project header. Reattach after a reload replays the buffered screen — and that replay killed its own connection above ~48 KiB, deterministically, while reporting that the OWNER had closed it. Fixed at both ends (`b64fdfa1`) and proven live at the full 64 KiB tail: *él 65536 bájt visszajátszva · a puffer eleje levágva*.

## 9. Proof — written against the result, not the mechanism

- [x] 9.1 Discovery against known truth: assert the result against the registry's own recorded process-to-session bindings, failing on any *silent* mismatch. A labelled guess is not a failure; an unlabelled one is. [REQ: a-process-is-bound-to-its-session-log-by-recorded-fact-and-a-guess-says-so] **DONE 2026-08-19.** Fixture-driven in `test_a_binding_is_never_guessed_and_an_unlabelled_guess_would_fail_here`, which asserts something stronger than the task asks: there is no guessing path at all, so EVERY binding present must be confirmed — that fails the moment a heuristic is added without a label, rather than only when an existing label is dropped. The fixture puts a plausible log next to an unbound agent, because preferring no binding to an arbitrary one is the rule. Live check the same day: **23 agents, 0 silent mismatches, 0 unlabelled guesses.**
- [x] 9.2 Delivery, driven apart: a fixture that produces each of the four outcomes explicitly — including *held*, and a held message that then lapses — asserting the reported outcome each time. A test asserting only that the send call was made passes identically on all of them. **DONE 2026-08-19** — `tests/unit/test_fleet_instruct.py`, and the four are driven apart by the FIXTURE rather than by the call: the channel's answer and the waiter list are set independently, so the same successful send yields *arrives-now*, *at-turn-end* or *sits-unread*. All four were then reproduced end to end against the **real** `sac` binary in an isolated store, which is the half a fake cannot prove. Proven by mutation, not by passing: **11 mutations, 11 caught** — among them *a waiter is not required for arrives-now*, *an answer with no `wakes` upgraded to a delivery*, *a held message counted as reaching the agent*, *a hold treated as a resting state* and *the address dropped from the send*. The loop asserts the mutation pattern is UNIQUE (one ambiguity was caught and fixed), clears `__pycache__` with `PYTHONDONTWRITEBYTECODE=1`, and verifies the restore byte for byte — the file is untracked, so a `git checkout` restore would have failed silently.
- [x] 9.3 Assert the union and the worktree rule on a fixture with two worktrees of one repository and a project known only to a process — the two cases that produced the false absence and the phantom projects. [REQ: a-working-directory-resolves-to-a-project-through-git-not-by-path-matching] **DONE 2026-08-19** for the worktree half — two checkouts of one repository resolve to one project, which is the case that produced the phantom. ⚠ The union half is only two of three sources (see 2.4), so a fixture asserting all three would assert a union that does not exist yet; that assertion lands with 2.4 rather than being written now against a shape that is about to change.
- [x] 9.4 Assert unknown-not-idle on all three of its causes: no log yet, unreadable log, absent (not empty) status key. [REQ: a-state-that-cannot-be-determined-is-unknown-never-idle] **DONE 2026-08-19, with a FOURTH cause the task did not name.** The parametrisation covered absent, missing and unparsable; `unreadable` — the file exists and a permission or filesystem error stops the read — takes a different code path and is the one an agent actually hits. Three named causes and three covered looked complete; it was three of four.
- [x] 9.5 `web/tests/e2e/`: drive the terminal the way a person does — a keystroke entering the browser terminal component reaches the agent and its output returns there. Writing to the pty file descriptor from the test is not accepted as proof. **DONE 2026-08-19** — `web/tests/e2e/fleet-terminal.spec.ts`. The test **starts its own agent** through `POST /api/fleet/agents` and stops it again; it deliberately does not borrow one that is running, because typing into somebody's live session is not a test, it is an interruption. Then it does what a person does: selects the project in the left column (the right panel shows only the selected project's agents, and reaching straight for a tile would be the harness using a route the user does not have), clicks the terminal control keyed by the LABEL it started under, and types with `page.keyboard.type` — the browser's own event path. **PROVEN BY MUTATION, not by passing:** the WebSocket relay's `await stream.write(payload)` was replaced with `pass`, the service restarted, and 9.5 **failed** while 9.6 and 9.7 correctly stayed green; restored, verified by grep, and green again. A test that failed in both directions would be measuring nothing. [REQ: the-terminal-is-proven-by-driving-it-as-a-person-drives-it]
- [x] 9.6 `web/tests/e2e/`: assert the negative half too — no terminal offered for an agent the framework did not start. A positive-only check passes on a build that offers one for every agent. **DONE 2026-08-19.** Asserts on a card scoped to ONE foreign agent rather than page-wide: the project may also hold agents the framework did start, and a page-wide *no terminal offered* would then assert the opposite of the truth. Two assertions, not one — the control is **absent** (`toHaveCount(0)`) and the **reason is shown** (`data-fleet-terminal-absent`), because a missing control with no explanation leaves a reader wondering whether the screen is broken. [REQ: the-terminal-is-proven-by-driving-it-as-a-person-drives-it]
- [x] 9.7 `web/tests/e2e/`: the landing screen's pre-answer state — with discovery delayed, assert the screen says it is looking and shows no zero. **DONE 2026-08-19.** Discovery is held open with a route interception so the screen can be observed in a state it otherwise passes through too fast to see — and the hold is on the **producer**, so what is measured is still the screen's own behaviour. Asserts `data-fleet-phase="looking"` and that the body contains **no count**: no `0 agents`, no `0 waiting for an answer`, no `idle`. ⚠ **And the second half matters as much:** after the response is released the looking state must go to `toHaveCount(0)`, because a test that only saw the first half would pass on a screen that never finishes looking — a worse bug than the one it guards. ⚠ Recorded for the next runner: the readiness check must poll the **root page**, not `/api/fleet/owner`. The API answers within a second of a restart while the static page takes ~25 s, and polling the API produced three instant failures that looked like product defects. [REQ: the-fleet-is-the-landing-screen-and-an-unfinished-answer-is-not-an-empty-one]
- [x] 9.8 Stash-check every test written above: `git stash && pytest <new tests>; git stash pop`. A test that passes without its fix proves nothing and looks like proof forever. Restore by a means that works on untracked files, and re-grep the file to confirm the restore landed. [REQ: a-state-that-cannot-be-determined-is-unknown-never-idle] **DONE 2026-08-19 by a STRONGER means, and the substitution is deliberate rather than a shortcut.** `git stash` is unsafe here for two measured reasons: most of the new files are UNTRACKED, so a stash-and-restore cycle cannot restore them (and `git checkout` silently succeeds while restoring nothing), and other sessions are writing to this tree, so a stash would take their work too. Every fix this round was instead mutation-tested: the fix put back one at a time, each pattern asserted unique before replacing (never a silent `replace(..., 1)` tie-break), each restore verified BYTE-WISE with `cmp` afterwards, and `__pycache__` cleared between runs because two mutations of one file can produce identical sizes and reuse a stale `.pyc`. Mutations run this round: ~20, all caught after correction. **Three of them did not fire on the first attempt and the MUTATION was the weak half each time** — a pattern that did not exist in the file, a move that changed nothing load-bearing, and a semantic swap that is currently equivalent because no method returns a null result. Each is recorded where it happened.
- [x] 9.9 Regression check against a real baseline: `git worktree add --detach`, `PYTHONPATH` at the baseline's own three source roots, plus the session-end leak assertion from `CLAUDE.md`. Compare failure sets, not counts. [REQ: nothing-derived-from-an-agent-s-session-is-persisted] **DONE 2026-08-19, and run after every commit this round rather than once at the end.** `git worktree add --detach` at `HEAD`, `PYTHONPATH` at the baseline's own source roots, and the session-end leak assertion from `CLAUDE.md`. Result each time: **0 leaks, 107 = 107, the diff EMPTY.** One real regression was caught by it and fixed — see 6.4's note on `asyncio.run` clearing the thread's event loop, which turned ten unrelated streaming tests red in a full-suite run only.
- [x] 9.10 Open the screen and look at it, with several agents in several projects and at least one waiting. Structural counts prove it renders; they say nothing about whether it is readable or whether two fields contradict each other. **DONE 2026-08-19 — looked at, with 12 agents in 6 projects and one waiting, and it found five things the structural counts had all passed.** The screen renders, every row is there, no JS errors — and: **(1) it is HALF TRANSLATED, which is worse than either state.** `Fleet.tsx` still says `Flotta`, `csendes`, `ismeretlen`, `válaszra vár`, `agent indítása`, `BESOROLATLAN`, `oszlop`, `+ csoport`, `feljegyezve` — sitting beside `open the log`, `enlarge`, `full screen`, `2 waiting for an answer`, `35 with no agents — show` from the already-translated column. One card reads `● válaszra vár input needed`: **the same state said twice, in two languages, on one line.** That is 7.21's remainder and it is now the loudest thing on the screen. **(2) The agent grid uses the top third and leaves the rest black** — five agents in two columns stop after three rows, and roughly 60 % of a 1900×1100 viewport is empty while the left column is a dense list. Density is a decision, and this one was made by the grid rather than by anybody. **(3) An excerpt renders raw markdown as prose:** `| |---|---| | **7.7** utasítás | input a csempén; **három mező külön**` — pipe-table fragments and bold markers shown literally. The excerpt is the tile's answer to *what is going on*, and here it is unreadable exactly on the agent doing the most. **(4) The header count is rendered twice, in two places and two languages** — `12 agent · 6 projektben` at the top and `12 agents · 41 projects` in the column. Two copies of one number is the second-place defect inside one screen. **(5) What WORKS and is worth recording:** `no input: this session has no seat on the messaging bus` on four of five cards — 4.4 doing its job and making the enrolment gap visible where a reader is standing; `2 orphaned waiter(s)` in the project header; and the instruction input present on the one agent that has a seat. Items 1–4 are handed to the UI agent; this task is about having looked, and looking is what produced them. [REQ: an-agent-tile-carries-state-log-excerpt-and-its-own-input]

- [x] 9.11 Assert the resume refusal on a session with a live process, and assert the *shape* of the damage it prevents rather than only the refusal: a fixture that performs the forbidden resume must produce a log with two leaves under one parent. A test asserting only "the button is disabled" passes on a build whose backend still resumes. [REQ: resuming-a-session-that-is-running-is-refused-not-offered] **PARTIAL 2026-08-19 — the refusal is asserted, the SHAPE OF THE DAMAGE is not, and the name of this task is wider than what is delivered.** Four tests hold the refusal from every angle: a session bound to a live process, an undeterminable liveness treated as live, a scope still `deactivating`, and the ordering (the liveness check runs before the stop, or a refusal would kill what it was protecting). What is NOT built is the second half — a fixture that performs the forbidden resume and demonstrates the fork, so the test asserts against an observed harm rather than against a rule. That needs two live sessions on one transcript and a way to observe the branch; §6.1 records that it was seen once, by hand. **COMPLETED 2026-08-19 — the second half is built, so the name and the delivery now agree.** The harm is performed rather than described: `test_a_resume_against_a_live_session_forks_its_transcript` drives `recover()` with a `start` that does what a resumed session does first — read the transcript's tail and append under it — while the original, which knows nothing, appends its own next turn under the tail it was already holding. The result is read off the file afterwards: `{"turn-3": ["resumed-1", "original-4"]}`, **two children of one parent**, where a session has exactly one continuation. Nothing raised and nothing logged, which is the point. Its twin, `test_the_refusal_is_what_keeps_the_transcript_single_threaded`, runs the SAME fixture, the same call and the same writer, changing only what the liveness reader answers — so what it asserts is the guard and nothing else. **Why the four existing tests were not enough, stated so it is not re-litigated:** all four assert that something was refused, and a refusal is a proxy for a harm; they pass on a build whose guard checks the wrong thing as long as it raises. These two assert the harm. Mutation-proven: replacing `live = discovery.live_session_ids()` with `live = set()` — the unit-based check the docstring warns about, which clears a hand-started session because it has no scope — makes the refusal test produce the fork instead, and it fails. And the demonstration is itself falsifiable: a `start` that writes nothing makes the fork test fail, so it cannot pass on a fixture where no second writer ever existed. The fork now has a searchable name — *two leaves under one parent* — which design §6.1 recorded only as something seen once by hand.
- [x] 9.12 Assert that an agent with no registry entry and no session log is listed anyway, driven from a process that genuinely has neither — not from a record with the fields blanked, which is a different case and the one that already works. [REQ: an-agent-that-registers-nothing-is-still-an-agent] **DONE 2026-08-19, in the shape the task insists on.** Driven from a `/proc` that genuinely holds an agent and a record directory that genuinely holds nothing about it — not from an `Agent` with its fields blanked, which tests the dataclass rather than the discovery. The difference is where the bug would be: a lookup that raises, a dict access that assumes a key, a filter that drops what it cannot resolve — and none of that runs when the object is built by hand.
- [x] 9.13 Assert *waiting for a person* against a stale record as well as a fresh one: fresh yields waiting with its reason, stale yields unknown. A positive-only test passes on a build that believes any record it finds. [REQ: waiting-for-a-person-is-a-state-of-its-own-and-it-says-what-for] **DONE 2026-08-19, and the task's second half is SUPERSEDED by measurement rather than skipped.** "Stale yields unknown" assumes a freshness signal in the record; there is none — `updatedAt` equals `statusUpdatedAt` in 22 of 22, so the timestamp is the age of the STATE, not of the observation, and an age threshold would discard the true positives first (see 3.8). What IS asserted is the staleness that fires in practice: a record declaring a state the log contradicts yields the MEASURED state plus `declaration_ignored`, never the declared one. Both directions are held, so a build that believes the record fails.

- [x] 9.14 Assert the orphan-waiter rule in its dangerous direction: a fixture holding one dead-session waiter, one live one, and one whose session cannot be determined — only the first may be offered or removed. Include a candidate whose match arises from the checking command itself, and assert it is not offered. A test that only proves the orphan gets removed passes on a build that removes all three. **DONE 2026-08-19** — a fake `/proc` holding exactly those three, plus **three** impostors a substring test would take: a `grep -af 'sac.mjs wait'`, a shell running `pgrep -af 'sac.mjs wait' | wc -l`, and a `python3 -c`. `test_a_bare_substring_check_would_not_have_caught_it` holds the WRONG pattern beside the right one and asserts they disagree, so a simplification fails here rather than looking identical. Mutating the structural test back to a substring is caught.
- [x] 9.15 Assert the no-agent case directly: a fixture with an open decision recorded against a task and **no process running**, asserting the project is surfaced as awaiting an answer. A test that only covers a waiting agent passes on a build blind to the common case. **DONE 2026-08-19, and writing the test FOUND the defect it was meant to guard.** The three kinds 7.14 shipped (`manual`, `stalled`, `orphaned`) all read the **orchestration state file** and were blind to an open decision entirely — yet that is the carrier the engine actually uses: `connector.mark_awaiting` writes `<!-- awaiting: … -->` into the change's own **task file**, which is the thing that outlives the run. So the ordinary shape of stopped work — question recorded, no process, no state entry, nothing on any agent tile — counted as **zero**. A fourth kind, `decision`, now reads the task files and reaches the total. ⚠ The marker regex is a **second copy** (D10 again: `set_orch` may not import the engine), held by a test that imports both and fails on divergence. The archive is skipped — an archived change's decision was answered or overtaken, and counting it would make a finished project look permanently blocked. A marker on an unnumbered line is kept under its change rather than dropped, because dropping it for a less precise key is the false absence this module exists against. 4/4 mutations caught. [REQ: a-project-awaiting-a-human-is-surfaced-even-when-no-agent-is-running]
- [x] 9.16 Enumerate the surface's start paths and assert exactly one starts a work unit. A test that only checks the engine's command works passes on a build that also kept a direct spawn. **DONE 2026-08-19, and the first version of this test was the defect it exists to catch.** It scanned the route sources for the engine's name — a substring test wearing a guarantee: it passes on a route that merely *mentions* the constant and fails on one that reaches it through a variable, which is exactly what happened on the first run. Rewritten to **drive** both starters against a fake owner and assert the argv each one actually hands over. Two starters are expected, not one — the bare-session route is deliberately still there, so *only one start route* would be wrong in the other direction; what must be unique is the one that starts a WORK UNIT. And the bare one is asserted to hand over **no argv at all**, so it cannot become a second way to run the engine by being passed one. A companion test asserts the subcommand this surface names is the one the **engine's own parser** marks `starts_a_unit=True`, rather than trusting the string `run`. [REQ: starting-work-goes-through-the-engine-s-one-entry-point-not-a-second-spawn-path]
- [x] 9.17 Assert the install surface on its unhappy paths, which are the ones a demo never reaches: an install that skips every file, one that writes nothing, and one refused for a missing required module. Assert what the SCREEN shows in each, not what the installer returned — the two differ exactly when the surface is wrong. [REQ: an-install-offered-from-the-screen-goes-through-the-module-installer-and-shows-what-it-did-not-do] ⚠ **NO LONGER BLOCKED as of 2026-08-19.** The missing module installer was the blocker; it is built (`module-install-writer`), and the route it needs is live at `POST /api/fleet/projects/{name}/install` — `dry_run` by default, returning `written`, `skipped` (each with its reason), `changed_nothing` as its own field, and `lines`. A refusal comes back as **409** with the missing requirement named, which is the case this task calls a refusal rather than a warning. Nothing here is done yet; the blocker note is lifted so the next session does not re-derive it — a blocker that is gone and not recorded reads as still blocked.

## 10. Debt this change names rather than absorbs

- [x] 10.1 Record that `openspec/specs/unified-navigation/spec.md` is stale against the shipped app — it describes `/manager/*` and `/set/*` sidebar routes that are now legacy redirects in `App.tsx`. Not fixed here; a retroactive correction of its own, so the landing change does not quietly inherit a rewrite of an unrelated spec. **DONE 2026-08-19** — the record is a banner at the top of `openspec/specs/unified-navigation/spec.md` itself, not a line in this task list. That placement is the point: **that file is the one a reader will believe**, and a correction filed anywhere else is a correction put where nobody is standing — the same failure this change keeps finding in layouts. Verified rather than asserted: the spec describes `/manager/*` and `/set/*`; measured in `web/src/App.tsx`, the app serves `/projects` and `/p/:name/*` and both of those families are **redirects** (`App.tsx:197`, `App.tsx:208`), with the root route rendering the fleet. Not corrected here, deliberately — rewriting an unrelated capability's spec inside a landing-screen change is how a correction ships with nobody having reviewed it as one. [REQ: the-fleet-is-the-landing-screen-and-an-unfinished-answer-is-not-an-empty-one]

- [x] 10.2 Record the user's request of 2026-08-18 for a **third source of state — an interpreting agent** that reads the other sessions' output and, knowing which task each was given, reports what is running, waiting or finished. **Deferred by the user until the base functions work**, and deliberately NOT built here. Design §6.5 settles the shape it must take before anyone starts: an interpretation is a guess and is labelled one, it may add a state but never replace a measured one nor silence an `unknown`, its uncertainty resolves toward *needs a look* rather than *done*, its "finished" is a claim shown next to a checkable trace or next to the absence of one, and it persists nothing derived from what it reads. It belongs in its own capability rather than in `agent-fleet-state`, which is defined as measurement. [REQ: a-state-that-cannot-be-determined-is-unknown-never-idle] **RECORDED 2026-08-19 — which is what this task asks for, and it is done.** The shape is settled in design §6.5 (four constraints, each a defect class this repository has already paid for) and the deferral is the user's. Marked here rather than left open, because an item whose deliverable is *a record* and whose record exists is done — leaving it ticked-open makes the count say there is work where there is none, which is the same false value in the other direction.

- [x] 10.3 **A latent flake in the fleet suite, found 2026-08-19 and deliberately NOT fixed here — and it is currently HIDDEN by this change's own file.** Running the eight pre-existing `test_fleet_*.py` files together, in that order, fails `test_fleet_api.py::test_the_terminal_route_is_registered_before_the_project_ws_wildcard` with a `KeyError` inside `importlib._ModuleLock` at the first import of `set_orch.server`, preceded by five `Task was destroyed but it is pending` from `ownerd`/`owner_client`. Measured: deterministic in that combination, green alone, green for every subset tried, green at `ba3418fa` (121 passed, isolated worktree with `PYTHONPATH` set), and **green again once `test_fleet_instruct.py` joins the run** — 187 passed, twice. So this change made the symptom disappear by luck, not by repair. Recorded rather than absorbed for the reason this file keeps finding: *a dead test looks exactly like a passing one from far enough away*, and a suite that is green because of collection order will go red on an unrelated new file. The cause to fix is the asyncio teardown in the owner tests leaking into a later import, not the assertion that fails. **RECORDED 2026-08-19, and it is still true.** Re-measured after every commit of this round: the eight pre-existing `test_fleet_*.py` files still fail together in that order, and the suite is still green once this change's own files join the run. Nothing has been done to the cause — the asyncio teardown in the owner tests leaking into a later import — and this item stays open because the DEBT is open, unlike 10.1 and 10.2 whose deliverable was the record itself. **FIXED 2026-08-19, and two things in the paragraph above are now WRONG — both corrected here rather than deleted, because the refuted pattern is the durable half.** **(a) The reproducer no longer reproduces.** The eight files, in that order, are green — three consecutive runs, 189 passed. They have grown from 144 tests to 189 since the record was written, so the collection order that produced the `KeyError` is gone. The line above claiming *"re-measured after every commit … still fail together"* was true when written and stopped being true without anybody noticing, which is the stale-figure class applied to a reproducer. A symptom that stops appearing is not a repair. **(b) The cause was attributed to the wrong side.** It said *the asyncio teardown in the owner tests*. Half of it was there — `_run()` closed its loop with handler tasks still pending — but the thing that actually raised is **production code**: `ownerd._serve_client`'s `finally` calls `writer.close()`, which schedules on the loop, and by the time the garbage collector runs that `finally` the loop is gone. And `shutdown()` never ended those handlers at all: `Server.wait_closed()` answers about the LISTENING sockets, not about the per-connection tasks, which asyncio creates and keeps no public handle on. So on a real shutdown the unsubscribe was skipped and the transport close raised — reported as an *unraisable* exception, printed and carried on from, attributed to whichever test the collector interrupted. The fail direction is the reassuring one twice over: no caller ever sees it, and it lands far from its cause. **The fix, three parts:** `_serve_client` tracks its own task and tolerates a close after the loop; `shutdown()` cancels the tracked handlers while the loop is still alive, which is the only moment their cleanup can run; and `_run()` in the test file cancels what is left instead of abandoning it. **Proved by making the CAUSE fatal rather than waiting for the symptom** — `-W error::pytest.PytestUnraisableExceptionWarning` over the same eight files, which asks about the unraisable exception directly instead of about an import lock that may or may not notice it: **without the fix 3 failed + 1 error, with it 192 passed.** Three mutations, each caught — and the first version of the `shutdown()` test was NOT caught, because it asked its question after `_run` returned and so measured the runner's own cleanup instead of `shutdown()`'s. That is the mechanism-not-result class inside the very test written to close it; the assertion now runs inside the loop. Held by `test_shutdown_ends_the_client_handlers_while_the_loop_is_still_alive`, `test_the_handlers_cleanup_survives_a_transport_that_can_no_longer_be_closed` and `test_this_files_loop_runner_does_not_abandon_pending_tasks` — the last one holds the wrong pattern, a bare `loop.close()`, so a later simplification fails here instead of in an unrelated file's import.

## Acceptance Criteria (from spec scenarios)

### agent-fleet-inventory

**An agent is discovered from process state, not from a command line**

- [ ] AC-1: WHEN an agent process runs with a working directory inside a known project and a command line that names no path THEN it appears in the inventory, attributed to that project [REQ: an-agent-is-discovered-from-process-state-not-from-a-command-line, scenario: an-interactive-session-is-discovered]
- [ ] AC-2: WHEN an agent process runs in a directory belonging to no registered project THEN it appears in the inventory, and its project is reported as known from the process alone [REQ: an-agent-is-discovered-from-process-state-not-from-a-command-line, scenario: a-session-in-an-unregistered-directory-is-still-discovered]

**A process is bound to its session log by recorded fact, and a guess says so**

- [ ] AC-3: WHEN the registry records a session id and owning process id for a live process THEN the inventory binds that process to that session log, marked as confirmed [REQ: a-process-is-bound-to-its-session-log-by-recorded-fact-and-a-guess-says-so, scenario: a-recorded-binding-is-used]
- [ ] AC-4: WHEN no registry record exists and a session log is inferred for a process THEN the binding is reported as unconfirmed, and the surface shows it as a guess [REQ: a-process-is-bound-to-its-session-log-by-recorded-fact-and-a-guess-says-so, scenario: a-heuristic-binding-is-marked]
- [ ] AC-5: WHEN several session logs are equally plausible for one process and no record exists THEN the agent is listed with no session log rather than with an arbitrary one [REQ: a-process-is-bound-to-its-session-log-by-recorded-fact-and-a-guess-says-so, scenario: no-binding-is-better-than-a-wrong-one]

**The inventory is a union of its sources, and names them**

- [ ] AC-6: WHEN an agent runs in a project that appears in neither registry THEN the project is listed, sourced from process discovery [REQ: the-inventory-is-a-union-of-its-sources-and-names-them, scenario: a-project-known-only-to-a-live-process-is-listed]
- [ ] AC-7: WHEN a registered project has no live agent THEN it is listed as holding no agents, and is not dropped from the list [REQ: the-inventory-is-a-union-of-its-sources-and-names-them, scenario: a-registered-project-with-no-agent-is-listed]
- [x] AC-8: WHEN a project is known to more than one source THEN the entry names each source that knew about it [REQ: the-inventory-is-a-union-of-its-sources-and-names-them, scenario: sources-are-reported-rather-than-merged-away]

**An agent that registers nothing is still an agent**

- [ ] AC-9: WHEN an agent process runs having inherited a marker that suppresses its registration and its transcript THEN it is listed from process discovery, with its project, and its missing sources named [REQ: an-agent-that-registers-nothing-is-still-an-agent, scenario: a-child-session-appears]
- [ ] AC-10: WHEN an agent process is alive but has not yet registered because it is waiting at a start-up prompt THEN it is listed, and reported as waiting rather than as unknown-and-idle [REQ: an-agent-that-registers-nothing-is-still-an-agent, scenario: a-session-at-a-start-up-prompt-appears]
- [ ] AC-11: WHEN an agent has no registry entry and no session log THEN the entry states which sources lacked it, rather than presenting a gap as a determined state [REQ: an-agent-that-registers-nothing-is-still-an-agent, scenario: a-record-s-absence-is-reported-not-resolved]

**A working directory resolves to a project through git, not by path matching**

- [ ] AC-12: WHEN two agents run in two worktrees of the same repository THEN both are attributed to that one project, each reporting its own branch [REQ: a-working-directory-resolves-to-a-project-through-git-not-by-path-matching, scenario: agents-in-different-worktrees-belong-to-one-project]
- [ ] AC-13: WHEN a worktree directory sits beside the main checkout with a similar name THEN no separate project entry is created for it [REQ: a-working-directory-resolves-to-a-project-through-git-not-by-path-matching, scenario: a-worktree-is-not-a-project-of-its-own]
- [ ] AC-14: WHEN an agent runs in a directory under no repository THEN the directory itself is the project, and no branch is reported [REQ: a-working-directory-resolves-to-a-project-through-git-not-by-path-matching, scenario: a-directory-that-is-not-a-git-repository]

**An agent started by another agent is identified as its descendant**

- [ ] AC-15: WHEN an agent process descends from another agent process THEN the inventory reports the parent's seat identity [REQ: an-agent-started-by-another-agent-is-identified-as-its-descendant, scenario: a-spawned-agent-names-its-parent]
- [ ] AC-16: WHEN an agent process has no agent ancestor THEN it is reported as having no parent, which is the ordinary case [REQ: an-agent-started-by-another-agent-is-identified-as-its-descendant, scenario: an-agent-started-by-a-person-has-no-parent]
- [ ] AC-17: WHEN an agent has at least one agent descendant THEN it is reported as directing, and each descendant as executing [REQ: an-agent-started-by-another-agent-is-identified-as-its-descendant, scenario: role-follows-from-the-relation-not-from-a-guess]

**A project reports what it has wired in, and dim is not absent**

- [x] AC-18: WHEN the files that constitute a capability are present in a project THEN the project reports that capability as connected [REQ: a-project-reports-what-it-has-wired-in-and-dim-is-not-absent, scenario: a-connected-capability] — tests/unit/test_fleet_capabilities.py::test_a_capability_with_every_file_present_is_connected
- [x] AC-19: WHEN those files are absent but the capability applies to any project THEN the project reports it as not connected, not as absent [REQ: a-project-reports-what-it-has-wired-in-and-dim-is-not-absent, scenario: a-capability-that-could-be-connected] — tests/unit/test_fleet_capabilities.py::test_absent_is_not_connected_rather_than_missing (asserts `not-connected`, never a dropped row); tests/unit/test_fleet_capabilities.py::test_an_unreadable_project_is_unknown_and_never_not_connected (the other direction: unreadable is not not-connected either)
- [x] AC-20: WHEN a project carries a record of the modules it installed THEN the capability report is taken from that record rather than from the presence of files [REQ: a-project-reports-what-it-has-wired-in-and-dim-is-not-absent, scenario: a-declared-install-record-is-the-source] — tests/unit/test_fleet_capabilities.py::test_a_ledgered_file_is_reported_with_its_provenance (ledgered=1, inferred=1 on the same capability — the record is the source where it exists); tests/unit/test_fleet_capabilities.py::test_no_declaration_is_reported_as_the_normal_case
- [x] AC-21: WHEN a project expects a module version it does not have THEN the mismatch is reported, distinctly from the module being absent [REQ: a-project-reports-what-it-has-wired-in-and-dim-is-not-absent, scenario: a-version-mismatch-is-reported-as-such] — tests/unit/test_fleet_capabilities.py::test_a_project_running_a_version_it_did_not_ask_for_is_reported_as_a_mismatch; tests/unit/test_fleet_capabilities.py::test_a_module_asked_for_but_not_installed_is_unknown_never_a_match (the distinctness the AC asks for); tests/unit/test_fleet_capabilities.py::test_a_version_that_agrees_is_a_match_so_the_mismatch_above_means_something
- [x] AC-22: WHEN no install record exists and presence is inferred from files THEN the report marks that entry as inferred rather than declared [REQ: a-project-reports-what-it-has-wired-in-and-dim-is-not-absent, scenario: an-inference-says-it-is-one] — tests/unit/test_fleet_capabilities.py::test_present_without_a_ledger_is_inferred_and_never_stale
- [x] AC-23: WHEN a new capability is added to the framework THEN it can be reported without changing the surface's rendering logic [REQ: a-project-reports-what-it-has-wired-in-and-dim-is-not-absent, scenario: the-capability-set-is-data-not-a-fixed-list] — tests/unit/test_fleet_capabilities.py::test_the_capability_set_is_data_not_a_hand_kept_list

**Nothing derived from an agent's session is persisted**

- [ ] AC-24: WHEN the inventory reads the tail of a session log to derive state THEN the excerpt is served to the caller and retained nowhere [REQ: nothing-derived-from-an-agent-s-session-is-persisted, scenario: a-log-excerpt-is-read-and-dropped]
- [ ] AC-25: WHEN reading a session log fails or its content cannot be parsed THEN the diagnostic names the file and the failure kind, and quotes no line of it [REQ: nothing-derived-from-an-agent-s-session-is-persisted, scenario: a-failure-reports-shape-not-content]


### agent-fleet-state

**Activity is read from the session log, never from a heartbeat**

- [ ] AC-26: WHEN an agent's session log was written to more recently than its registry entry was updated THEN the reported time-since-movement is measured from the log [REQ: activity-is-read-from-the-session-log-never-from-a-heartbeat, scenario: last-movement-comes-from-the-log]
- [ ] AC-27: WHEN a registry entry is hours old and the session log is seconds old THEN the agent is reported as recently active [REQ: activity-is-read-from-the-session-log-never-from-a-heartbeat, scenario: a-stale-registry-does-not-make-an-agent-look-idle]

**An outstanding tool call is what "working" means**

- [ ] AC-28: WHEN the log tail holds a tool invocation with no matching result THEN the state is working, naming that tool and how long it has been outstanding [REQ: an-outstanding-tool-call-is-what-working-means, scenario: an-agent-inside-a-tool]
- [ ] AC-29: WHEN the last log entry is an assistant message and no tool call is outstanding THEN the state is waiting [REQ: an-outstanding-tool-call-is-what-working-means, scenario: an-agent-that-finished-its-turn]

**A state that cannot be determined is unknown, never idle**

- [ ] AC-30: WHEN a live process has no session log written yet THEN the agent is listed as running with an unknown state [REQ: a-state-that-cannot-be-determined-is-unknown-never-idle, scenario: a-session-with-no-log-yet]
- [ ] AC-31: WHEN the session log cannot be read THEN the state is unknown and the reason is reported [REQ: a-state-that-cannot-be-determined-is-unknown-never-idle, scenario: an-unreadable-log]
- [ ] AC-32: WHEN a source of state omits the status field entirely rather than leaving it blank THEN the state is unknown, and a test for any particular state SHALL NOT be the thing that decides it [REQ: a-state-that-cannot-be-determined-is-unknown-never-idle, scenario: a-status-field-that-is-absent-not-empty]

**Waiting for a person is a state of its own, and it says what for**

- [ ] AC-33: WHEN the runtime's record reports the session as waiting and names what for THEN the agent is reported as waiting, carrying that reason [REQ: waiting-for-a-person-is-a-state-of-its-own-and-it-says-what-for, scenario: a-waiting-agent-is-reported-as-waiting]
- [ ] AC-34: WHEN the log's tail is inconclusive and the record is absent or stale THEN the state is unknown, not waiting and not idle [REQ: waiting-for-a-person-is-a-state-of-its-own-and-it-says-what-for, scenario: a-waiting-state-is-not-derived-from-the-log-alone]
- [ ] AC-35: WHEN an agent's log has not moved for a long time and the record reports it waiting THEN it is reported as waiting for a person, not as merely still [REQ: waiting-for-a-person-is-a-state-of-its-own-and-it-says-what-for, scenario: waiting-outranks-a-quiet-log]

**What an agent is working towards is read from the engine's record, never guessed**

- [ ] AC-36: WHEN an agent runs in a project the engine drives, and the engine has recorded a run THEN the agent reports what it is working on and how far it has got, from that record [REQ: what-an-agent-is-working-towards-is-read-from-the-engine-s-record-never-guessed, scenario: an-adopted-project-reports-purpose-and-progress]
- [ ] AC-37: WHEN recorded state claims a run in progress whose process is no longer alive THEN it is reported as stale, distinguishably from a live run [REQ: what-an-agent-is-working-towards-is-read-from-the-engine-s-record-never-guessed, scenario: a-stale-run-is-not-reported-as-running]
- [ ] AC-38: WHEN a project has no engine record THEN the agent reports no purpose, and the absence is stated rather than shown as an empty label [REQ: what-an-agent-is-working-towards-is-read-from-the-engine-s-record-never-guessed, scenario: no-engine-no-invented-purpose]
- [ ] AC-39: WHEN progress is reported THEN it is derived from completed tasks, and not from a count of turns or events [REQ: what-an-agent-is-working-towards-is-read-from-the-engine-s-record-never-guessed, scenario: progress-is-completed-work-not-activity]

**A phase is reported only where the agent declared one**

- [ ] AC-40: WHEN an agent declares the phase it is in THEN the surface reports that phase [REQ: a-phase-is-reported-only-where-the-agent-declared-one, scenario: a-declared-phase-is-shown]
- [ ] AC-41: WHEN an agent declares nothing THEN no phase is reported, and none is inferred from its log [REQ: a-phase-is-reported-only-where-the-agent-declared-one, scenario: an-undeclared-phase-produces-no-icon]
- [ ] AC-42: WHEN an agent is known to have spawned another agent THEN its directing role may be reported, because that relation was measured rather than guessed [REQ: a-phase-is-reported-only-where-the-agent-declared-one, scenario: role-is-not-a-phase]

**A declared blockage is independent of the measured state**

- [ ] AC-43: WHEN an agent's measured state is busy and it has declared itself blocked THEN both are reported, and the blockage is visible [REQ: a-declared-blockage-is-independent-of-the-measured-state, scenario: blocked-while-busy]
- [ ] AC-44: WHEN an agent's measured state is waiting and it has declared no blockage THEN no blockage is reported — it may simply have finished a turn [REQ: a-declared-blockage-is-independent-of-the-measured-state, scenario: waiting-is-not-blockage]

**Watching the fleet costs a bounded number of file watchers**

- [ ] AC-45: WHEN the number of running agents doubles THEN the number of watchers the framework holds does not increase [REQ: watching-the-fleet-costs-a-bounded-number-of-file-watchers, scenario: watchers-do-not-scale-with-agents]
- [ ] AC-46: WHEN an agent's log is displayed THEN its content is read on demand rather than by arming a watcher per log [REQ: watching-the-fleet-costs-a-bounded-number-of-file-watchers, scenario: log-content-is-read-not-watched]

**Listing every agent does not read every log in full**

- [ ] AC-47: WHEN the fleet inventory derives state for every discovered agent THEN no session log is read in full [REQ: listing-every-agent-does-not-read-every-log-in-full, scenario: the-list-is-derived-from-metadata-and-tails]
- [ ] AC-48: WHEN a caller opens one agent's log THEN that log alone may be parsed in full [REQ: listing-every-agent-does-not-read-every-log-in-full, scenario: an-opened-log-is-parsed-fully]


### agent-fleet-instruct

**An instruction is delivered over the messaging bus**

- [x] AC-49: WHEN a caller sends an instruction to an agent that has a bus identity THEN the message is delivered addressed to that specific session, not broadcast to its project [REQ: an-instruction-is-delivered-over-the-messaging-bus, scenario: an-addressed-instruction-reaches-an-agent] — tests/unit/test_fleet_instruct.py::test_an_addressed_instruction_names_the_seat_on_the_wire
- [x] AC-50: WHEN the intended agent's identity cannot be resolved THEN the send is refused and the reason reported, rather than sent to everyone in its room [REQ: an-instruction-is-delivered-over-the-messaging-bus, scenario: a-broadcast-is-never-a-substitute-for-an-address] — tests/unit/test_fleet_instruct.py::test_an_unresolvable_addressee_is_refused_and_not_broadcast

**The delivery report distinguishes every outcome, and an outcome can expire**

- [x] AC-51: WHEN the bus reports that the send woke the addressed session THEN the outcome is reported as arriving now [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire, scenario: an-agent-with-a-waiter] — tests/unit/test_fleet_instruct.py::test_a_woken_seat_with_a_live_waiter_arrives_now
- [x] AC-52: WHEN the bus reports no session woken and the agent's state is working THEN the outcome is reported as arriving at the end of the current turn [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire, scenario: a-working-agent-without-a-waiter] — tests/unit/test_fleet_instruct.py::test_a_working_agent_without_a_waiter_arrives_at_the_end_of_the_turn
- [x] AC-53: WHEN the bus reports no session woken and the agent's state is not working THEN the outcome is reported as sitting unread until someone types into that session [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire, scenario: an-idle-agent-without-a-waiter] — tests/unit/test_fleet_instruct.py::test_an_idle_agent_without_a_waiter_sits_unread
- [x] AC-54: WHEN the bus returns no usable answer about what it woke THEN the outcome is reported as unknown, and not as delivered [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire, scenario: an-unknown-outcome-is-not-upgraded-to-success] — tests/unit/test_fleet_instruct.py::test_an_unknown_answer_is_never_upgraded_to_delivered
- [x] AC-55: WHEN the send call returns successfully THEN nothing is reported as delivered on that basis alone, and the outcome remains pending until the channel says what became of the message [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire, scenario: the-send-call-s-success-is-not-an-outcome] — tests/unit/test_fleet_instruct.py::test_the_send_calls_success_is_not_an_outcome
- [~] AC-56: WHEN the channel reports that it did not deliver the message and that someone at the receiving end must approve it first THEN the outcome is reported as held pending that approval, distinctly from delivered and from sitting unread [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire, scenario: a-message-held-for-the-recipient-s-human] **PARTIAL 2026-08-19 — the outcome is DISTINCT and nothing produces it.** `held` is representable (`instruct.held()`), is excluded from the terminal outcomes and reports `delivered_to_agent: False`, so it is told apart from delivered and from sitting unread. What is missing is a channel that reports it: the hold exists only on the runtime's cross-session channel, which 4.3 forbids for content, and a test asserts the durable path never reaches `HELD` — including when the answer carries a `held` field. — tests/unit/test_fleet_instruct.py::test_a_held_message_is_a_modelled_outcome_and_not_a_manufactured_one
- [x] AC-57: WHEN a held message expires without the recipient's human answering THEN the lapse is reported where the original outcome was shown, and the message is reported as not delivered [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire, scenario: a-hold-that-lapses] — tests/unit/test_fleet_instruct.py::test_a_lapse_replaces_the_earlier_outcome_rather_than_standing_beside_it
- [x] AC-58: WHEN a message is held THEN nothing reports the agent as having been instructed, because the agent has not seen it [REQ: the-delivery-report-distinguishes-every-outcome-and-an-outcome-can-expire, scenario: a-held-message-is-never-counted-as-reaching-the-agent] — tests/unit/test_fleet_instruct.py::test_a_held_message_is_never_counted_as_reaching_the_agent

**A direct channel may ring the bell, but never carry the message**

- [x] AC-59: WHEN an instruction is written to the durable channel and the reply reports that no session was woken THEN the framework may prompt that session over the direct channel to read its mailbox [REQ: a-direct-channel-may-ring-the-bell-but-never-carry-the-message, scenario: the-durable-send-woke-nobody] — tests/unit/test_fleet_instruct.py::test_the_bell_is_offered_only_where_nothing_would_start_a_turn
- [x] AC-60: WHEN the direct channel is used THEN it carries only a prompt to check the mailbox, never the instruction text [REQ: a-direct-channel-may-ring-the-bell-but-never-carry-the-message, scenario: the-content-never-travels-on-the-direct-channel] — tests/unit/test_fleet_instruct.py::test_the_bell_cannot_carry_a_message_at_all
- [x] AC-61: WHEN the direct channel cannot be used at all THEN delivery still happens through the durable path, and the reported outcome says the message is waiting rather than that it failed [REQ: a-direct-channel-may-ring-the-bell-but-never-carry-the-message, scenario: the-direct-channel-is-unavailable] — tests/unit/test_fleet_instruct.py::test_the_durable_send_happens_before_any_bell

**An orphaned waiter is shown, and removing it is an offer rather than a tidy-up**

- [~] AC-62: WHEN a project holds waiter processes whose sessions have exited THEN they are reported, alongside the agents reported as having no waiter [REQ: an-orphaned-waiter-is-shown-and-removing-it-is-an-offer-rather-than-a-tidy-up, scenario: an-orphan-is-reported-next-to-the-missing-waiter] — RÉSZLEGES: tests/unit/test_fleet_api.py::test_the_three_statuses_are_kept_apart (a magban: `GET /api/fleet/waiters`; a felület fele a 7.13)
- [x] AC-63: WHEN an orphaned waiter is to be removed THEN it happens only on an action naming that specific process, and never as a side effect of installing a waiter or of any other operation [REQ: an-orphaned-waiter-is-shown-and-removing-it-is-an-offer-rather-than-a-tidy-up, scenario: removal-is-explicit-and-named] — tests/unit/test_fleet_instruct.py::test_removal_of_a_named_orphan_signals_that_one_process + test_there_is_no_bulk_removal_entry_point
- [x] AC-64: WHEN a candidate waiter's session is alive, or cannot be determined to be dead THEN it is not offered for removal and not removed [REQ: an-orphaned-waiter-is-shown-and-removing-it-is-an-offer-rather-than-a-tidy-up, scenario: a-live-waiter-is-never-removed] — tests/unit/test_fleet_instruct.py::test_removal_refuses_a_live_session + test_removal_refuses_when_liveness_is_undeterminable
- [x] AC-65: WHEN waiter candidates are gathered THEN each is resolved to a process and checked against its session, and no candidate comes from the number of matches a pattern returned [REQ: an-orphaned-waiter-is-shown-and-removing-it-is-an-offer-rather-than-a-tidy-up, scenario: a-candidate-is-an-identity-not-a-match-count] — tests/unit/test_fleet_instruct.py::test_a_waiter_is_an_identity_and_not_a_match_count + test_a_bare_substring_check_would_not_have_caught_it

**An answer to an open decision goes to the connector, not to a session**

- [x] AC-66: WHEN an open decision is answered from the surface THEN the answer is written to the connector under the change and task it answers [REQ: an-answer-to-an-open-decision-goes-to-the-connector-not-to-a-session, scenario: an-answer-is-keyed-to-what-it-answers] — tests/unit/test_fleet_instruct.py::test_an_answer_is_keyed_to_what_it_answers
- [x] AC-67: WHEN the session that raised the decision has exited THEN the answer is still accepted and recorded [REQ: an-answer-to-an-open-decision-goes-to-the-connector-not-to-a-session, scenario: the-asking-session-no-longer-exists] — tests/unit/test_fleet_instruct.py::test_the_asking_session_is_never_consulted
- [x] AC-68: WHEN an answer is recorded THEN the outcome states that it is recorded and awaiting the next run, not that an agent has seen it [REQ: an-answer-to-an-open-decision-goes-to-the-connector-not-to-a-session, scenario: recorded-is-not-received] — tests/unit/test_fleet_instruct.py::test_recorded_is_not_received

**An agent that cannot be instructed says so where the input would be**

- [~] AC-69: WHEN an agent is discovered in a project where the messaging bus is not installed THEN it appears with its state and log, its input disabled, and the reason stated [REQ: an-agent-that-cannot-be-instructed-says-so-where-the-input-would-be, scenario: an-agent-in-a-project-without-the-bus] — RÉSZLEGES: tests/unit/test_fleet_api.py::test_the_listing_carries_why_an_agent_cannot_be_instructed (a magban; a tile-fele a 7.3 input)
- [~] AC-70: WHEN no messaging bus is present on the machine THEN discovery and state still work for every agent, and every tile reports instruction as unavailable [REQ: an-agent-that-cannot-be-instructed-says-so-where-the-input-would-be, scenario: the-bus-is-unavailable-entirely] — RÉSZLEGES: tests/unit/test_fleet_api.py::test_an_unreadable_bus_is_a_different_reason_from_an_unenrolled_agent + a listázás `bus_reachable` mezője


### agent-fleet-terminal

**A terminal exists only for a process the framework started or adopted**

- [ ] AC-71: WHEN an agent is started from the surface THEN it runs under a framework-owned pseudo-terminal, and a terminal is reported as available [REQ: a-terminal-exists-only-for-a-process-the-framework-started-or-adopted, scenario: a-surface-started-agent-has-a-terminal]
- [ ] AC-72: WHEN an agent process was started outside the framework and has not been adopted THEN no terminal is reported for it, and no write into its terminal is attempted [REQ: a-terminal-exists-only-for-a-process-the-framework-started-or-adopted, scenario: a-foreign-session-has-none]
- [ ] AC-73: WHEN a caller asks whether an agent can be typed into THEN the answer names which of the three populations it is in, rather than leaving the caller to guess from other fields [REQ: a-terminal-exists-only-for-a-process-the-framework-started-or-adopted, scenario: the-reason-is-carried-not-inferred]

**Adoption of a running session is measured before it is relied upon**

- [ ] AC-74: WHEN measurement shows a running session can be resumed into an owned terminal without losing its history THEN adoption is offered for foreign sessions, and the resulting agent reports a terminal [REQ: adoption-of-a-running-session-is-measured-before-it-is-relied-upon, scenario: adoption-works]
- [ ] AC-75: WHEN measurement shows it cannot be done, or cannot be done without losing session state THEN foreign sessions keep the bus input, their tiles state that no terminal is possible, and the finding is written into the design with what was run and what it returned [REQ: adoption-of-a-running-session-is-measured-before-it-is-relied-upon, scenario: adoption-does-not-work]
- [ ] AC-76: WHEN the measurement has not been made THEN the framework behaves as if adoption is impossible, rather than offering a control whose outcome is unknown [REQ: adoption-of-a-running-session-is-measured-before-it-is-relied-upon, scenario: the-unmeasured-state-is-not-the-optimistic-one]

**Resuming a session that is running is refused, not offered**

- [ ] AC-77: WHEN an agent is discovered with a live process bound to its session log THEN no resume or adopt control is offered for it, and the reason given is that the session is running [REQ: resuming-a-session-that-is-running-is-refused-not-offered, scenario: a-live-session-is-not-resumable-from-the-surface]
- [ ] AC-78: WHEN a session log has no live process writing it THEN resuming it is permitted [REQ: resuming-a-session-that-is-running-is-refused-not-offered, scenario: a-dead-session-may-be-resumed]
- [ ] AC-79: WHEN it cannot be determined whether a session is still running THEN it is treated as running and the resume is refused, rather than attempted on the optimistic reading [REQ: resuming-a-session-that-is-running-is-refused-not-offered, scenario: the-refusal-survives-a-stale-binding]

**Starting work goes through the engine's one entry point, not a second spawn path**

- [ ] AC-80: WHEN a work unit is started from the surface THEN the engine's command is invoked, and the run is indistinguishable from an agent-started one except in what recorded who started it [REQ: starting-work-goes-through-the-engine-s-one-entry-point-not-a-second-spawn-path, scenario: the-surface-starts-a-unit]
- [ ] AC-81: WHEN the engine's command is invoked from the surface THEN it runs under the pseudo-terminal the framework owns, so every terminal rule above applies to it unchanged [REQ: starting-work-goes-through-the-engine-s-one-entry-point-not-a-second-spawn-path, scenario: the-terminal-still-belongs-to-the-surface]
- [ ] AC-82: WHEN an interactive session is started with no work unit THEN it is presented as a bare session, and no change, group or verdict field is shown for it — rather than shown empty [REQ: starting-work-goes-through-the-engine-s-one-entry-point-not-a-second-spawn-path, scenario: a-bare-session-is-labelled-as-one]
- [ ] AC-83: WHEN the ways the surface can start an agent are enumerated THEN exactly one of them starts a work unit [REQ: starting-work-goes-through-the-engine-s-one-entry-point-not-a-second-spawn-path, scenario: no-second-start-path-exists]

**Terminal traffic travels in both directions and is never persisted**

- [ ] AC-84: WHEN the agent writes to its terminal THEN the connected browser receives that output [REQ: terminal-traffic-travels-in-both-directions-and-is-never-persisted, scenario: output-reaches-the-browser]
- [ ] AC-85: WHEN a key is typed into the connected terminal component THEN the agent process receives it as terminal input [REQ: terminal-traffic-travels-in-both-directions-and-is-never-persisted, scenario: keystrokes-reach-the-agent]
- [ ] AC-86: WHEN the stream fails or a frame cannot be decoded THEN the diagnostic names the stream and the failure kind, and quotes none of its content [REQ: terminal-traffic-travels-in-both-directions-and-is-never-persisted, scenario: a-failure-reports-shape-not-content]
- [ ] AC-87: WHEN a terminal session ends THEN no transcript of it remains on disk [REQ: terminal-traffic-travels-in-both-directions-and-is-never-persisted, scenario: nothing-is-kept]

**A started agent's lifetime is defined for the browser leaving and the service restarting**

- [x] AC-88: WHEN the browser showing an agent's terminal closes THEN the agent keeps running, and is still listed with its terminal reattachable [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting, scenario: the-browser-disconnects] — tests/unit/test_fleet_ownerd.py::test_a_viewer_detaching_leaves_the_agent_held_and_running (the agent stays held and running when a viewer goes); tests/unit/test_fleet_ownerd.py::test_a_second_viewer_attaches_to_the_same_terminal (reattachable, and by more than one reader)
- [x] AC-89: WHEN a browser reconnects to an agent the framework started THEN it attaches to the same terminal and the agent continues [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting, scenario: reattaching-after-disconnect] — tests/unit/test_fleet_ownerd.py::test_a_second_viewer_attaches_to_the_same_terminal; tests/unit/test_fleet_ownerd.py::test_the_replayed_screen_is_not_eaten_by_the_attach_response (the reattach carries the screen as it already is); web/tests/unit/fleetTerminalReplayGeometry.test.tsx::adopts the pty's shape before writing a single replayed byte (B-16 — the reattached screen is the one the pty drew)
- [x] AC-90: WHEN the service that started an agent restarts THEN the agent is still running and still instructable, and its terminal alone is reported as gone — never as attachable [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting, scenario: the-service-restarts] — tests/unit/test_fleet_owner.py::test_orphans_are_live_framework_scopes_this_owner_does_not_hold; tests/unit/test_fleet_owner.py::test_population_is_about_the_handle_not_about_history (an agent a PREVIOUS owner started is foreign to this one — the handle is the property, not history); tests/unit/test_fleet_api.py::test_an_agent_the_framework_started_is_orphaned_not_foreign (the surface half: still listed, terminal gone)
- [x] AC-91: WHEN any service in the framework restarts, including the one that owns agent lifetime THEN an agent started from the surface keeps running, because it was placed outside that service's lifetime rather than inside it [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting, scenario: an-agent-outlives-the-service-that-started-it] — tests/unit/test_fleet_owner.py::test_a_scope_inside_the_service_cgroup_is_refused_not_warned (the structural guarantee: a scope that came out inside the service's cgroup is REFUSED, so an agent cannot inherit the service's lifetime); tests/unit/test_fleet_owner.py::test_a_sibling_scope_is_accepted (the other direction); tests/unit/test_fleet_owner.py::test_a_prefix_of_the_service_path_is_not_a_parent
- [x] AC-92: WHEN any path would try to take over a terminal by reopening another process's descriptor THEN it is not attempted, and the terminal is reported gone while the agent stays listed and instructable [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting, scenario: a-terminal-handle-is-never-reacquired-from-outside] — tests/unit/test_fleet_owner.py::test_population_is_about_the_handle_not_about_history; tests/unit/test_fleet_owner.py::test_orphans_are_live_framework_scopes_this_owner_does_not_hold; tests/unit/test_fleet_api.py::test_an_agent_the_framework_started_is_orphaned_not_foreign — nothing reacquires a descriptor: an agent whose owner is gone is reported orphaned and stays listed
- [x] AC-93: WHEN an agent started here is to be stopped THEN it stops on an explicit action, and never as a consequence of a view being closed [REQ: a-started-agent-s-lifetime-is-defined-for-the-browser-leaving-and-the-service-restarting, scenario: stopping-is-deliberate] — tests/unit/test_fleet_ownerd.py::test_a_viewer_detaching_leaves_the_agent_held_and_running (the server half); web/tests/unit/fleetTerminalReplayGeometry.test.tsx::closes without asking the server to stop anything (the SCREEN half, which the server test is silent about — mutation-proven: making close call stop fails it); web/tests/unit/fleetTerminalReplayGeometry.test.tsx::does not stop the agent on the first click; ::offers a stop and a close as two separate controls

**The terminal is proven by driving it as a person drives it**

- [ ] AC-94: WHEN a key is entered into the terminal component in a browser THEN the agent process receives it, and its response appears in that same component [REQ: the-terminal-is-proven-by-driving-it-as-a-person-drives-it, scenario: a-keystroke-makes-the-round-trip]
- [ ] AC-95: WHEN the tests run against an agent the framework did not start THEN no terminal is offered for it, and this is asserted rather than assumed [REQ: the-terminal-is-proven-by-driving-it-as-a-person-drives-it, scenario: the-negative-case-is-asserted-too]


### agent-fleet-surface

**Projects on the left, the selected project's agents on the right**

- [ ] AC-96: WHEN a project tile is selected THEN that project's agents appear as tiles, one per agent [REQ: projects-on-the-left-the-selected-project-s-agents-on-the-right, scenario: selecting-a-project-shows-its-agents]
- [ ] AC-97: WHEN a selected project holds no live agent THEN the panel says so, and the project remains in the list [REQ: projects-on-the-left-the-selected-project-s-agents-on-the-right, scenario: a-project-with-no-running-agent]

**A project tile carries the state of the agents inside it**

- [ ] AC-98: WHEN any agent in an unselected project is waiting THEN its project's tile shows that, without the project being selected [REQ: a-project-tile-carries-the-state-of-the-agents-inside-it, scenario: a-waiting-agent-is-visible-from-the-project-list]
- [ ] AC-99: WHEN the agent area is compacted for density THEN the count of waiting agents remains readable [REQ: a-project-tile-carries-the-state-of-the-agents-inside-it, scenario: the-counts-stay-visible-when-the-grid-is-compacted]

**A project awaiting a human is surfaced even when no agent is running**

- [ ] AC-100: WHEN a project has work awaiting a human answer and no agent process running THEN its tile reports it as awaiting an answer, not as holding nothing [REQ: a-project-awaiting-a-human-is-surfaced-even-when-no-agent-is-running, scenario: a-stopped-project-with-no-agents-is-not-empty]
- [ ] AC-101: WHEN a project tile shows a waiting count THEN that count includes work awaiting an answer with no agent attached to it [REQ: a-project-awaiting-a-human-is-surfaced-even-when-no-agent-is-running, scenario: the-count-is-of-what-is-waiting-not-of-who-is-present]
- [ ] AC-102: WHEN everything that produced the question has exited and restarted THEN the project is still surfaced as awaiting an answer [REQ: a-project-awaiting-a-human-is-surfaced-even-when-no-agent-is-running, scenario: the-marker-outlives-every-process]

**An agent tile carries state, log excerpt and its own input**

- [ ] AC-103: WHEN an agent is inside a tool THEN the tile names the tool and how long it has been running [REQ: an-agent-tile-carries-state-log-excerpt-and-its-own-input, scenario: a-tile-shows-what-the-agent-is-doing]
- [ ] AC-104: WHEN an agent has ended its turn THEN the tile shows the last lines of its log, so the reason for waiting is readable [REQ: an-agent-tile-carries-state-log-excerpt-and-its-own-input, scenario: a-tile-shows-why-an-agent-is-waiting]
- [ ] AC-105: WHEN the number of agents forces a denser layout THEN each tile still shows its state and its input, with other content shortened instead [REQ: an-agent-tile-carries-state-log-excerpt-and-its-own-input, scenario: density-does-not-remove-state-or-input]
- [ ] AC-106: WHEN an agent's session log was bound heuristically THEN the tile marks the log as unconfirmed [REQ: an-agent-tile-carries-state-log-excerpt-and-its-own-input, scenario: a-tile-whose-binding-is-a-guess-says-so]

**A tile can be enlarged, and the other agents stay visible as rows**

- [x] AC-107: WHEN an agent tile is enlarged THEN it shows a larger log area and the other agents appear as tabs in one strip [REQ: a-tile-can-be-enlarged-and-the-other-agents-stay-visible-as-a-tab-strip, scenario: enlarging-one-tile] — web/tests/unit/fleetSurface.test.tsx::leaves every other agent in a tab that carries its state; web/tests/unit/fleetWorkspace.test.tsx::keeps enlarging as its own act, which puts the others in the tab strip
- [x] AC-108: WHEN a tile is enlarged while another agent is waiting THEN that agent's tab shows its waiting state [REQ: a-tile-can-be-enlarged-and-the-other-agents-stay-visible-as-a-tab-strip, scenario: the-other-agents-are-still-readable] — web/tests/unit/fleetSurface.test.tsx::leaves every other agent in a tab that carries its state (asserts the undetermined agent is readable from the strip); the state's colour comes from `TAB_DOT`, keyed by state string with an amber default so an unknown state is loud
- [x] AC-109: WHEN a tab is selected THEN that agent becomes the enlarged tile [REQ: a-tile-can-be-enlarged-and-the-other-agents-stay-visible-as-a-tab-strip, scenario: a-tab-is-the-way-back] — web/tests/unit/fleetSurface.test.tsx::selects back: clicking a tab enlarges that agent instead

**View state is remembered per project**

- [x] AC-110: WHEN a project is selected, a tile enlarged, another project visited, and the first selected again THEN the same tile is enlarged [REQ: view-state-is-remembered-per-project, scenario: returning-to-a-project-restores-its-view] — web/tests/unit/fleetViewMemory.test.ts::gives each project back its own view; ::restores the enlarged tile after visiting another project; ::does not keep one shared view for every project (holds the WRONG shape — a single shared key would satisfy every other test here and break the requirement)
- [x] AC-111: WHEN the remembered enlarged agent is no longer running THEN the grid is shown, and no empty enlarged tile [REQ: view-state-is-remembered-per-project, scenario: a-remembered-agent-that-is-gone] — web/tests/unit/fleetWorkspaceState.test.ts::goes back to the grid when the remembered agent is gone — never substitutes another; web/tests/unit/fleetViewMemory.test.ts::discards a remembered agent that is gone, and falls back rather than blanking (mutation-proven)
- [x] AC-112: WHEN text is typed into an agent's input and the reader moves around the screen THEN nothing of what was typed reaches `localStorage`, and the loss of an unsent draft on a project switch is accepted as the price [REQ: view-state-is-remembered-per-project, scenario: an-unsent-draft-is-not-written-to-the-browser-s-store] — ⚠ THE CRITERION WAS REVERSED, not satisfied: it used to require that the draft SURVIVE. A typed instruction can carry a consumer's own words, and the standing rule forbids such content reaching `localStorage`; the requirement and its scenario now say so. Evidence: web/tests/unit/fleetInstructSurface.test.tsx::puts nothing an agent declared into localStorage
- [x] AC-113: WHEN a project with exactly one agent is opened for the first time THEN that agent's tile is enlarged, because a grid of one leaves the rest of the area empty [REQ: view-state-is-remembered-per-project, scenario: a-project-holding-one-agent-opens-enlarged] — web/tests/unit/fleetViewMemory.test.ts::enlarges the only agent when no choice has been made; ::enlarges nothing when there is more than one; ::reports no choice for a project never visited (mutation-proven: removing the single-agent default fails 2)
- [x] AC-114: WHEN the single tile is collapsed and the project is visited again THEN it stays collapsed — a default may choose the first view, never override a chosen one [REQ: view-state-is-remembered-per-project, scenario: a-remembered-choice-outranks-the-default] — web/tests/unit/fleetViewMemory.test.ts::stays collapsed when the single tile was deliberately closed (mutation-proven: dropping the explicit-null branch fails it); ::keeps a remembered tile that is still there

**Dictation writes into the same input as typing**

- [ ] AC-115: WHEN dictation is used on an agent tile THEN the transcript appears in that agent's input, editable before sending [REQ: dictation-writes-into-the-same-input-as-typing, scenario: dictated-text-lands-in-the-input]
- [ ] AC-116: WHEN voice input is not configured THEN typing is unaffected and the dictation control is absent rather than failing on use [REQ: dictation-writes-into-the-same-input-as-typing, scenario: dictation-unavailable]

**The delivery outcome is shown where the message was sent**

- [ ] AC-117: WHEN an instruction is sent to an agent THEN the tile distinguishes arriving now, arriving at the end of the turn, and sitting unread [REQ: the-delivery-outcome-is-shown-where-the-message-was-sent, scenario: each-outcome-reads-differently]
- [ ] AC-118: WHEN the outcome is that the message sits unread because no waiter is running THEN the tile says so, and offers the action that would make that agent wakeable [REQ: the-delivery-outcome-is-shown-where-the-message-was-sent, scenario: an-agent-that-will-not-wake-offers-the-remedy]

**The fleet is the landing screen, and an unfinished answer is not an empty one**

- [ ] AC-119: WHEN the application is opened at its root route THEN the fleet screen is shown [REQ: the-fleet-is-the-landing-screen-and-an-unfinished-answer-is-not-an-empty-one, scenario: the-root-route-renders-the-fleet]
- [ ] AC-120: WHEN the screen paints before discovery has returned THEN it states that it is still looking, and shows neither an empty fleet nor a count of zero [REQ: the-fleet-is-the-landing-screen-and-an-unfinished-answer-is-not-an-empty-one, scenario: discovery-has-not-answered-yet]
- [ ] AC-121: WHEN discovery has completed and found no live agent THEN the screen says that no agent is running, distinctly from the state above [REQ: the-fleet-is-the-landing-screen-and-an-unfinished-answer-is-not-an-empty-one, scenario: discovery-answered-and-there-genuinely-is-nothing]
- [ ] AC-122: WHEN a reader wants the projects overview THEN it is reachable from the navigation, with every behaviour it had before [REQ: the-fleet-is-the-landing-screen-and-an-unfinished-answer-is-not-an-empty-one, scenario: the-projects-overview-is-not-lost]

**An install offered from the screen goes through the module installer, and shows what it did not do**

- [ ] AC-123: WHEN the screen offers to wire a capability into a project THEN it invokes the module installer for the module that provides it, and no capability-specific install path exists on the surface [REQ: an-install-offered-from-the-screen-goes-through-the-module-installer-and-shows-what-it-did-not-do, scenario: the-install-is-the-installer-s-not-the-screen-s]
- [ ] AC-124: WHEN an install leaves files alone because the project modified them THEN each skipped file and its reason appear on the screen, not only in the installer's output [REQ: an-install-offered-from-the-screen-goes-through-the-module-installer-and-shows-what-it-did-not-do, scenario: skips-are-shown-not-swallowed]
- [ ] AC-125: WHEN an install writes no files THEN the screen states that outcome, rather than reporting a plain success [REQ: an-install-offered-from-the-screen-goes-through-the-module-installer-and-shows-what-it-did-not-do, scenario: a-run-that-changed-nothing-says-so]
- [ ] AC-126: WHEN a module requires another that the project does not have THEN the install is refused and the missing requirement is named, and no control offers to proceed regardless [REQ: an-install-offered-from-the-screen-goes-through-the-module-installer-and-shows-what-it-did-not-do, scenario: a-missing-requirement-is-a-refusal-not-a-warning]
- [ ] AC-127: WHEN the screen presents what can be installed into a project THEN a module's machine-wide executable part is not among it [REQ: an-install-offered-from-the-screen-goes-through-the-module-installer-and-shows-what-it-did-not-do, scenario: the-executable-part-is-never-offered-into-a-project]

**A tile offers a terminal only where one can exist, and says why when it cannot**

- [ ] AC-128: WHEN an agent was started from this screen THEN its tile offers a terminal that types into that agent [REQ: a-tile-offers-a-terminal-only-where-one-can-exist-and-says-why-when-it-cannot, scenario: a-surface-started-agent-offers-its-terminal]
- [ ] AC-129: WHEN an agent was started outside the framework and cannot be adopted THEN its tile offers no terminal, states the reason, and keeps its bus input [REQ: a-tile-offers-a-terminal-only-where-one-can-exist-and-says-why-when-it-cannot, scenario: a-foreign-session-offers-no-terminal]
