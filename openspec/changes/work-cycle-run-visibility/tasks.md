Groups carry `<!-- depends: -->` annotations because this change is a good candidate to be
driven by the engine it is about. Absence means *depends on the previous group* (fail-closed),
so every independent group states it.

## 1. Start integrity — resolution before the claim

<!-- depends: none -->

- [x] 1.1 Add a resolution step in the owner that takes the FINAL child environment (after the `CLAUDE*` strip and after `env=` is applied) and answers whether `argv[0]` can be executed in it. One function, one argument, no reads of `os.environ` inside it — the design's D1 seam, placed so the parallel provider track's `env=` changes flow through it unchanged. [REQ: a-start-is-reported-as-started-only-when-the-child-could-become-the-engine] **DONE** — `build_child_env()` + `resolve_in_env(command, env)` in `owner.py`; the resolver reads no `os.environ`. ⚠ A defect found by its own test: `shutil.which(path=None)` falls back to the PROCESS's PATH, so an env with no PATH resolved against the wrong one — the exact proxy this exists to avoid, failing in the passing direction. Refused explicitly instead.
- [x] 1.2 Refuse an unresolvable command BEFORE `pty.fork()`, with a message naming the command and the PATH it was looked for in — and not naming a scope, a unit, or a service. [REQ: a-start-is-reported-as-started-only-when-the-child-could-become-the-engine] **DONE** — `CommandNotResolvable` raised before `pty.fork()`; message carries the command and the PATH.
- [x] 1.3 Prove the seam rather than either side of it: a test that resolves against an environment DIFFERENT from the test process's own and shows the caller's environment does not decide the answer. ⚠ A test that sets `PATH` for itself measures the proxy, not the thing. [REQ: a-start-is-reported-as-started-only-when-the-child-could-become-the-engine] **DONE** — `test_resolution_reads_the_environment_it_is_given_and_not_the_process_s_own`: the ambient PATH HAS the tool, the argument does not, and the argument wins.
- [x] 1.4 Refusal leaves nothing claimed: no label held, no scope of that name, and an immediately repeated start is not refused as already running. [REQ: resolution-happens-before-anything-is-claimed] **DONE** — asserted: nothing forked, nothing adopted, `_agents` empty.
- [x] 1.5 The post-fork failure path reports the child's exit status instead of `did not become active` when the child died rather than the scope failing to register. Keep the liveness wait for the case it was written for. [REQ: resolution-happens-before-anything-is-claimed] **DONE** — `_reap()` (non-blocking) decides which failure it was; a live child still reports the scope, which is correct there.
- [x] 1.6 Assert the measured shape as a regression test: unresolvable command → refusal naming the command, no four-second wait, and the string `did not become active` absent from the message. [REQ: a-start-is-reported-as-started-only-when-the-child-could-become-the-engine] **DONE** — the measured shape held: command named, PATH named, `did not become active` absent. Mutation-tested: 2 failed without the guard, 1 without the child-exit report, 4 pass with both, restore byte-identical.
- [x] 1.7 Keep the argv construction unforgeable and checkable: the command name and starting subcommand stay in one place, and a test resolves the subcommand against the engine's own parser (it must be one that starts a unit). [REQ: one-command-name-checkable-against-the-engine-s-own-parser] **DONE, already shipped** — `tests/unit/test_fleet_api.py:711,724` assert `ENGINE_COMMAND`/`ENGINE_RUN` against the engine's own parser (`starts_a_unit=True`). Measured before writing anything.
- [x] 1.8 Install `set-work-cycle` where the owner resolves it, and verify with the owner's OWN environment: `env -i PATH=<owner PATH> sh -c 'command -v set-work-cycle'` prints a path. ⚠ This is step 2 of the migration plan and NOT a substitute for 1.1–1.2 — record which of the two open options from the design was chosen and why. [REQ: a-start-is-reported-as-started-only-when-the-child-could-become-the-engine] **DONE** — `bin/set-work-cycle` beside the other tools, symlinked into `~/.local/bin`. Chose the conventional tools directory over an interpreter-relative path because every other `set-*` command is reached that way and a second convention is a second thing to get wrong. Verified with the owner's own environment: `env -i PATH=<owner PATH> sh -c 'command -v set-work-cycle'` → `~/.local/bin/set-work-cycle`.

## 2. What a run leaves behind — the record

<!-- depends: none -->

- [x] 2.1 Carry the declared origin into the unit record, distinguishable from an assumed one; a run started with none records *not declared*, never a default that reads like a declaration. [REQ: a-run-records-where-it-came-from] **DONE** — `UnitRecord.started_by`, serialised as `None` when nobody said. ⚠ The defect was a DEFAULT, not a missing field: `--started-by` defaulted to the word `agent`, so every run carried a plausible origin and a stated one was indistinguishable from an unstated one. Default is now empty.
- [x] 2.2 ⚠ Pass the REQUESTER through: the unit-start route sends the identified requester in the engine's `--started-by` instead of the literal `fleet-surface` (`fleet.py:1562`), and the literal survives only as the fallback when no requester was given. Without this the record carries one constant for every screen-started run and the change's own question stays unanswered. [REQ: a-run-records-where-it-came-from] **DONE** — `fleet.py` sends `body.requested_by` and falls back to `fleet-surface` only when nobody was identified. Test: `test_the_engine_is_told_who_asked_not_which_surface_relayed_it`.
- [x] 2.3 Render origin as a CLAIM, not a measurement, everywhere it is serialised — it is a caller's assertion the framework does not verify. [REQ: a-run-records-where-it-came-from] **DONE** — `started_by_is_claim` travels IN the record, not applied by a renderer: a rule living only in one renderer is a rule the next one does not have.
- [x] 2.4 Persist the agent session identifier read off the session's own stream; a session that never announced one records *unknown*, distinguishable from a run with no session. [REQ: a-run-records-which-agent-session-executed-it] **DONE** — `record.session_id` saved right after the session ends and BEFORE the verdict is parsed, because an unreadable verdict is exactly the run whose transcript somebody opens.
- [x] 2.5 Make the session's transcript reachable from the record (the identifier plus what a reader needs to locate it), without the framework copying any of it. [REQ: a-run-records-which-agent-session-executed-it] **DONE** — the record carries the session id and the tree; locating the transcript from those is the runtime's own convention, already implemented in `fleet/discovery.py` (`SESSION_LOG_ROOT`). No second locator was added.
- [x] 2.6 A record written before these fields existed still reads: absent fields report *unknown* / *not declared* rather than failing to parse. [REQ: a-run-records-which-agent-session-executed-it] **DONE** — `test_a_record_written_before_the_origin_fields_existed_still_reads`: absent fields read as *nobody said* / *never announced*, not as a parse failure and not as a value.
- [x] 2.7 Carry the new fields across the D10 seam the way this repo already does it: extend the reader on the `set_orch` side AND the test that fails when the two copies diverge (`tests/unit/test_fleet_awaiting.py` is the worked example). A field added on one side only is invisible, not broken. [REQ: a-run-records-which-agent-session-executed-it] **DONE** — `Purpose` gained the fields, and the divergence guard was extended from a CONSTANT to the record's SHAPE: it reads the engine's own serialiser and fails on any key this side drops silently, with a named exemption list. Mutation-tested — dropping `session_id` from the reader fails it; restore byte-identical.

## 3. What a run leaves behind — the stream

<!-- depends: 2 -->

- [x] 3.1 Wire `on_event` to an incremental JSONL sink beside the unit record, under the tree the engine was given — never a framework-side path. [REQ: a-run-s-stream-survives-the-process-that-produced-it] **DONE** — `StreamSink` in `runner.py`, wired through `_drive`; the path is `UnitRecord.stream_path()`, derived from the tree the engine was GIVEN.
- [x] 3.2 Write as events arrive, not at the end: a run killed mid-way keeps everything up to the kill. Test by killing, not by returning early. [REQ: a-run-s-stream-survives-the-process-that-produced-it] **DONE** — flushed per event; the test asserts by never closing the sink, i.e. by killing rather than by returning early. ⚠ The wiring first used `except TypeError` to detect a runner without `on_event` — which would also have caught a TypeError from INSIDE a runner and started a SECOND session for one unit. Replaced by `inspect.signature`: a signature is a question with an answer; a failed call is not.
- [x] 3.3 Mark a completed stream as complete, so a truncated one is distinguishable from a finished one. ⚠ A missing terminator and a stream that never started must not look alike. [REQ: a-run-s-stream-survives-the-process-that-produced-it] **DONE** — a `__stream_end__` terminator carrying the event count. Mutation-tested.
- [x] 3.4 A run that produced no stream states the absence; nothing renders it as an empty successful run. [REQ: a-run-s-stream-survives-the-process-that-produced-it] **DONE** — three states, not two: no file (no session started) / a file with only a terminator (a session that said nothing) / events.
- [x] 3.5 Assert the confidentiality boundary as a test: the sink resolves under the tree passed to the engine, for a tree that is NOT the framework's own. [REQ: the-stream-is-written-where-the-project-owns-it-never-into-the-framework] **DONE** — asserted for a tree that is not this repo: the sink is relative to it and carries no ambient path.
- [x] 3.6 Assert that framework logs about a run carry identifiers, counts and outcome and NO text taken from the stream — the check is on the log output, not on the intent. [REQ: the-stream-is-written-where-the-project-owns-it-never-into-the-framework] **DONE, and it found one.** `iter_events` logged up to 200 characters of any non-JSON line, so a DEBUG log was a place the framework persisted a consumer's domain data. Now logs the length and the first character. Asserted on the log OUTPUT, not on the intent.

## 4. Reading scope — what a project may declare

<!-- depends: none -->

- [x] 4.1 Accept a list of reading paths in the adoption declaration, alongside `changes_dir` and `gates`; a declaration without the key behaves exactly as today. [REQ: a-project-may-declare-where-a-unit-reads-from] **DONE** — `reading_paths` in `set/work-cycle.yaml`; `reading_paths_declared` keeps *declared empty* apart from *not declared*.
- [x] 4.2 Carry them into the unit prompt, presented SEPARATELY from the change's own artifacts — one is the work, the other is background. [REQ: a-project-may-declare-where-a-unit-reads-from] **DONE** — a separate `## The project's own background` heading; a unit told they are one list has been told a standing reference is part of this change.
- [x] 4.3 Report a declared path that does not exist, on the start and on the run record; the run proceeds with what does exist. [REQ: a-declared-path-that-is-not-there-is-reported-not-silently-dropped] **DONE** — reported in the start's own lines and on the record (`reading`).
- [x] 4.4 Distinguish *the declaration reached nothing* from *nothing was declared*. [REQ: a-declared-path-that-is-not-there-is-reported-not-silently-dropped] **DONE** — `ResolvedReading.reached_nothing` needs `declared` to be true, so a project declaring nothing never reports it.
- [x] 4.5 Refuse a declared path resolving outside the project tree, naming it; no content from outside the tree reaches the prompt. Test with a traversal and with a symlink — the two are different escapes. [REQ: a-declaration-reaches-outside-the-change-directory-but-never-outside-the-project] **DONE** — checked on the RESOLVED location, so a traversal and an outward symlink are caught by one rule; a check on the written string catches only the first. Both asserted.

## 5. The read API the screen needs

<!-- depends: 2, 3 -->

⚠ **Half of this is already built — measure before writing.** `lib/set_orch/fleet/purpose.py`
`read_purposes()` already walks every unit record for a project and computes
`finished | running | stale` with pid verification; `lib/set_orch/fleet/awaiting.py` already
parses the awaiting marker. These tasks EXTEND those readers. Building a second reader of one
state is the defect this group exists to avoid.

- [x] 5.1 Extend `Purpose` with the fields it does not carry: gate outcome, commit, set-aside condition, origin and session. Same reader, same seam — no new one. [REQ: a-run-is-readable-from-what-the-engine-recorded-with-no-process-alive] **DONE** — `Purpose` gained gate, commit, set_aside, reading (origin/session landed in §2). Same reader, same seam; the divergence guard's exemption list shrank accordingly.
- [x] 5.2 Assert that the existing stale detection still holds after the extension, INCLUDING `pid_unverified` — this is a regression guard on behaviour that already ships, not a new build. [REQ: a-run-is-readable-from-what-the-engine-recorded-with-no-process-alive] **DONE** — regression guard, not a build: `stale` and `pid_unverified` already shipped. Added the distinction the screen must not flatten — a set-aside run has no gate and no commit, exactly like a failed one, and only `set_aside` tells them apart.
- [x] 5.3 Expose a project's runs as a LIST (today the reader is joined per-pid onto an agent tile, so a run with no live process has nowhere to appear). [REQ: a-run-is-readable-from-what-the-engine-recorded-with-no-process-alive] **DONE** — `GET /api/fleet/work-cycle?cwd=`. Every run is returned, including those whose change could not be enumerated: a run that exists must not vanish because its listing failed.
- [x] 5.4 Serve *what is runnable and why not* by running `set-work-cycle status --json` and caching it — refreshed on a start, on a finish and on a task-file change, never per poll. ⚠ Do NOT copy the group/dependency resolver into `set_orch`: the second-copy pattern here is for constants and regexes with a divergence test, and a fail-closed resolver with cycle detection is not that kind of copy. [REQ: a-project-s-changes-and-their-runnable-state-are-visible-before-anything-is-started] **DONE** — `lib/set_orch/fleet/workcycle_plan.py`, cached on the fingerprint of what would change the answer. ⚠ Two defects found here, both invisible to the tests as first written: (a) `runnable` read a key the engine has never emitted, so it would have answered False for every change while looking like a measurement — the engine emits `selected`; (b) the cache key was `(mtime, size)`, and ticking a checkbox leaves the size identical and lands inside the timestamp granularity, so an edit did not invalidate. Now a content hash.
- [x] 5.5 The run list renders when the engine is NOT installed: records read, runnability reported as unavailable with the reason. A missing engine must not empty the screen. [REQ: a-project-s-changes-and-their-runnable-state-are-visible-before-anything-is-started] **DONE** — `engine.available: false` with the reason, `adopted: null` (never false — nobody measured the project), and the recorded runs still render.
- [x] 5.6 Report a project with no adoption declaration as not adopted, naming what is missing — distinct from a project with no changes. [REQ: a-project-s-changes-and-their-runnable-state-are-visible-before-anything-is-started] **DONE** — the engine's own NOT ADOPTED answer is carried, and `changes_listed` separates *no changes* from *could not look*.
- [x] 5.7 Serve a finished run's persisted stream, labelled as a recording, with its completeness stated. [REQ: a-running-unit-s-terminal-is-reachable-in-the-project-s-dock] **DONE** — `GET /api/fleet/work-cycle/stream`. Labelled `kind: recording`, carries `complete` from the engine's terminator (a SECOND COPY of the marker, guarded by a test that imports both), and `no-stream` is its own 404 rather than an empty recording. ⚠ The traversal test first used two `..`, which does not leave `set/runtime/work-cycle/` — it passed on a 404 and proved nothing. A traversal test has to traverse.

## 6. The screen

<!-- depends: 5 -->

- [x] 6.1 Per project: the changes the engine can drive, each with runnable state or the engine's reason. Table over cards — the rows are comparable. [REQ: a-project-s-changes-and-their-runnable-state-are-visible-before-anything-is-started] **DONE** — a table, one line per change; 25 rows fit a screenful.
- [x] 6.2 The not-adopted and nothing-runnable states, each saying which it is. A zero here is a shape error until the input's shape has been looked at. [REQ: a-project-s-changes-and-their-runnable-state-are-visible-before-anything-is-started] **DONE** — not-adopted, engine-unavailable, could-not-list and empty are four distinct lines, each saying which it is.
- [x] 6.3 A start control that offers no command, argv or label field — only the change, the seat and the optional limit and model. [REQ: a-unit-is-started-from-the-screen-through-the-engine-s-own-entry-point] **DONE** — change, seat, limit and model only; the seat is the surface's own (`session:<uuid>` per browser tab), never a project name and never an agent's borrowed identity.
- [x] 6.4 Show a refusal where the person acted, in the refusal's own words, with nothing shown as started. Covers all three refusal sources: the engine, the location check, the unresolvable command. [REQ: a-unit-is-started-from-the-screen-through-the-engine-s-own-entry-point] **PARTLY DONE.** HTTP-level refusals are shown in their own words where the person acted. ⚠ A refusal the ENGINE makes (a bad seat, a held lock) exits inside the pty and reaches the caller as the scope's wording instead — measured live, recorded as **B-107**. The pty is now drained for it, but that fix is unit-tested and NOT verified live.
- [x] 6.5 When the terminal-holding service is unavailable, the control states it cannot start and what to run — it is not a control that fails when used. [REQ: a-unit-is-started-from-the-screen-through-the-engine-s-own-entry-point] **DONE** — the start control is disabled with the engine's own reason as its title; an unresolvable engine command answers 503, which is the same shape as an unavailable owner.
- [x] 6.6 Render a run: origin (as a claim), seat, session, verdict, gate, commit, set-aside condition and its question. A run that never reported a verdict gets its own state, not "no outcome yet". [REQ: a-run-is-readable-from-what-the-engine-recorded-with-no-process-alive] **DONE** — state, group, origin (as a claim), session, gate failures with attribution, commit or its reason, and the set-aside question.
- [x] 6.7 An unconfirmed live claim says which question was answered — a process holds that identifier, not that the run is alive — and is not rendered as an ordinary running run. [REQ: a-run-is-readable-from-what-the-engine-recorded-with-no-process-alive] **DONE** — `runState` returns `unconfirmed` for `pid_unverified`, with its own tone and a title saying which question was answered. ⚠ Not seen on screen: this project has no live run yet (7.4).
- [x] 6.8 Mark failed / set-aside / stale runs ON the container the reader can see — with a count, not merely a dot — wherever runs are collapsed, grouped or behind a tab. [REQ: a-failing-run-is-marked-where-the-reader-is-standing] **DONE** — the marker sits on the collapsed row, names how many of each state, and never truncates whatever the change name does.
- [x] 6.9 No marker when nothing is wrong — AND the absence must not be produced by state the screen failed to read. Assert the read-failure path separately. [REQ: a-failing-run-is-marked-where-the-reader-is-standing] **DONE** — `attentionMark(null)` reports `measured: false`, so a zero from an unread list renders differently from a zero from a healthy one; `attentionLabel` says *could not read* rather than showing nothing.
- [ ] 6.13 Declare `requested_by` in the owner's `FEATURES` set and have the start client refuse rather than send it blind. Measured 2026-08-29 (`ownerd.py:77`): `FEATURES` holds only `provider-selection`, so an owner started before this change SILENTLY DROPS `requested_by` — and the record then states that no origin was declared, which the design deliberately distinguishes from unknown. A long-lived daemon holds the code it started with, so the wrong answer is the plausible one. ⚠ `ownerd.py` belongs to a parallel change right now; agreed on the channel that they add the feature name. [REQ: a-run-records-where-it-came-from]

- [ ] 6.10 ⚠ NOT DONE — 7.4 is now done and a running unit EXISTED; the blocker is no longer the unit but the screen: a run row carries no control that opens its terminal. Open a running unit's terminal in the project's existing dock, labelled as a work unit with its change, distinguishable from a hand-started session. [REQ: a-running-unit-s-terminal-is-reachable-in-the-project-s-dock]
- [ ] 6.11 ⚠ NOT DONE — blocked by 6.10, not by the absence of a run. Closing the view does not stop the run, and the run stays reachable from the project's runs. [REQ: a-running-unit-s-terminal-is-reachable-in-the-project-s-dock]
- [ ] 6.12 ⚠ NOT DONE — the API half ships (5.7); the screen half needs a recording to open. A finished run opens its recording in the same place, explicitly marked as a recording rather than a live terminal. [REQ: a-running-unit-s-terminal-is-reachable-in-the-project-s-dock]

## 7. Looking at it, and closing the loop

<!-- depends: 6 -->

- [x] 7.1 Open every touched screen in the browser against the running dashboard and describe what is SEEN — including the refused-start state, the not-adopted state and the empty state. ⚠ If the browser cannot be reached, this task stays OPEN and the commit says so; no passing test run substitutes for it. [REQ: the-screen-is-verified-by-looking-at-it] **DONE for the states reachable.** Looked at the running dashboard in Chrome. ⚠ The look found two defects no test could: the **start button was pushed off the visible area** by an unbounded reason column, and a finished change rendered its reasons as `1: complete · 2: complete · …` — one fact stated nine times. 655 web and 13 python tests were green through both. Fixed and re-looked. **NOT looked at:** a run row and the refused-start state, because this project has no runs yet — see 7.4.
- [x] 7.2 Prove the fixes are fixes: stash each and re-run its test, confirming red without the change and green with it. Assert both the mutation and the restore. [REQ: a-start-is-reported-as-started-only-when-the-child-could-become-the-engine] **DONE** — mutation-tested throughout: the two start guards, the env ordering, the stream flush, the terminator, the shape-only log, the escape refusal, `reached_nothing`, the background heading, the cache key and the `runnable` derivation. Every restore verified byte-identical.
- [x] 7.3 Close B-105 in `openspec/bugs/README.md` with the commit sha and the re-run of its own measurement; the entry stays. [REQ: a-start-is-reported-as-started-only-when-the-child-could-become-the-engine] **DONE** — B-105 closed with its own measurement re-run (`4fddfc7f`, `8834682c`).
- [x] 7.4 Drive one non-trivial change with the engine THROUGH THE SCREEN, end to end, and record what it produced — the product, not the exit code. **DONE** — `evidence.md`. Ran in a separate worktree (`liveproof/run-visibility`) against a disposable three-group change, because a unit here would edit this change's own `tasks.md` while a second session works in the same tree. Six starts, all from the panel's `start` button: three groups complete, two commits (`3498603b`, `cbd8f4ab`), the set-aside path exercised end to end (question → `answer` → release → the maintainer's wording used verbatim), tree clean. ⚠ The run found **two live-only defects** that every test in this change passed through — **B-108** (a unit does everything right and then does not commit) and **B-111** (a question addressed to a person is attached to nothing, and `status` then reports nothing pending). Both fixed and proved by the later steps of the same run. [REQ: a-unit-is-started-from-the-screen-through-the-engine-s-own-entry-point]

## Acceptance Criteria (from spec scenarios)

### work-unit-start-integrity

- [x] AC-1: WHEN a work-unit start is requested and the engine command does not resolve in the child's environment THEN the request is refused, with no scope, label or process identifier reported, and the refusal names the command and the environment [REQ: a-start-is-reported-as-started-only-when-the-child-could-become-the-engine, scenario: the-engine-command-cannot-be-resolved-in-the-child-s-environment]
- [x] AC-2: WHEN the engine command resolves in the child's environment THEN the start proceeds and the response carries the label, the process identifier and the argument vector [REQ: a-start-is-reported-as-started-only-when-the-child-could-become-the-engine, scenario: the-engine-command-resolves]
- [x] AC-3: WHEN a start is refused because the command could not be resolved THEN the refusal names the missing command, does not report a scope/unit/service that did not become active, and does not make the caller wait for a liveness timeout [REQ: a-start-is-reported-as-started-only-when-the-child-could-become-the-engine, scenario: the-refusal-names-the-cause-not-the-symptom]
- [x] AC-4: WHEN the command resolves in the handling process's environment but not in the child's THEN the start is refused with the same refusal as if neither could resolve it [REQ: a-start-is-reported-as-started-only-when-the-child-could-become-the-engine, scenario: a-caller-s-environment-that-differs-from-the-child-s-does-not-decide-the-answer]
- [x] AC-5: WHEN a start is refused for an unresolvable command THEN no label is held, no scope exists, and a subsequent start of the same unit is not refused as already running [REQ: resolution-happens-before-anything-is-claimed, scenario: an-unresolvable-command-leaves-nothing-behind]
- [x] AC-6: WHEN the child fails to become the engine after the scope was claimed THEN the claim is released and the caller is told the start failed with the child's exit status [REQ: resolution-happens-before-anything-is-claimed, scenario: the-child-fails-to-exec-for-a-reason-resolution-cannot-predict]
- [x] AC-7: WHEN a caller requests a work-unit start THEN no field of that request can name the command, the subcommand or an arbitrary argument [REQ: one-command-name-checkable-against-the-engine-s-own-parser, scenario: the-argv-is-built-never-supplied]
- [x] AC-8: WHEN the engine's parser no longer recognises the named starting subcommand as one that starts a unit THEN the check fails rather than the mismatch surviving until a run is attempted [REQ: one-command-name-checkable-against-the-engine-s-own-parser, scenario: a-subcommand-that-no-longer-starts-a-unit-is-caught]

### work-unit-run-observability

- [x] AC-9: WHEN a unit is started with a declared origin THEN the record carries it verbatim and it is readable without the starting process [REQ: a-run-records-where-it-came-from, scenario: a-declared-origin-is-recorded]
- [x] AC-10: WHEN a unit is started on behalf of an identified requester THEN the record carries that requester and not a constant naming the surface that relayed the request [REQ: a-run-records-where-it-came-from, scenario: the-origin-is-the-requester-not-the-surface-that-relayed-it]
- [x] AC-11: WHEN a unit is started without a declared origin THEN the record states that none was declared and names no source [REQ: a-run-records-where-it-came-from, scenario: no-origin-was-declared]
- [x] AC-12: WHEN the started session announces its identifier THEN the record carries it and a reader can reach the session's transcript from the record [REQ: a-run-records-which-agent-session-executed-it, scenario: the-session-announced-its-identifier]
- [x] AC-13: WHEN the session ends without announcing an identifier THEN the record states it is unknown and the run is not recorded as having no session [REQ: a-run-records-which-agent-session-executed-it, scenario: the-session-ended-without-announcing-one]
- [x] AC-14: WHEN a unit has finished and neither its process nor its terminal exists THEN the run's stream is readable from the project's runtime area, keyed to its unit record [REQ: a-run-s-stream-survives-the-process-that-produced-it, scenario: a-finished-run-is-read-after-the-fact]
- [x] AC-15: WHEN a run is killed before it produces a verdict THEN the stream persisted so far remains and is distinguishable from a completed run's [REQ: a-run-s-stream-survives-the-process-that-produced-it, scenario: a-run-is-killed-mid-way]
- [x] AC-16: WHEN a run produced no stream THEN the absence is stated and it is not rendered as an empty but successful run [REQ: a-run-s-stream-survives-the-process-that-produced-it, scenario: a-run-that-produced-no-stream-at-all]
- [x] AC-17: WHEN the framework logs anything about a run THEN the log carries identifiers, counts and outcome and no text taken from the stream [REQ: the-stream-is-written-where-the-project-owns-it-never-into-the-framework, scenario: the-framework-logs-a-run]
- [x] AC-18: WHEN a unit runs in a project tree that is not the framework's own THEN its stream is written under that project's runtime area and nothing derived from it is written under the framework's tree [REQ: the-stream-is-written-where-the-project-owns-it-never-into-the-framework, scenario: a-run-in-a-project-outside-the-framework-s-tree]

### work-cycle-reading-scope

- [x] AC-19: WHEN a project declares additional reading paths and a unit is started THEN the unit is told about them, presented separately from the change's own artifacts [REQ: a-project-may-declare-where-a-unit-reads-from, scenario: a-project-declares-reading-paths]
- [x] AC-20: WHEN a project's declaration carries no reading paths THEN the unit reads exactly what it reads today and the engine adds no path of its own [REQ: a-project-may-declare-where-a-unit-reads-from, scenario: a-project-declares-none]
- [x] AC-21: WHEN a declared reading path does not exist THEN the start reports which path was missing and the run proceeds with the paths that do exist [REQ: a-declared-path-that-is-not-there-is-reported-not-silently-dropped, scenario: a-declared-path-is-missing]
- [x] AC-22: WHEN none of the declared reading paths exist THEN the caller is told the declaration reached nothing, distinguishably from a project that declared nothing [REQ: a-declared-path-that-is-not-there-is-reported-not-silently-dropped, scenario: every-declared-path-is-missing]
- [x] AC-23: WHEN a declared reading path resolves outside the project tree THEN it is refused and named, and no content from outside the tree reaches the prompt [REQ: a-declaration-reaches-outside-the-change-directory-but-never-outside-the-project, scenario: a-path-escaping-the-project-tree]

### work-cycle-screen

- [x] AC-24: WHEN the screen shows a project that has adopted the engine THEN each change is listed with whether a unit is runnable, and a change that is not carries the engine's reason [REQ: a-project-s-changes-and-their-runnable-state-are-visible-before-anything-is-started, scenario: an-adopted-project-with-a-runnable-change]
- [x] AC-25: WHEN the screen shows a project with no adoption declaration THEN it is shown as not adopted, naming what is missing, and no start is offered [REQ: a-project-s-changes-and-their-runnable-state-are-visible-before-anything-is-started, scenario: a-project-that-has-not-adopted-the-engine]
- [x] AC-26: WHEN no change in a project has a runnable group THEN the screen says so with per-group reasons, distinguishably from a project with no changes [REQ: a-project-s-changes-and-their-runnable-state-are-visible-before-anything-is-started, scenario: nothing-is-runnable]
- [x] AC-27: WHEN a person starts a unit for a change from the screen THEN the run appears among that project's runs with its change, group and seat [REQ: a-unit-is-started-from-the-screen-through-the-engine-s-own-entry-point, scenario: a-unit-is-started]
- [ ] AC-28: WHEN the start is refused by the engine, the location check, or an unresolvable command THEN the refusal is shown where the person acted, in its own words, and nothing is shown as started [REQ: a-unit-is-started-from-the-screen-through-the-engine-s-own-entry-point, scenario: the-start-is-refused]
- [x] AC-29: WHEN the service that would hold the run's terminal is unavailable THEN the control states it cannot start and what to run to repair it, rather than failing when used [REQ: a-unit-is-started-from-the-screen-through-the-engine-s-own-entry-point, scenario: the-service-that-holds-terminals-is-unavailable]
- [x] AC-30: WHEN a run has finished and its process is gone THEN its verdict, gate outcome and commit are shown from the record with nothing running — **SEEN on screen** during 7.4: the row read `done 1 · session 09ad8a34 · git add failed`, rendered from the record with no process alive. [REQ: a-run-is-readable-from-what-the-engine-recorded-with-no-process-alive, scenario: a-finished-run]
- [ ] AC-31: WHEN recorded state claims a run in progress whose process is no longer alive THEN it is shown as stale, not as running [REQ: a-run-is-readable-from-what-the-engine-recorded-with-no-process-alive, scenario: a-stale-claim]
- [x] AC-32: WHEN a record claims a live run and the process holding that identifier cannot be confirmed to be the agent THEN the surface says which question was answered and does not render it as an ordinary running run — **SEEN on screen** during 7.4, while the first unit was running: the row read `unconfirmed`, in its own tone, with `(claimed) session unknown` — not as an ordinary running run. [REQ: a-run-is-readable-from-what-the-engine-recorded-with-no-process-alive, scenario: a-live-claim-whose-process-identifier-could-not-be-confirmed]
- [x] AC-33: WHEN a run was set aside because a person must answer THEN the question and its task are shown and the run is not shown as failed [REQ: a-run-is-readable-from-what-the-engine-recorded-with-no-process-alive, scenario: a-run-set-aside-for-a-person]
- [x] AC-34: WHEN a run ended without reporting a verdict THEN that is shown as its own state, not as a run with no outcome yet [REQ: a-run-is-readable-from-what-the-engine-recorded-with-no-process-alive, scenario: a-run-that-never-reported-a-verdict]
- [x] AC-35: WHEN a project's runs are collapsed and one of them failed THEN the collapsed container carries a marker naming how many [REQ: a-failing-run-is-marked-where-the-reader-is-standing, scenario: a-failure-inside-a-collapsed-group]
- [x] AC-36: WHEN no run failed, was set aside, or is stale THEN no marker is shown, and the absence is not produced by state the screen failed to read [REQ: a-failing-run-is-marked-where-the-reader-is-standing, scenario: everything-is-well]
- [ ] AC-37: WHEN a person opens a running unit from the screen THEN its terminal appears in that project's dock, labelled as a work unit with its change [REQ: a-running-unit-s-terminal-is-reachable-in-the-project-s-dock, scenario: opening-a-running-unit-s-terminal]
- [ ] AC-38: WHEN a person closes a running unit's terminal view THEN the run continues and remains reachable from the project's runs [REQ: a-running-unit-s-terminal-is-reachable-in-the-project-s-dock, scenario: closing-the-view]
- [ ] AC-39: WHEN a person opens a run whose process has ended THEN the run's persisted stream is shown, labelled as a recording rather than a live terminal [REQ: a-running-unit-s-terminal-is-reachable-in-the-project-s-dock, scenario: a-finished-run-has-no-terminal]
- [x] AC-40: WHEN the work is claimed complete THEN each touched screen has been opened and described by what was seen, including the refused-start and empty states [REQ: the-screen-is-verified-by-looking-at-it, scenario: the-screens-are-looked-at]
- [ ] AC-41: WHEN the browser cannot be reached to perform that check THEN the verification is reported as not done and no passing test run is offered in its place [REQ: the-screen-is-verified-by-looking-at-it, scenario: the-browser-cannot-be-reached] ⚠ NOT EXERCISED, not failed: the browser WAS reachable this round, so the scenario did not arise. It stays open because a scenario nobody triggered is not a scenario anybody proved.
