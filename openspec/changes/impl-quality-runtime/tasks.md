## 1. Proactive Rule Injection at Dispatch

- [ ] 1.1 Add `rule_keyword_mapping()` method to the base profile interface in `profile_loader.py` — returns a dict mapping category names to keyword lists and rule glob patterns; default implementation returns `{"auth": {"keywords": ["auth", "login", "session", "middleware", "cookie", "password", "token"], "globs": ["web/auth-middleware.md", "web/security-patterns.md"]}, "api": {"keywords": ["api", "route", "endpoint", "handler", "REST", "mutation"], "globs": ["web/api-design.md", "web/security-patterns.md"]}, "database": {"keywords": ["database", "query", "migration", "schema", "model", "prisma", "drizzle"], "globs": ["web/security-patterns.md"]}}` [REQ: dispatcher-injects-relevant-rules-into-agent-context, REQ: rule-keyword-mapping-is-configurable-via-profile]
- [ ] 1.2 Add `_build_rule_injection()` function in `dispatcher.py` — reads the change's scope string (already available in the dispatch context) for keyword matches against `rule_keyword_mapping()`, loads matching rule files from `{wt_path}/.claude/rules/`, deduplicates, truncates to 4000 chars total, returns a formatted string with `## Relevant Patterns` header; note: `rule_keyword_mapping()` is for dispatch-time proactive injection, complementary to `security_rules_paths()` which is for review-time injection [REQ: dispatcher-injects-relevant-rules-into-agent-context]
- [ ] 1.3 Call `_build_rule_injection()` from the scope enrichment block in `dispatcher.py` (around line 929 where `ctx.design_context` is built — follow the same pattern) and append the result to the agent's scope block; skip silently if `.claude/rules/` does not exist or no keywords match [REQ: dispatcher-injects-relevant-rules-into-agent-context]

## 2. Content-Aware Review Enhancement

- [ ] 2.1 Add `classify_diff_content(diff_text: str) -> set[str]` function in `templates.py` — scans diff for file path patterns and code patterns; returns a set of categories: "auth" (files matching `*auth*`, `*session*`, `*middleware*`, `*login*`), "api" (code containing `router.`, `app.get(`, `app.post(`, handler exports), "database" (code containing `prisma.`, `db.`, `query(`, `.findMany`, `.create(`), "frontend" (files matching `*.tsx`, `*.jsx`, `*.vue`, `*.svelte`) [REQ: llm-code-review]
- [ ] 2.2 Add `_content_aware_instructions(categories: set[str]) -> str` function in `templates.py` — maps each category to specific review instructions (auth→middleware+cold-visit check, api→IDOR+pagination check, database→data-scoping check); returns formatted instruction text capped at 2000 chars [REQ: llm-code-review]
- [ ] 2.3 Update `render_review_prompt()` in `templates.py` to call `classify_diff_content()` on the diff and append `_content_aware_instructions()` output after the existing Review Criteria section [REQ: llm-code-review]
- [ ] 2.4 Add `_load_security_rules(wt_path)` call in `review_change()` in `verifier.py` BEFORE the first `run_claude()` call and pass the result to `render_review_prompt()` via a new optional `security_rules` parameter; currently `review_change()` does NOT call `_load_security_rules()` at all — the function exists but is only used in the `handle_change_done()` retry context; also update the `wt-orch-core template review` JSON input schema to pass `security_rules` through [REQ: llm-code-review]

## 3. E2E Readiness Probe

- [ ] 3.1 Add E2E health check call in `handle_change_done()` Step 3 (E2E gate section, around line 1362) — after `e2e_port` is assigned locally (it's a Python variable, NOT an env var) but BEFORE running the E2E command, call the existing `health_check()` function with `http://localhost:{e2e_port}` and a configurable timeout; note: this assumes the dev server is started by the E2E globalSetup (webServer config in playwright.config.ts) — the probe verifies it's actually responding before assertions run; if health check fails, set `e2e_result_str = "skip-unhealthy"` and log a warning instead of running E2E [REQ: e2e-gate-performs-health-check-before-running-playwright]
- [ ] 3.2 Add `E2E_HEALTH_TIMEOUT` constant (default 30) to the constants section of `verifier.py`; allow override from orchestration config via `config.get("e2e_health_timeout", 30)` [REQ: e2e-gate-performs-health-check-before-running-playwright]

## 4. Smoke/E2E Output Parsing

- [ ] 4.1 Add `parse_test_output(output: str) -> dict` function in `verifier.py` — uses regex patterns to extract structured results from test runner output: Jest/Vitest pattern `Tests:\s+(\d+)\s+passed(?:.*?(\d+)\s+failed)?`, Playwright pattern `(\d+)\s+passed(?:.*?(\d+)\s+failed)?` (failed group optional — clean runs only show passed count), generic fallback returns `{"framework": "unknown"}`; returns dict with keys `framework`, `passed`, `failed` (default 0), `total` [REQ: test-output-is-parsed-for-structured-results]
- [ ] 4.2 Call `parse_test_output()` after each test gate execution (unit tests in Step 2, E2E in Step 3) and store parsed results in change state via `update_change_field()` — `test_parsed` for unit tests, `e2e_parsed` for E2E; do not change gate pass/fail logic [REQ: test-output-is-parsed-for-structured-results]

## 5. Spec Coverage Annotation Report

**Prerequisite:** Requires `spec-implementation-fidelity` tasks 6.1-6.2 to be applied first (introduces `deferred_requirements[]` in plan schema). If applied before that change, treat missing `deferred_requirements` key as empty `[]`.

- [ ] 5.1 Add `generate_coverage_report(plan: dict, digest_dir: str, output_path: str)` function in `planner.py` — reads `requirements.json` from digest_dir, iterates each requirement, checks if its ID appears in any change's `requirements[]` or `also_affects_reqs[]`, checks `plan.get("deferred_requirements", [])`, marks as COVERED/DEFERRED/UNCOVERED; writes a markdown table to output_path with columns: Requirement ID, Title, Status, Change(s); includes summary line; note: `validate_plan()` already has the `plan` dict in local scope (around line 307) — pass it directly [REQ: post-planning-generates-spec-coverage-report, REQ: coverage-report-is-human-readable-with-per-requirement-detail]
- [ ] 5.2 Call `generate_coverage_report()` from `validate_plan()` after successful validation (no errors) — output to `wt/orchestration/spec-coverage-report.md`; skip if no digest_dir; regenerate on replan by overwriting existing file [REQ: post-planning-generates-spec-coverage-report]

## Acceptance Criteria (from spec scenarios)

- [ ] AC-1: WHEN a change has tasks containing "auth" or "login" keywords THEN dispatcher injects auth rule files into agent scope [REQ: dispatcher-injects-relevant-rules-into-agent-context, scenario: task-with-auth-keywords-triggers-auth-rule-injection]
- [ ] AC-2: WHEN a change has tasks containing "route" or "endpoint" keywords THEN dispatcher injects API rule files into agent scope [REQ: dispatcher-injects-relevant-rules-into-agent-context, scenario: task-with-api-keywords-triggers-api-rule-injection]
- [ ] AC-3: WHEN a change has tasks with no matching keywords THEN no rules are injected and dispatch proceeds normally [REQ: dispatcher-injects-relevant-rules-into-agent-context, scenario: task-with-no-matching-keywords-skips-rule-injection]
- [ ] AC-4: WHEN a project has no `.claude/rules/` directory THEN rule injection is skipped without error [REQ: dispatcher-injects-relevant-rules-into-agent-context, scenario: project-without-rules-directory-degrades-gracefully]
- [ ] AC-5: WHEN the project profile defines `rule_keyword_mapping()` THEN the dispatcher uses the profile's mapping [REQ: rule-keyword-mapping-is-configurable-via-profile, scenario: profile-provides-custom-keyword-mapping]
- [ ] AC-6: WHEN a review diff contains auth-related files THEN the review prompt includes auth-specific instructions [REQ: llm-code-review, scenario: diff-with-auth-related-changes-triggers-auth-checks]
- [ ] AC-7: WHEN a review diff contains API route patterns THEN the review prompt includes IDOR check instructions [REQ: llm-code-review, scenario: diff-with-api-route-changes-triggers-idor-checks]
- [ ] AC-8: WHEN a review diff contains database patterns THEN the review prompt includes data scoping instructions [REQ: llm-code-review, scenario: diff-with-database-changes-triggers-data-scoping-checks]
- [ ] AC-9: WHEN a code review runs for the first time THEN security rules are included in the prompt (not deferred to retry) [REQ: llm-code-review, scenario: security-rules-injected-on-first-review-attempt]
- [ ] AC-10: WHEN the E2E gate starts and dev server responds within timeout THEN Playwright proceeds immediately [REQ: e2e-gate-performs-health-check-before-running-playwright, scenario: dev-server-responds-within-timeout]
- [ ] AC-11: WHEN the E2E gate starts and dev server does not respond within timeout THEN E2E is skipped with "skip-unhealthy" status [REQ: e2e-gate-performs-health-check-before-running-playwright, scenario: dev-server-does-not-respond-within-timeout]
- [ ] AC-12: WHEN test output contains Jest/Vitest patterns THEN parsed pass/fail counts are stored in change state [REQ: test-output-is-parsed-for-structured-results, scenario: jest-vitest-output-is-parsed]
- [ ] AC-13: WHEN test output format is unrecognized THEN parser returns fallback with exit code only [REQ: test-output-is-parsed-for-structured-results, scenario: unrecognized-output-format-falls-back-gracefully]
- [ ] AC-14: WHEN parsed results are extracted THEN gate pass/fail decisions remain based on exit codes [REQ: test-output-is-parsed-for-structured-results, scenario: parsed-results-do-not-change-gate-behavior]
- [ ] AC-15: WHEN all digest requirements are assigned to changes THEN coverage report marks all as COVERED [REQ: post-planning-generates-spec-coverage-report, scenario: all-requirements-covered-by-changes]
- [ ] AC-16: WHEN a requirement is deferred THEN coverage report marks it as DEFERRED with reason [REQ: post-planning-generates-spec-coverage-report, scenario: some-requirements-deferred]
- [ ] AC-17: WHEN coverage report is generated THEN it includes a markdown table with Requirement ID, Title, Status, Change(s) columns and a summary line [REQ: coverage-report-is-human-readable-with-per-requirement-detail, scenario: report-format-includes-requirement-details]
- [ ] AC-18: WHEN a replan occurs THEN the coverage report is regenerated with new assignments [REQ: coverage-report-is-human-readable-with-per-requirement-detail, scenario: report-is-regenerated-on-replan]
- [ ] AC-19: WHEN the project profile does not define `rule_keyword_mapping()` THEN the dispatcher uses the built-in default (auth/api/database categories) [REQ: rule-keyword-mapping-is-configurable-via-profile, scenario: profile-does-not-provide-keyword-mapping]
- [ ] AC-20: WHEN orchestration config provides `e2e_health_timeout: N` THEN the probe uses N seconds as the timeout [REQ: e2e-gate-performs-health-check-before-running-playwright, scenario: health-check-timeout-is-configurable]
- [ ] AC-21: WHEN test output shows only passed count with zero failures THEN parser extracts passed count and sets failed to 0 [REQ: test-output-is-parsed-for-structured-results, scenario: clean-test-run-without-failures]
