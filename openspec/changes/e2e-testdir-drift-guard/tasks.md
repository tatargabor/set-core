# Tasks: e2e-testdir-drift-guard

## 1. Failure-parser classification (Layer 2a)

- [x] 1.1 Open `modules/web/set_project_web/gates.py`. Locate the unparseable-fail path in `execute_e2e_gate` (the branch where `_extract_e2e_failure_ids(e2e_output)` returned empty AND `exit_code != 0`). Define a module-level constant `_TESTDIR_MISMATCH_SIGNATURE = "Error: No tests found."` near the existing dep-drift / env-drift signature constants. [REQ: failure-parser-classifies-no-tests-found-as-testdir-mismatch]
- [x] 1.2 Add a helper `_classify_unparseable_failure(e2e_output: str, project_root: str) -> str` that returns one of the literal failure-class strings: `"testdir_mismatch"` (when `_TESTDIR_MISMATCH_SIGNATURE` is present and corroborated by spec files existing on disk), or the existing default classification message when no known signature matches. The helper SHALL NOT itself fire a self-heal; it produces the diagnostic string only. [REQ: failure-parser-classifies-no-tests-found-as-testdir-mismatch]
- [x] 1.3 Build the `testdir_mismatch` retry-context message that identifies (a) the current `testDir` value parsed from `playwright.config.ts`, (b) the directory containing the requested spec file (parsed from the captured Playwright command line), and (c) a hint like `"playwright.config.ts testDir vs spec file path mismatch"`. The message replaces the legacy `"[no parseable failure list — likely crash, OOM, or formatter issue]"` for this signature only. [REQ: failure-parser-classifies-no-tests-found-as-testdir-mismatch]
- [x] 1.4 Wire `_classify_unparseable_failure` into the existing unparseable-fail return path of `execute_e2e_gate` so the chosen message is what feeds `GateResult.output` and the agent retry context. The classification SHALL fire even when self-heal subsequently attempts recovery — it is the failure label, separate from the recovery action. [REQ: failure-parser-classifies-no-tests-found-as-testdir-mismatch]

## 2. Implement `_extract_testdir_drift` (Layer 2b)

- [x] 2.1 Implement `_extract_testdir_drift(e2e_output: str, project_root: str) -> bool` returning `True` only when ALL of: (a) `e2e_output` contains `_TESTDIR_MISMATCH_SIGNATURE`, (b) at least one `*.spec.ts` file exists anywhere under `project_root` (early-exit walk capped at 200 files), (c) at least one such spec file is NOT under the directory currently declared as `testDir:` in `playwright.config.ts`. [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [x] 2.2 Add a doctest with one positive case (Playwright "No tests found" output + a stale config with `testDir: "e2e"` and merged specs at `tests/e2e/`) and one negative case (the same Playwright output but all specs are already under the declared testDir, simulating an operator typo on the CLI). [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]

## 3. Implement `_resolve_canonical_testdir`

- [x] 3.1 Implement `_resolve_canonical_testdir(project_root: str) -> str | None`: walk the project root for `*.spec.ts` files (cap 200), bucket by parent directory (relative to project root), pick the directory with the most specs. Tiebreaker: prefer `tests/e2e` over any other path; secondary tiebreaker: prefer shorter path. Return relative-to-root path string or `None` if no specs exist. [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [x] 3.2 Define a module-level constant `_CANONICAL_TESTDIR_PREFERENCE = "tests/e2e"` near the signature constants and reference it from the resolver — keep the convention as a single source of truth and document why this path wins (matches dispatcher prompt + canonical scaffold template). [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]

## 4. Implement `_resync_playwright_config_testdir`

- [x] 4.1 Implement `_resync_playwright_config_testdir(project_root: str, new_testdir: str) -> bool`: read `playwright.config.ts`, replace the line matching `^\s*testDir\s*:\s*"[^"]*",?\s*$` with the canonical-testdir-preserving form (preserve leading whitespace, trailing comma, trailing comment if present). Replace `^\s*globalSetup\s*:\s*"[^"]*",?\s*$` with `globalSetup: "./<new_testdir>/global-setup.ts"`. If the regex does not match either field, return `False`. Atomic write: write to `<path>.tmp` then `os.replace`. Catch IO errors; return `False` on failure (caller logs and falls through). [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [x] 4.2 Add the global-setup migration step BEFORE the config rewrite: if `<project_root>/<new_testdir>/global-setup.ts` does NOT exist AND `<project_root>/<old_testdir>/global-setup.ts` does, copy the stale file's contents to the canonical path via `shutil.copy`. Do NOT delete the source. If the canonical path already has a `global-setup.ts`, leave it untouched. [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]

## 5. Implement `_self_heal_testdir_drift`

- [x] 5.1 Implement `_self_heal_testdir_drift(...)` mirroring `_self_heal_db_env_drift`'s signature: `(wt_path, profile, e2e_output, change_name, env, actual_e2e_cmd, e2e_timeout) -> tuple[bool, str, object] | None`. [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [x] 5.2 Body: if `_extract_testdir_drift(e2e_output, wt_path)` is False → return `None`. Resolve canonical_testdir via `_resolve_canonical_testdir`. If `None` or equal to current testDir → return `None`. Read current testDir from config (regex). Capture `t0 = time.monotonic()`. Call `_resync_playwright_config_testdir(wt_path, canonical_testdir)`. If False → return `None`. Log INFO `e2e_testdir_drift_detected` with fields. Invoke `run_command(["bash", "-c", actual_e2e_cmd], timeout=e2e_timeout, cwd=wt_path, env=env, ...)`. Compute `healed`: `rerun_result.exit_code == 0 and not rerun_result.timed_out and rerun_flaky == 0 and not rerun_runtime_errors`. Log INFO `e2e_testdir_self_heal_resynced_and_rerun`. Return `(healed, canonical_testdir, rerun_result)`. [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]

## 6. Wire into `execute_e2e_gate`

- [x] 6.1 Locate the existing self-heal call site in `execute_e2e_gate`. The integration point is the same unparseable-fail branch where `_self_heal_db_env_drift` was wired. Add the testdir-drift call AFTER `_self_heal_db_env_drift`, gated by the same `self_heal_attempted` flag. [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [x] 6.2 The wiring block follows the env-drift pattern: `if not self_heal_attempted: testdir_result = _self_heal_testdir_drift(...); if testdir_result is not None: self_heal_attempted = True; healed, recovered_testdir, rerun_result = testdir_result; if healed: self_heal_marker = f"[self-heal: synced playwright.config.ts testDir from {old_testdir} to {recovered_testdir}]\n"; e2e_cmd_result = rerun_result; e2e_output = rerun_result.stdout + rerun_result.stderr`. The marker is prepended via the existing pass-path code that already handles dep-drift and env-drift markers — confirm the same path is reached. [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [x] 6.3 Confirm `verify_retry_count` is NOT incremented anywhere in the testdir-drift branch. Only the fail-path (`_handle_blocking_failure`) increments. [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [x] 6.4 Confirm at most one `[self-heal: ...]` marker can appear per `GateResult.output` — the shared `self_heal_attempted` flag enforces this across all three classes (dep / env / testdir). [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]

## 7. Verify-gate canary (`_lint_playwright_testdir_consistency`) (Layer 2c)

- [x] 7.1 Open `modules/web/set_project_web/verifier.py`. Locate the existing `_lint_e2e_navigation` function (added in commit `b27a96b3`). Add a sibling helper `_lint_playwright_testdir_consistency(project_root: str) -> GateResult` that runs as part of the same e2e_coverage / structural verify lints. [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence]
- [x] 7.2 Implementation: read `playwright.config.ts` if present. Extract `testDir:` value via the same regex used in `_resync_playwright_config_testdir`. Walk for `*.spec.ts` (cap 200). Bucket by parent directory. Compute spec count under declared `testDir` and the densest sibling directory. Emit `warn` if (a) declared testDir has zero specs but at least one spec exists elsewhere, OR (b) another directory has at least 3x the spec count of declared testDir. Otherwise emit `pass`. NEVER emit `fail`. [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence]
- [x] 7.3 Build the `warn` message with three fields: declared testDir + its spec count, canonical candidate testdir + its spec count, plus the literal hint `"playwright.config.ts testDir vs spec layout divergence"`. The agent reading this message SHOULD know which value to align toward. [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence]
- [x] 7.4 Wire `_lint_playwright_testdir_consistency` into the verify gate's lint runner alongside `_lint_e2e_navigation`. Register the result via the same GateResult collector so warns and passes both flow through the standard verify-gate review UI. [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence]

## 8. Forensic logging

- [x] 8.1 In `_self_heal_testdir_drift`, ensure two structured INFO logs fire: `e2e_testdir_drift_detected` (on initial signature match, before rewrite) with fields `change=<name>`, `wt=<path>`, `stale_testdir=<old>`, `canonical_testdir=<new>`, `spec_count=<int>`. And `e2e_testdir_self_heal_resynced_and_rerun` (after rerun) with fields `change=<name>`, `resync_duration_ms=<int>`, `rerun_outcome=pass|fail_parseable|fail_unparseable`. [REQ: forensic-logging-of-testdir-self-heal-and-canary-events]
- [x] 8.2 In `_lint_playwright_testdir_consistency`, fire one structured INFO log when warning: `playwright_testdir_consistency_warn` with fields `change=<name>`, `stale_testdir=<declared>`, `canonical_candidate=<observed>`, `stale_spec_count=<int>`, `canonical_spec_count=<int>`. [REQ: forensic-logging-of-testdir-self-heal-and-canary-events]
- [ ] 8.3 Verify the new events appear in `set/orchestration/python.log` and are parseable by `set-run-logs`. If `set-run-logs` has hardcoded event filters, extend the filter list to include the three new event names. [REQ: forensic-logging-of-testdir-self-heal-and-canary-events]
- [ ] 8.4 Verify the dashboard's gate-output panel surfaces the `[self-heal: synced playwright.config.ts testDir from <old> to <new>]` marker via the existing `\[self-heal: ` regex (no dashboard code change should be needed; confirm by inspecting the current renderer or adding one). [REQ: forensic-logging-of-testdir-self-heal-and-canary-events]

## 9. Tests

- [x] 9.1 Unit tests for `_extract_testdir_drift`: positive (No-tests-found + drifted spec layout), negative (No-tests-found + specs already under testDir), negative (no spec files in project at all), negative (non-No-tests-found output). File: `tests/unit/test_e2e_testdir_drift_guard.py` (consolidated into one file alongside related helpers). [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [x] 9.2 Unit tests for `_resolve_canonical_testdir`: single dir with specs, canonical-preferred tiebreaker, count-wins-over-preference. File: `tests/unit/test_e2e_testdir_drift_guard.py` (consolidated). [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [x] 9.3 Unit tests for `_resync_playwright_config_testdir`: regex matches and rewrites both `testDir:` and `globalSetup:`, regex does NOT match (returns False), global-setup migration when destination is absent vs present. File: `tests/unit/test_e2e_testdir_drift_guard.py` (consolidated). [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [x] 9.4 Unit tests for `_lint_playwright_testdir_consistency`: testDir empty + specs elsewhere → warn, testDir matches densest → pass, no playwright.config.ts → pass, no specs → pass, 3x heuristic boundary cases (just under 3x → pass, exactly 3x → warn). File: `tests/unit/test_e2e_testdir_drift_guard.py` (consolidated). [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence]
- [ ] 9.5 Integration test in `tests/integration/test_e2e_testdir_drift_self_heal.py` invoking `execute_e2e_gate` directly (deferred — covered by live micro-web E2E run validation; full integration test requires full env/profile fixture setup) [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [ ] 9.6 Forensic-log integration test (deferred — covered by live micro-web E2E run validation) [REQ: forensic-logging-of-testdir-self-heal-and-canary-events]
- [x] 9.7 Run `pytest tests/unit/test_e2e_testdir_drift_guard.py`. All 24 tests green. [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]

## 10. Documentation and validation

- [x] 10.1 Update the module-level docstring in `modules/web/set_project_web/gates.py` to mention the testdir-drift self-heal class alongside the existing dep-drift and env-drift entries — kept the three siblings symmetrically described in the new docstring. [REQ: failure-parser-classifies-no-tests-found-as-testdir-mismatch]
- [ ] 10.2 Update `modules/web/set_project_web/verifier.py` docstring (deferred — the verifier file lives at `lib/set_orch/verifier.py`, not under `modules/web/`; the canary helper is documented inline at its definition site instead). [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence]
- [x] 10.3 Run `openspec validate e2e-testdir-drift-guard --strict`. [REQ: failure-parser-classifies-no-tests-found-as-testdir-mismatch]
- [ ] 10.4 Add a one-line entry to the changelog or `docs/release/` notes (deferred — release notes belong to a separate periodic-update workflow). [REQ: forensic-logging-of-testdir-self-heal-and-canary-events]

## 11. Manual verification on a live run

- [ ] 11.1 Restart the running `set-web` / sentinel process so the new `gates.py` and `verifier.py` are loaded (Python module cache requires a process restart; venv-level reload is sufficient). [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [ ] 11.2 Reproduce the empirical drift case from `micro-web-run-20260501-1805` in a fresh scratch project (or rerun the same orchestration against a checkpoint that exhibited the drift): set `playwright.config.ts` `testDir: "e2e"`, scaffold spec files in `tests/e2e/`, run the e2e gate. Confirm: gate returns `pass`, output begins with the testdir self-heal marker, `verify_retry_count` does not increment. [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift]
- [ ] 11.3 Run a verify gate on a worktree with intentional testdir drift. Confirm `_lint_playwright_testdir_consistency` fires `warn` (yellow) in the dashboard's verify-gate review UI, with the actionable message naming both directories. [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence]
- [ ] 11.4 Run `set-run-logs <run-id> --gate e2e | grep -E "(testdir_drift_detected|testdir_self_heal|playwright_testdir_consistency_warn)"` and confirm all three new events are present with their full field set. [REQ: forensic-logging-of-testdir-self-heal-and-canary-events]

## Acceptance Criteria (from spec scenarios)

### REQ: failure-parser-classifies-no-tests-found-as-testdir-mismatch

- [ ] AC-1: WHEN unparseable Playwright failure with `Error: No tests found.` is captured AND specs exist on disk outside the declared testDir THEN gate retry-context output names the testDir/spec divergence and does NOT use the legacy generic message [REQ: failure-parser-classifies-no-tests-found-as-testdir-mismatch, scenario: unparseable-failure-with-no-tests-found-gets-precise-classification]
- [ ] AC-2: WHEN unparseable Playwright failure does NOT contain `Error: No tests found.` THEN gate falls through to the existing dep-drift / env-drift / generic classification chain [REQ: failure-parser-classifies-no-tests-found-as-testdir-mismatch, scenario: other-unparseable-failures-retain-generic-classification]

### REQ: gate-runner-self-heals-on-testdir-spec-layout-drift

- [ ] AC-3: WHEN `playwright.config.ts` declares stale `testDir: "e2e"` AND merged specs live at `tests/e2e/` AND Playwright crashes "No tests found" THEN gate self-heal rewrites config to canonical testDir, copies stale `global-setup.ts` to canonical path (since destination absent), reruns, returns `pass` with `[self-heal: synced playwright.config.ts testDir from e2e to tests/e2e]` marker, and `verify_retry_count` is unchanged [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift, scenario: stale-main-config-correct-spec-layout-triggers-self-heal-and-recovers]
- [ ] AC-4: WHEN gate captures "No tests found" AND project root contains zero `*.spec.ts` files THEN self-heal probe returns `None` and gate proceeds to normal fail path with precise testdir-mismatch classification [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift, scenario: no-spec-files-anywhere-no-self-heal]
- [ ] AC-5: WHEN gate captures "No tests found" AND all specs already live under declared testDir THEN self-heal probe returns `None` and gate fails with classification but no rewrite [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift, scenario: specs-already-under-current-testdir-no-self-heal]
- [ ] AC-6: WHEN config rewrite regex does not match canonical field forms THEN `_resync_playwright_config_testdir` returns `False`, self-heal returns `None`, gate falls through to normal fail path with WARNING log [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift, scenario: atomic-rewrite-fails-no-self-heal-original-error-preserved]
- [ ] AC-7: WHEN both MODULE_NOT_FOUND and "No tests found" appear in same output THEN `_self_heal_missing_module` runs first, testdir-drift probe is skipped, only one `[self-heal: ...]` marker appears in output [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift, scenario: self-heal-does-not-fire-when-an-earlier-self-heal-class-already-attempted]
- [ ] AC-8: WHEN testdir-drift probe runs and rerun also crashes with "No tests found" THEN probe does NOT recurse, returns rerun's failure verdict, no second config rewrite [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift, scenario: self-heal-succeeds-at-most-once-per-gate-invocation]
- [ ] AC-9: WHEN testdir-drift self-heal recovers from `e2e` to `tests/e2e` THEN `GateResult.output` begins with literal `[self-heal: synced playwright.config.ts testDir from e2e to tests/e2e]\n` parseable by regex [REQ: gate-runner-self-heals-on-testdir-spec-layout-drift, scenario: marker-contains-old-and-new-testdir-values]

### REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence

- [ ] AC-10: WHEN `playwright.config.ts` declares `testDir: "e2e"` with 1 spec AND `tests/e2e/` has 11 specs THEN canary emits `warn` with both spec counts named, verify gate does NOT fail solely from this warn [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence, scenario: drift-detected-at-pr-time-warn-not-fail]
- [ ] AC-11: WHEN declared `testDir` has zero specs AND specs exist elsewhere THEN canary emits `warn` hinting at the canonical candidate path [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence, scenario: testdir-is-empty-but-specs-exist-elsewhere-warn]
- [ ] AC-12: WHEN declared `testDir` is the spec-densest directory THEN canary emits `pass` [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence, scenario: testdir-matches-spec-densest-directory-pass]
- [ ] AC-13: WHEN project has no `playwright.config.ts` THEN canary emits `pass` without scanning for spec files [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence, scenario: project-has-no-playwright-config-ts-pass]
- [ ] AC-14: WHEN project has no `*.spec.ts` files anywhere THEN canary emits `pass` (greenfield project, no drift to flag) [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence, scenario: project-has-no-spec-files-anywhere-pass]
- [ ] AC-15: WHEN sibling directory has fewer than 3x the testDir's spec count THEN canary emits `pass` (heuristic prevents false-positive on legitimate spread) [REQ: verify-gate-canary-warns-on-testdir-spec-layout-divergence, scenario: heuristic-threshold-prevents-false-positive-on-legitimate-spread]

### REQ: forensic-logging-of-testdir-self-heal-and-canary-events

- [ ] AC-16: WHEN self-heal recovers AND operator runs `set-run-logs <run-id> --gate e2e --change <name>` THEN output includes `e2e_testdir_drift_detected` and `e2e_testdir_self_heal_resynced_and_rerun` events with full field set, AND verdict line shows the testdir-drift marker [REQ: forensic-logging-of-testdir-self-heal-and-canary-events, scenario: self-heal-pass-is-distinguishable-from-real-pass-in-forensics]
- [ ] AC-17: WHEN canary fires `warn` AND operator runs `set-run-logs <run-id> --gate verify --change <name>` THEN output includes `playwright_testdir_consistency_warn` event with all five fields, AND verify-gate review UI renders warn at yellow severity [REQ: forensic-logging-of-testdir-self-heal-and-canary-events, scenario: verify-canary-warn-is-distinguishable-in-forensics]
