## 1. The retroactive half — spec the reading code that already ships

These tasks are **already implemented**; they exist so the capability spec covers what shipped
without one, and so a later change cannot alter it silently. Each is verified by reading the code
and running the check named, not by assuming.

- [x] 1.1 Verify `read_project_declaration` distinguishes absent from empty and does not raise on a malformed file; add a test for the malformed case if none holds it. [REQ: a-projects-declaration-is-read-as-what-it-says-and-absence-is-not-emptiness] **DONE 2026-08-19, and it found a real gap.** Absent-vs-empty was already held; **malformed was not**, and driving four malformed shapes turned up one the reader got WRONG: a top-level YAML *list* parsed fine, so `present` came back `True` with nothing wanted — a project rendering as *declared* while every comparison built on it comes back empty, which reads as agreement. That is the false-value direction. Fixed: a shape this reader cannot use is malformed whether or not the parser objected, and is reported as absent. Held by a parametrised test over four shapes plus the present-and-empty case.
- [x] 1.2 Verify `plan_files` excludes declared executable paths and reports a path declared as both; hold both directions with a test. [REQ: the-files-a-module-places-are-declared-and-its-executable-part-is-never-among-them] **VERIFIED 2026-08-19 — already held, nothing to add.** `test_the_executable_part_is_not_copied_into_a_project` covers the exclusion and `test_declaring_a_path_as_both_executable_and_installed_is_an_error` covers the contradiction, both in `tests/unit/test_module_install.py`. The install-level half is 3.2's pair.
- [x] 1.3 Verify the three version outcomes, including that a project with no declaration yields **no comparisons** rather than an empty set of mismatches. (Held since 2026-08-19 by `tests/unit/test_fleet_capabilities.py`; confirm the tests live where this capability can find them.) [REQ: a-version-comparison-never-reports-cannot-tell-as-agreement] **VERIFIED 2026-08-19 — already held.** Four tests in `tests/unit/test_fleet_capabilities.py` drive `report_for_project` end to end over a real project tree: mismatch, match, unknown-never-a-match, and no-declaration-yields-no-comparisons. They live in the fleet suite because the surface is what starves without them; the capability they prove is this one.

## 2. The writer

- [x] 2.0 **First, make the per-file outcome available as DATA.** `_deploy_single_template` returns `List[str]` of prose; a wrapper that parsed it back into structure would be a parser over human-facing text, which is a named defect class here. Have the engine produce per-file outcomes structurally and render the existing prose FROM them, so the CLI's output is unchanged byte for byte. Assert that last part — an unchanged-output test is what makes this safe to do on `set-project init`'s critical path. [REQ: the-install-reports-what-it-did-not-do-and-says-so-when-it-changed-nothing] **DONE 2026-08-19.** `FileOutcome` + a single `_outcome()` recorder that appends to BOTH forms in one call, so a branch cannot report to one and not the other. Every branch of the loop rewired: tombstoned, deleted-by-project, deleted-in-git-history, exists, merge, scaffold, protected, newer-generator, and the write itself — plus **the announcement**, which edits a file the PROJECT owns and would otherwise have been the one write no report mentions. `outcomes` is an optional sink, so no existing caller changed. **The prose is proven unchanged against the previous commit, not asserted to be:** an isolated worktree at `HEAD`, `PYTHONPATH` at its own `lib`, **import origin checked** (`FileOutcome` absent there, present here), the same fixture through four `force`×`dry_run` combinations — byte-identical JSON. That proof cannot live in a test, so the golden text it produced does, including **two inconsistencies pinned as they are**: `exists` and `protected` say "Skipped" even in a dry run where every other skip says "Would skip". Fixing those changes what `set-project init` prints and does not belong in a change about something else. Five mutations caught — and a sixth was NOT, because the test it targeted asserted the wrong thing: a mutable `outcomes=[]` default still returns the prose, so a test asking only *did the prose come back* passes on it. Retargeted at the hazard's actual shape (two runs, and the default asserted to be `None`), it is caught.
- [x] 2.1 `install_module(decl, project_root, *, dry_run=False) -> InstallReport` in `lib/set_orch/module_install.py`. A **wrapper**: plans with `plan_files(decl)` so the executable exclusion is structural, calls the engine's per-file path, and turns 2.0's outcomes into the report. **No copier of its own** (D1). [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline] **DONE 2026-08-19.** `install_module(decl, project_root, *, template_dir=None, dry_run=False, force=True)` in `module_install.py`. A wrapper: it plans with `plan_files`, calls `_deploy_single_template` with an outcome sink, and turns those outcomes into an `InstallReport`. Not a byte is copied here. `force=True` by default and the reason is stated in the docstring — with `force=False` the engine skips every file that merely *exists*, so an install into a project that has the module becomes a no-op reporting nothing; the guards that protect the project's own edits (`protected`, `once`, the ledger, tombstones) are untouched by `force`.
- [x] 2.2 The requirement check runs FIRST and raises, naming the missing requirement. Not a skip, not a field — a refusal is control flow (D2). [REQ: a-missing-requirement-is-a-refusal-before-anything-is-written] **DONE 2026-08-19.** `check_requirements` runs before anything else and raises `InstallRefused`, naming the missing module. Two tests, both directions, plus the ordering one — `test_the_refusal_precedes_the_first_write` hashes the whole project tree before and after and asserts the FIRST planned path specifically, because a refusal after one write is the failure the ordering exists to prevent.
- [x] 2.3 Every skipped file reaches `InstallReport.skip()` with the reason the deploy path gave — protected-and-modified, seeded-once, tombstoned, deleted-on-purpose. A reason invented by the caller is a second copy of the engine's own vocabulary; take the engine's. [REQ: the-install-reports-what-it-did-not-do-and-says-so-when-it-changed-nothing] **DONE 2026-08-19.** Every skip carries the engine's own reason, taken from the `FileOutcome` rather than re-worded by the caller — one vocabulary, one owner. `unchanged` is deliberately a skip with the reason `identical`: calling a byte-identical no-op a *write* would turn a run that changed nothing into a report of twelve files written, and saying nothing at all would be the silent half.
- [x] 2.4 `InstallRecord.save()` runs LAST and never on refusal (D3). ⚠ **And it must run for a module with no `announce:` section** — measured, the only `record.save(target_dir)` in `lib/` sits inside `if decl.announce is not None`, so a non-announcing module installs and leaves no record of itself. That absence is why the capability report falls back to inference. Hold it with a test whose module declares no announcement. Decide and record open question 1 — whether a run that skipped everything updates the record — in the spec, not only in the code. [REQ: what-is-installed-is-recorded-and-the-record-is-written-only-after-the-write] **DONE 2026-08-19, and the fix is in the ENGINE, not in the wrapper.** The record write moved OUT of the `if decl.announce is not None` branch, so every install records itself — measured: a module with no announcement previously installed its files and left no trace, which is why the capability report has to infer from file presence. ⚠ **This is a deliberate deviation from the design's non-goal** *("changing what `set-project init` does")*: two writers of one record would be the second-place defect, and the wrapper alone would have left `init` still not recording. The change is strictly additive — a record appears where none did — and the deploy suites (116 tests) are green. Held by a test whose module declares NO announcement; a suite whose every fixture announces would pass on the broken build.
- [x] 2.5 Resolve open question 3 before writing: where `perform_announcement` sits in the sequence, and how it appears in the report. It edits a project-owned file and must not become the one write the report does not mention. [REQ: the-install-reports-what-it-did-not-do-and-says-so-when-it-changed-nothing] **RESOLVED 2026-08-19 (open question 3).** `perform_announcement` runs where it always did — after the files — and is now an `announced` outcome, so it reaches the report like every other write. It edits a file the PROJECT owns, and it is not one of the planned files, which is exactly why it would otherwise have been the one write no report mentions. Held by `test_the_announcement_is_an_outcome_too`.
- [x] 2.6 `dry_run` threads through to the engine and produces a report that names what WOULD be written, with nothing on disk changed (D4). Assert the "nothing changed" half by hashing the tree before and after, not by trusting the flag. [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline] **DONE 2026-08-19.** `dry_run` threads through; the test hashes every file in the project before and after and asserts the tree is unchanged, rather than trusting the flag — and asserts the report still says what it WOULD write, because a preview that reports nothing is indistinguishable from one that found nothing.
- [x] 2.7 A name-taking resolver over the declaration-taking form (open question 2), so a route can ask for a module by name without the writer knowing how templates are located. [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline] **DONE 2026-08-19.** `resolve_module(name, *, root=None)`. The name is the **template directory's name**, because that is what `framework_capabilities()` reports and a caller can only ask for what it was shown; a resolver accepting a different spelling would answer *no such module* for something the reader is looking at. Held by a test that asks the REAL reporter for its names and resolves every one. **Ambiguity is refused, never resolved** — two manifests under one name is a broken checkout or a shadowing plugin, and `glob()[0]` inside a decision that writes into somebody's repository is a guess wearing a result's clothes. Three mutations caught; a fourth was an equivalent mutant (widening the glob to `*.yaml` finds the same file, since nothing else lives there) and was re-aimed at the name agreement itself.

## 3. The divergence this change found, and must not inherit quietly

- [x] 3.1 Make `profile_deploy` honour `executable:` the same way `plan_files` does, so the two manifest readers agree for a reason rather than because **no manifest declares one** (measured: 0). Until this lands, `set-project init` would copy a module's executable part into a project while `install_module` excludes it. [REQ: the-files-a-module-places-are-declared-and-its-executable-part-is-never-among-them] ⚠ **NOT DONE — the finding it rests on was WRONG, and the correction is worth more than the task.** The design claimed the deploy parser and the declaration parser diverge on `executable:`, agreeing only because no shipped manifest uses the key. I implemented the exclusion, and the test written to prove it FAILED — with `ManifestValidationError`. Measured: `validate_declaration` runs at `profile_deploy.py:474`, **before** `_resolve_file_list` at `:482`, and it REFUSES a path declared both as an installed file and as the executable part (`guard-cannot-be-applied`). So the only input that could separate the two readers never reaches the second one; the divergence does not exist on any reachable path; and the code I had just written was **unreachable code claiming to be a guard** — the same shape as a manifest declaring a protection nothing reads, which this repository has already paid for from the other direction. Reverted to a comment naming why the obvious fix is wrong, so the next reader does not re-derive it. The guarantee is the refusal, and it is stronger than an exclusion: it stops the install instead of quietly installing a subset.
- [x] 3.2 Hold the agreement with a test that feeds ONE manifest to both readers and asserts identical paths — including a manifest that declares an executable path, which is the only input that can tell them apart. A test over the three real manifests would pass on both the fixed and the broken build. [REQ: the-files-a-module-places-are-declared-and-its-executable-part-is-never-among-them] **DONE 2026-08-19, aimed at the real guarantee.** Two tests: a manifest declaring a path as both is REFUSED by the deploy path and leaves the project untouched (asserting that even the *first* file was not written); and the valid spelling — an executable named but not listed among `core` — is simply never planned, which is what *structural* means here. The originally-planned test (feed one manifest to both readers) is kept as well, because it is the only input that can tell them apart and a test over the three shipped manifests would pass on either build.
- [x] 3.3 Stop `read_project_declaration` logging the absolute project path (design D5). The boundary is persistence, not naming: a count is safe to log, a path is not. Hold it with a caplog test, and prove the detector fires. [REQ: a-projects-declaration-is-read-as-what-it-says-and-absence-is-not-emptiness] **DONE 2026-08-19.** A `_shape()` helper logs the project-relative tail (`set/modules.yaml`), never the root, at all four log sites in this module — and the two failure paths log the exception's TYPE rather than its text, since a parser's message quotes the file. The rule has one owner rather than four copies. Two tests: no site names the project, driven over **all three** failure paths, and the detector proven to fire. ⚠ Two mutations initially escaped and both were the fixture's fault, not the assertion's: the sweep never reached the not-a-mapping branch (added), and removing the anchor list leaks nothing because the fallback is `path.name` — safe but useless, so the *debugging* half needed its own test. ⚠ **And fixing this turned `test_the_neighbouring_module_still_logs_a_project_path` red** — a test written to hold the finding until it was repaired. That red was the design working; it is now inverted and keeps the history.

## 4. Proof — the unhappy paths, which are the ones a demo never reaches

- [x] 4.1 An install into a project that has none of the module's files: every planned path present afterwards, every one named in the report. [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline] **DONE.**  `test_the_declared_files_are_written_and_every_one_is_named`.
- [x] 4.2 An install where the project edited a protected file: the file is untouched **byte for byte** (compare the hash, not the mtime) and the skip carries its reason. [REQ: the-install-reports-what-it-did-not-do-and-says-so-when-it-changed-nothing] **DONE.** Hash compared, not mtime: `test_a_file_the_project_edited_is_skipped_with_its_reason_and_left_byte_identical`.
- [x] 4.3 An install that skips EVERY file: `changed_nothing` is true, the report states it in its own words, and the caller cannot mistake it for success. [REQ: the-install-reports-what-it-did-not-do-and-says-so-when-it-changed-nothing] **DONE.** `test_an_install_that_skipped_everything_says_it_changed_nothing`.
- [x] 4.4 A refused install: the missing requirement is named, and the project contains **none** of the module's files — assert the first path in the plan specifically, because a refusal after one write is the failure this ordering exists to prevent. [REQ: a-missing-requirement-is-a-refusal-before-anything-is-written] **DONE.** `test_the_refusal_precedes_the_first_write` — the first planned path asserted by name.
- [x] 4.5 A refused install leaves the install record byte-identical, including when a record already existed for another module. [REQ: what-is-installed-is-recorded-and-the-record-is-written-only-after-the-write] **DONE.** `test_a_refused_install_leaves_the_record_alone`, with a record already naming another module.
- [x] 4.6 Installing one module in a project that has two: no file belonging only to the other is written. [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline] **DONE.** `test_installing_one_module_leaves_another_modules_files_alone` — two modules installed, one's file removed by the project, the other re-installed, and the removed file must stay removed.
- [x] 4.7 Mutation-test every test above: for each, break the specific line it guards and confirm it fails. A test that passes without its fix proves nothing and looks like proof forever. Use a unique-pattern assertion (`assert count == 1`), clear `__pycache__`, set `PYTHONDONTWRITEBYTECODE=1`, and **verify the restore** by re-grepping the file. [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline] **DONE — 24 mutations across this change, every one of them re-aimed until it measured the code and not the harness.** Five NOT-CAUGHT results, and none was a weak test: one equivalent mutant (deleting the `unchanged` branch falls through to an identical `else`), one glob widening that finds the same single file, one test asserting the wrong thing (a mutable `outcomes=[]` default still returns the prose), and two fixtures that never reached the branch being mutated. **The loop accuses the TEST every time; four of the five times it was wrong**, and following that accusation means weakening tests that were right. Every restore verified by re-reading the file and re-counting the pattern; `PYTHONDONTWRITEBYTECODE=1` and `__pycache__` cleared between runs, because two mutations of one file can produce byte-identical sizes and reuse each other's bytecode.
- [x] 4.8 Regression check against a real baseline: `git worktree add --detach`, `PYTHONPATH` at the baseline's own source roots, the session-end leak assertion, and a set-diff of failure names. An empty diff, not a matching count. [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline] **DONE.** Isolated worktree baseline, `PYTHONPATH` at its own source roots, session-end leak assertion (**0 leaks**), failure-name set diff: **107 vs 107, empty**. Counts moved (3748 → 3798 passing) and the failure SET did not, which is the only comparison that means anything.
- [x] 4.9 A module with NO announcement, installed: the install record names it afterwards. This is the case that fails today and the one the capability report is starved by; a test suite whose every fixture announces would pass on the broken build. [REQ: what-is-installed-is-recorded-and-the-record-is-written-only-after-the-write] **DONE.** `test_a_module_with_no_announcement_is_still_recorded`.

## 5. Hand-off to the surface

- [x] 5.1 Unblock `fleet-view` 6.5: the route calls `install_module` and returns its report unchanged. The route SHALL NOT interpret, summarise or reorder the report — the surface's obligations are asserted in `fleet-view` 7.15/9.17 and they assert what the SCREEN shows. [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline] **DONE 2026-08-19 — `POST /api/fleet/projects/{name}/install`.** The route decides exactly two things and delegates everything else. **(1) Which targets it accepts:** only a project the SCREEN listed, refused with 404 otherwise — accepting any path that exists would let this endpoint write into a directory nothing on the screen ever offered, and the screen is what made the target legible to whoever clicked. A listed project whose directory is gone is a 409 *before* the installer runs, because a listing is a memory and this route is about to write. **(2) `dry_run` defaults to TRUE**, and the default is the decision: every other write into a consumer tree here is approached through a preview, and a route whose default writes makes *I clicked it to see what it does* a destructive act. The report is returned **unchanged** — written, skipped with reasons, `changed_nothing` as its OWN field rather than left to be derived from an empty list (`len(written) == 0` is exactly the check that reads as success). A refusal is **409, not 400**: the request is well-formed and the project is real; what is wrong is the state, and that distinction tells a reader whether to fix their click or fix their project.
- [x] 5.2 Update `fleet-view`'s tasks 6.5, 7.15 and 9.17 to record that the blocker is gone, naming this change. A blocker that is lifted and not recorded reads as still blocked, and the next session re-derives it. [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline] **DONE 2026-08-19.** `fleet-view` 6.5 is marked done with the correction that unblocked it (the write was never missing; the account of it was), and 7.15 / 9.17 carry an explicit *no longer blocked* note naming this change and the route's shape. A blocker that is lifted and not recorded reads as still blocked, and the next session re-derives it — which is the failure this task exists to prevent, and which this change has now been on the receiving end of twice in one day.

## Acceptance Criteria (from spec scenarios)

- [x] AC-1: WHEN the project has no declaration file THEN the result reports that no declaration is present, and no module is reported as wanted [REQ: a-projects-declaration-is-read-as-what-it-says-and-absence-is-not-emptiness, scenario: a-project-that-declared-nothing]  ← `test_an_absent_project_declaration_is_reported_as_absent_not_as_empty`
- [x] AC-2: WHEN the project has a declaration file that names no modules THEN the result reports that a declaration IS present, with no modules wanted [REQ: a-projects-declaration-is-read-as-what-it-says-and-absence-is-not-emptiness, scenario: a-project-that-declared-an-empty-set]  ← `test_a_declaration_naming_no_modules_is_present_and_empty`
- [x] AC-3: WHEN the declaration file is not readable as its documented format THEN the result reports no declaration present, and the reader does not raise [REQ: a-projects-declaration-is-read-as-what-it-says-and-absence-is-not-emptiness, scenario: a-declaration-that-cannot-be-parsed]  ← `test_a_declaration_that_cannot_be_read_is_reported_as_absent_not_raised (4 shapes)`
- [x] AC-4: WHEN an install writes at least one file THEN the install record names that module at the version installed [REQ: what-is-installed-is-recorded-and-the-record-is-written-only-after-the-write, scenario: a-successful-install-updates-the-record]  ← `test_a_module_with_no_announcement_is_still_recorded`
- [x] AC-5: WHEN an install is refused for a missing requirement THEN the install record is unchanged, and does not name the refused module [REQ: what-is-installed-is-recorded-and-the-record-is-written-only-after-the-write, scenario: a-refused-install-leaves-the-record-alone]  ← `test_a_refused_install_leaves_the_record_alone`
- [x] AC-6: WHEN a module declares a path as its executable part THEN that path is absent from the planned file list [REQ: the-files-a-module-places-are-declared-and-its-executable-part-is-never-among-them, scenario: an-executable-path-is-not-planned]  ← `test_an_executable_declared_without_being_an_installed_file_is_simply_not_planned`
- [x] AC-7: WHEN a module declares one path both as an installed file and as executable THEN the path is excluded from the plan and the contradiction is reported [REQ: the-files-a-module-places-are-declared-and-its-executable-part-is-never-among-them, scenario: a-path-declared-as-both-is-excluded-and-named]  ← `test_a_manifest_that_declares_a_path_as_both_is_refused_by_the_deploy_path`
- [x] AC-8: WHEN a project expects one version and another is installed THEN the comparison reports disagreement, naming both versions [REQ: a-version-comparison-never-reports-cannot-tell-as-agreement, scenario: expected-and-installed-differ]  ← `test_a_project_running_a_version_it_did_not_ask_for_is_reported_as_a_mismatch`
- [x] AC-9: WHEN a project expects a module that is not installed THEN the comparison reports undeterminable, and does not report agreement [REQ: a-version-comparison-never-reports-cannot-tell-as-agreement, scenario: a-module-wanted-but-not-installed]  ← `test_a_module_asked_for_but_not_installed_is_unknown_never_a_match`
- [x] AC-10: WHEN the project has no declaration THEN no comparisons are produced [REQ: a-version-comparison-never-reports-cannot-tell-as-agreement, scenario: no-declaration-yields-no-comparisons]  ← `test_a_project_that_declared_nothing_reports_no_versions_rather_than_agreement`
- [x] AC-11: WHEN an install runs for a module in a project that has none of its files THEN each planned file is present in the project afterwards, and each is named in the report [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline, scenario: declared-files-are-written]  ← `test_the_declared_files_are_written_and_every_one_is_named`
- [x] AC-12: WHEN an install would write a protected file the project has edited THEN the file is left as the project has it, and the skip appears in the report with its reason [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline, scenario: a-file-the-project-modified-is-not-overwritten]  ← `test_a_file_the_project_edited_is_skipped_with_its_reason_and_left_byte_identical`
- [x] AC-13: WHEN an install runs for one module in a project that has two installed THEN no file belonging only to the other module is written [REQ: an-install-writes-the-modules-declared-files-through-the-existing-per-file-deploy-discipline, scenario: installing-one-module-leaves-another-alone]  ← `test_installing_one_module_leaves_another_modules_files_alone`
- [x] AC-14: WHEN an install is asked for a module that requires another the project does not have THEN the install is refused, the missing requirement is named, and no file is written [REQ: a-missing-requirement-is-a-refusal-before-anything-is-written, scenario: a-module-whose-requirement-is-absent]  ← `test_a_module_whose_requirement_is_absent_is_refused_and_the_requirement_is_named`
- [x] AC-15: WHEN an install is refused for a missing requirement THEN the project contains none of that module's files, including the first one in the plan [REQ: a-missing-requirement-is-a-refusal-before-anything-is-written, scenario: the-refusal-precedes-the-first-write]  ← `test_the_refusal_precedes_the_first_write`
- [x] AC-16: WHEN an install leaves files alone THEN each such file appears in the report with the reason it was skipped [REQ: the-install-reports-what-it-did-not-do-and-says-so-when-it-changed-nothing, scenario: skips-are-named-with-reasons]  ← `test_a_file_the_project_edited_is_skipped_with_its_reason_and_left_byte_identical`
- [x] AC-17: WHEN an install writes no files THEN the report states that outcome explicitly, rather than reporting a plain success [REQ: the-install-reports-what-it-did-not-do-and-says-so-when-it-changed-nothing, scenario: a-run-that-wrote-nothing-says-so]  ← `test_an_install_that_skipped_everything_says_it_changed_nothing`
- [x] AC-18: WHEN an install writes some files and skips others THEN the report names both sets, and neither is inferable only from the other's absence [REQ: the-install-reports-what-it-did-not-do-and-says-so-when-it-changed-nothing, scenario: both-halves-appear-together]  ← `test_the_report_names_both_halves_when_it_wrote_some_and_skipped_others`
