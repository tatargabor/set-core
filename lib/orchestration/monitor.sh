#!/usr/bin/env bash
# lib/orchestration/monitor.sh — STUB
#
# The monitor loop runs in Python via `wt-orch-core engine monitor`.
# cmd_start() in dispatcher.sh exec's directly to the Python monitor.
#
# Python implementation: lib/wt_orch/engine.py
#
# This file is sourced by wt-orchestrate but contains no active code.
# It exists only for backward compatibility with source chains.
