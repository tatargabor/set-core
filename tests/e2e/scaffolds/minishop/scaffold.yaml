project_type: web
template: nextjs
ui_library: shadcn

# v0.app design source. The runner clones this repo into the scaffold's
# v0-export/ directory at run time. Pin ref to a tag/commit/branch for
# reproducibility once the design is stable.
#
design_source:
  type: v0-git
  repo: https://github.com/tatargabor/v0-minishop-e-commerce-demo.git
  ref: set-author-fixes
