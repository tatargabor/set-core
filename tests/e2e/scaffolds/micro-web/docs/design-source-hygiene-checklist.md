# Design Source Hygiene — micro-web

Auto-generated by `set-design-hygiene` from `v0-export/` on 2026-04-26 08:14 UTC.
32 findings: 0 CRITICAL, 0 WARN, 32 INFO.

## CRITICAL (blocks design adoption)

_No findings at this severity._

## WARN (degrades agent quality)

_No findings at this severity._

## INFO (potential cleanup)

- [ ] **hardcoded-ui-strings** — `app/about/page.tsx:91`
      Hardcoded UI string in JSX body: 'About Us'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `app/about/page.tsx:117`
      Hardcoded UI string in JSX body: 'Our Team'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `app/blog/[slug]/not-found.tsx:10`
      Hardcoded UI string in JSX body: 'Post not found'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `app/blog/page.tsx:106`
      Hardcoded UI string in JSX body: 'Blog'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **inline-lambda-handler** — `app/blog/page.tsx:137`
      Inline lambda handler with 3+ line body
      _Fix:_ Extract to a prop callback or named function

- [ ] **hardcoded-ui-strings** — `app/contact/page.tsx:6`
      Hardcoded UI string in JSX body: 'Contact'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `app/page.tsx:61`
      Hardcoded UI string in JSX body: 'Welcome to Micro Web'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `app/page.tsx:68`
      Hardcoded UI string in JSX body: 'Learn more'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `app/page.tsx:90`
      Hardcoded UI string in JSX body: 'Stay in the loop'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `app/page.tsx:95`
      Hardcoded UI string in JSX body: 'Subscribe to updates'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `app/page.tsx:99`
      Hardcoded UI string in JSX body: 'Subscribe'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/blog-post-content.tsx:135`
      Hardcoded UI string in JSX body: 'Home'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/blog-post-content.tsx:141`
      Hardcoded UI string in JSX body: 'Blog'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/blog-post-content.tsx:191`
      Hardcoded UI string in JSX body: 'Share'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/blog-post-content.tsx:201`
      Hardcoded UI string in JSX body: 'Share on Twitter'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/blog-post-content.tsx:209`
      Hardcoded UI string in JSX body: 'Share via Email'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/command-palette.tsx:35`
      Hardcoded UI string in JSX body: 'Home'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/command-palette.tsx:39`
      Hardcoded UI string in JSX body: 'About'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/command-palette.tsx:43`
      Hardcoded UI string in JSX body: 'Blog'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/command-palette.tsx:47`
      Hardcoded UI string in JSX body: 'Contact'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/command-palette.tsx:58`
      Hardcoded UI string in JSX body: 'Understanding React Hooks'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **inline-lambda-handler** — `components/contact-wizard.tsx:173`
      Inline lambda handler with 6+ line body
      _Fix:_ Extract to a prop callback or named function

- [ ] **hardcoded-ui-strings** — `components/contact-wizard.tsx:181`
      Hardcoded UI string in JSX body: 'Get in touch'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/contact-wizard.tsx:185`
      Hardcoded UI string in JSX body: 'Contact Us'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/contact-wizard.tsx:196`
      Hardcoded UI string in JSX body: 'Name'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/contact-wizard.tsx:207`
      Hardcoded UI string in JSX body: 'Email'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/contact-wizard.tsx:224`
      Hardcoded UI string in JSX body: 'Subject'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **inline-lambda-handler** — `components/contact-wizard.tsx:248`
      Inline lambda handler with 3+ line body
      _Fix:_ Extract to a prop callback or named function

- [ ] **hardcoded-ui-strings** — `components/site-header.tsx:55`
      Hardcoded UI string in JSX body: 'Micro Web'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/site-header.tsx:106`
      Hardcoded UI string in JSX body: 'Micro Web'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **hardcoded-ui-strings** — `components/site-header.tsx:107`
      Hardcoded UI string in JSX body: 'Navigation menu'
      _Fix:_ Expected in v0-design HU canonical preview — consumer transforms via i18n catalog (`t('key')`). Audit only if v0 needs design-time multi-locale rendering.

- [ ] **inline-lambda-handler** — `components/site-header.tsx:130`
      Inline lambda handler with 3+ line body
      _Fix:_ Extract to a prop callback or named function
