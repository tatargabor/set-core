[project]
name = "set-mcp-server"
version = "0.1.1"
description = "MCP server for SET (ShipExactlyThis) - exposes worktree and Ralph status to AI agents"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=0.1.0",
]

[project.scripts]
set-mcp-server = "set_mcp_server:main"
